# TODO

- [x] Rename backend interactive session API, events, and runtime symbols from `chat` to `session`
- [x] Rename frontend routes, state, components, styles, and UI copy from `chat` to `session`
- [x] Update tests and docs for the new session terminology and paths
- [x] Run validation (`ruff`, `pytest`, `lint`, `typecheck`, `web:build`, `docs:build`)
- [x] Append `MEMORY.md` entry for the rename task
