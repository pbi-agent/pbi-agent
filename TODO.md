# TODO

[X] Replace tool_group header (Badge + Button) with shadcn Collapsible
[X] Update session.css: remove count + tool-item rules, switch chevron rotation to data-state
[X] Verified pre-existing test/lint/typecheck failures are unrelated (same set on baseline)
[X] Run validation: bun run test:web (84 pass / 2 pre-existing fail), lint (2 pre-existing), typecheck (4 pre-existing in ToolResult.tsx), web:build OK
