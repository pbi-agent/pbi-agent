# MEMORY.md

## Metadata
- Last compacted: 2026-05-03
- Scope: durable repo memory + active-day task events.
- Format: only `Metadata`, `Long-Term Memory`, and `Detailed Task Events`.

## Long-Term Memory
- Memory: token-efficient; keep durable decisions/follow-ups only, not logs.
- Workflow: no commit/push/merge/PR unless asked. Use session-only `TODO.md`; markers `[ ]`, `[>]`, `[X]`, `[!]`, `[-]`. Shell from workspace root; use `python3` for direct Python shell commands.
- Validate touched surface: Python Ruff+pytest; frontend Bun tests/lint/typecheck/build; docs build. Note skipped/hung checks.
- Architecture: CLI `src/pbi_agent/__main__.py` -> `src/pbi_agent/cli.py`; default command `web`; backend FastAPI `src/pbi_agent/web/`; frontend Vite React `webapp/`.
- API changes: align routes/schemas/session manager with `webapp/src/api.ts` + `webapp/src/types.ts`.
- Web API contract: `bun run web:api-types` runs `scripts/generate_api_types.py` to regenerate `webapp/src/api-types.generated.ts` from FastAPI OpenAPI; pytest `test_api_types_codegen.py` enforces generated output is current.
- Constraints: provider/tool HTTP via `urllib.request`; internal data in `~/.pbi-agent/`; no migrations/backcompat.
- Tools: built-in `python_exec`, `list_files`, `search_files` removed; use shell (`rg --files`, `find`, `rg -n`) and Python as needed.
- Project imports: top-level `skills`, `commands`, `agents`; public catalogs under `pbi-agent/{skills,commands,agents}`; local install to `.agents/...`.
- Sub-agents: `.agents/agents/*.md`; frontmatter only `name`/`description`; child model from active profile/CLI + `sub_agent_model`.
- Local commands: `plan` no questions; `review` Markdown; `fix-review` stops on `No findings.`; `ship-task` branch/merge helpers; `release` needs explicit publish wording and no history rewrite after PR unless approved.
- Release docs: `release-writing` skill governs releases; changelog index `docs/changelog/index.md`, files `docs/changelog/v<version>.md`, VitePress sidebar links releases; release retrospective added validation triage + publish verification + continuation gate safety.
- Branding: public copy = local coding agent; preserve `pbi-agent`, `pbi_agent`, `PBI_AGENT_*`, `~/.pbi-agent`; logo `src/pbi_agent/web/static/logo.jpg`.
- Web/session: saved sessions use session-scoped APIs; `/api/live-sessions/*` and `/sessions/live/:id` are removed from user-facing test expectations. SQLite leases use `BEGIN IMMEDIATE`/`busy_timeout`; duplicate manager startup emits one warning.
- Web events: frontend app/session streams use SSE over `GET /api/events/{stream}` and `/api/events/sessions/{session_id}` with `server.connected`/heartbeat and `since`/`Last-Event-ID`; legacy WebSocket routes remain during transition.
- Web message identity: saved history/live replay use canonical persisted item/message ids `msg-<messages.id>` plus stable part ids; optimistic/display-local live items are replaced by `message_rekeyed` once persisted.
- Web durable runs: `run_sessions` has web projection fields/status, `snapshot_json` caches timeline from persisted `web_event` records, unfinished web runs become `stale` on startup, and web `RunTracer` attaches to canonical run ids.
- Web durable event replay: saved-session event streams reconstruct from persisted `web_event` records with original `seq`/`created_at`, so SSE `since` and `Last-Event-ID` cursors remain valid after restart.
- Session compaction: clear completed `previous_id`; resume restores persisted user/assistant history; compaction includes unanswered trailing user turns and same-turn tool exchanges.
- Providers: OpenAI Responses use `instructions` + `previous_response_id`; ChatGPT subscription prepends system prompt; Codex transport is WebSocket-only (`chatgpt_codex_backend.py`), no unsupported compression.
- Web UI: prefer shadcn tokens/components; overlays use shared readable spacing; large modals need safe gutters + inner scroll; Radix tooltip `collisionPadding`/`sideOffset` may not protect cross-axis viewport gutters, so clamp both axes for edge-adjacent tooltips.
- Timeline: work runs coalesce thinking/tool groups; running closed by default; final assistant closes current collapsibles; file-edit results share `FILE_EDIT_TOOL_NAMES`.
- Syntax highlighting: `read_file` uses lazy Shiki code block; root `package.json` pins exact `shiki` `4.0.2`.
- v0.1.0 shipped.
- Kanban task images: stored as task `image_attachments_json`, task create/update accepts `image_upload_ids`, uploads reuse `~/.pbi-agent/web_uploads` + `/api/uploads/{id}` previews, initial task run sends images only with first full prompt.
- Provider history restore preserves user message image attachments for image-capable providers (OpenAI/ChatGPT, Anthropic, Google) by reloading upload IDs from `~/.pbi-agent/web_uploads`.
- Web server startup: same-workspace active lease is rejected; implicit default port `8000` may auto-select a free port, explicit `--port` stays strict.
- Web UI session context gauge: compact donut uses top-level `session_usage.context_tokens` with turn fallback over runtime `compact_threshold`; tooltip is custom fixed-position with both-axis viewport clamping.
- Kanban: task cards omit technical project/session metadata and image counts; active task sessions show a Stop shortcut whose pending state is tied to the interrupt mutation.
- Saved sessions: sidebar supports inline title edit via `PATCH /api/sessions/{session_id}` with cache/app-event invalidation.
- Interactive ask-user: Web per-message `interactive_mode` exposes `ask_user`; pending questions persist in snapshots and are answered via session-scoped question-response endpoints; topbar uses shadcn `Toggle` and `UserQuestionsPanel`.
- UserQuestionsPanel UX: one question at a time with shadcn Card, selected filled Button state, keyboard nav, auto-focus active option, Prev/Next + dots, and custom textarea retained in fieldset.
- Ask-user notifications: frontend-only localStorage prefs for desktop/sound; AppShell effect fires hidden/unfocused browser notifications/sound, deduped by live/session + prompt id, click focuses/navigates to waiting session.

## Detailed Task Events
## 2026-05-03
- Removed remaining frontend tests/assertions for legacy live-session API/route helpers: dropped stale `/sessions/live` SessionPage cases, BoardPage `fetchLiveSessions`/`interruptLiveSession` cases, the unbound ended-session live-route notification fallback, and the old `uploadSessionImages` API test. Validation: no web test/spec references to legacy helper names or `/api/live-sessions`; `bun run test:web -- api SessionPage BoardPage SessionEndedNotificationEffects`, `bun run lint`, and `bun run typecheck` passed.
- Fixed saved-session API validation after run projection refactor: session/live projections map run outcomes like `completed`/`interrupted` to lifecycle `ended`, `started` to `running`, exclude interrupted runs from active-run lookup, and ignore empty `{}` run snapshots for session timelines. Validation: targeted `tests/test_web_serve.py` session status tests + `tests/test_session_store.py::test_create_and_list_run_sessions`, Ruff check, and Ruff format check passed.
- Fixed review findings for session API/UI refactor: persisted saved-session web events replay by sequence order, lazy `/sessions` composer can send/attach before a live run id exists, and direct send paths use an in-flight pending guard to block duplicate submits. Validation: targeted replay pytest, `bun run test:web -- SessionPage Composer`, `bun run lint`, `bun run typecheck`, Ruff check/format-check, and `git diff --check` passed.
- Fixed blank `/sessions` route after session refactor: it no longer reuses the previous active saved session key/live id, so first startup and New Session shortcut show disconnected blank state without opening stale live-run websocket. Validation: `bun run test:web -- SessionPage`, `bun run lint`, `bun run typecheck`, and `git diff --check` passed.
- Fixed web-event persistence collision: web events use negative `step_index` again to avoid unique-key conflicts with positive observability steps, while replay still sorts by stored metadata `seq`. Validation: targeted saved-session replay pytest and Ruff check/format-check passed.
- Fixed repeated `input_state enabled=true` event spam: `WebDisplay` now emits input-state events only on state changes while `user_prompt()` waits. Session `7a3b358c975141efb2b471be2788acc6` showed 146 duplicate enabled events from the 0.5s wait loop. Validation: targeted WebDisplay pytest, Ruff check/format-check, and `git diff --check` passed.
- Reduced websocket snapshot replay noise: event streams accept `?since=<lastEventSeq>` and frontend session sockets pass the store cursor, so hydrated saved sessions no longer receive already-applied snapshot events on connect/reconnect. Session `df1dd9c8c3724c65a22a991f7074f27d` had only 20 persisted web events; remaining noise was replay. Validation: targeted event-stream pytest, `bun run test:web -- useLiveSessionEvents`, `bun run lint`, `bun run typecheck`, Ruff check/format-check passed.
- Run Detail excludes persisted `web_event` rows from the observability event list; they remain stored for saved-session websocket replay. Validation: targeted `test_get_run_detail_returns_observability_events` and Ruff check/format-check passed.
- Fixed Run Detail/History frontend status mapping for saved web runs: `ended` now renders as terminal/completed style, `stale` is terminal, and only `started`/`running` appear active. Session `221b5dbce2984d248cd8db9788dc7849` had DB run status `ended`; the stale display was frontend-only. Validation: `bun run test:web -- RunDetailModal RunHistory`, `bun run lint`, `bun run typecheck`, and `git diff --check` passed.
- Normalized run API statuses to session lifecycle statuses: run detail/history/all-runs now serialize storage `completed`/`interrupted` as `ended` and `started` as `running`; `/api/runs?status=ended` matches storage `ended`/`completed`/`interrupted`. Frontend `RunSession.status` is `SessionStatus`, so components no longer branch on both `completed` and `ended`. Validation: focused web API pytest, `bun run test:web -- RunDetailModal RunHistory`, Ruff check/format-check, `bun run lint`, `bun run typecheck`, and `git diff --check` passed.
- Removed mistaken turn-status edits that conflated a live chat session waiting for input with an individual run status; no code/test remains for mapping input-enabled websocket state to `waiting_for_input` run rows. Validation: targeted `rg`, Ruff check/format-check, and `git diff --check` passed.
- Fixed web run status model correctly: web workers no longer pass the long-lived live session id as `run_session_id` into `run_session_loop`, so each assistant turn creates/finalizes its own `session_turn` run row; run history/all-runs/dashboard/latest-run queries exclude internal `web_session` projection rows. Validation: targeted web serve pytest, Ruff check/format-check, and `git diff --check` passed.
- Fixed lazy saved-session title generation: blank sessions created before first submit now get their title from the first submitted message text in `submit_saved_session_input`, while existing/manual titles are preserved. Validation: targeted web serve pytest, Ruff check/format-check, and `git diff --check` passed.
- Fixed composer flashing during tool/model handoffs: SessionPage no longer enables input just because `processing` temporarily clears; active live sessions now require explicit `input_state=true`, while blank `/sessions` and saved sessions without a live run remain sendable. Validation: `bun run test:web -- SessionPage`, `bun run lint`, `bun run typecheck`, and `git diff --check` passed.
- Fixed initial chat readiness UI/focus: sendable idle pages show `ready` instead of disconnected before any websocket exists, and composer autofocus follows the same sendability gate. Validation: `bun run test:web -- SessionPage`, `bun run lint`, `bun run typecheck`, and `git diff --check` passed.
- Fixed completed Kanban task sessions rendering twice: session detail keeps ended runs for status/history but no longer returns ended run projections as active live sessions or replayable timelines, so the frontend does not open a completed-run event stream over saved history. Validation: focused `test_web_serve.py` session-detail tests, Ruff check/format-check, `bun run test:web -- SessionPage`, and `git diff --check` passed.
- Removed remaining legacy live API compatibility references: deleted frontend `fetchLiveSessions`/`fetchLiveSessionDetail`, removed old `/sessions/live` redirect route and unused live-session schema module, and decoupled latest-run status from active run ids in session list/detail. Validation: no legacy live API/path references remain outside built static assets; focused pytest, Ruff check/format-check, `bun run test:web -- AppShell SessionPage`, `bun run lint`, `bun run typecheck`, and `git diff --check` passed.
- Fixed completed Kanban task continuation: when a bound live run emits `session_state: ended`, the frontend store detaches the finished `liveSessionId` and leaves the saved session continuable instead of marking the conversation ended. Validation: `bun run test:web -- store SessionPage`, `bun run lint`, `bun run typecheck`, and `git diff --check` passed.
- Fixed saved-session composer continuation after completed Kanban tasks: `SessionPage` now lets `Composer` start a new run for an existing saved session with no active `liveSessionId`, matching the backend continuation path. Validation: `bun run test:web -- SessionPage store`, `bun run lint`, `bun run typecheck`, `bun run web:build`, and `git diff --check` passed.
- Restored working-trace hydration after restart: session detail now returns a static `timeline` from the latest web-session projection while keeping active run/live fields null; frontend hydrates ended saved-session snapshots without opening a live socket or disabling continuation. Validation: focused web serve pytest, `bun run test:web -- SessionPage store`, Ruff check/format-check, `bun run lint`, `bun run typecheck`, `bun run web:build`, and `git diff --check` passed.
- Summarized root `main.py`: tiny executable wrapper importing `pbi_agent.__main__.main` and exiting with its return code. Validation: read-only inspection.
- Added `pbi-agent kanban create` CLI task creation backed by `SessionStore`: required `--title`/`--desc` (aliases `--description`/`--prompt`), optional lane aliases resolving existing board stages, workspace-confined `--project-dir`, optional `--session-id`, and `--json` output; docs updated. Validation: `uv run pytest -q --tb=short -x tests/test_cli.py`, `uv run pytest -q --tb=short -x tests/test_session_store.py`, Ruff check/format-check, `bun run docs:build`, and `git diff --check` passed.
- Fixed review finding for `pbi-agent kanban create`: non-sluggable `--lane` values now fall through to the controlled unknown-stage error instead of raising `ConfigError`; regression test covers `--lane "!!!"`. Validation: focused Kanban CLI pytest and Ruff check/format-check passed.
- Fixed saved-session display during active Kanban/session continuations: SessionPage now overlays active run timeline snapshots after prior saved history items and dedupes the current turn's duplicated message, so previous conversation remains visible while live trace hydrates. Diagnosis used session `b70cf93988c14df49854d5e2fdeb17d7`: DB `messages` had all turns, but `pbi-agent.html` showed only the active run snapshot because `hydrateLiveSnapshot` replaced history items. Validation: `bun run test:web -- SessionPage`, `bun run lint`, `bun run typecheck`, and `git diff --check` passed.
- Added `pbi-agent kanban list`: lists current-workspace Kanban tasks, supports `--stage`/`--lane`/`--state` filtering by existing stage, outputs full human task detail blocks or `--json` arrays with serialized image attachment metadata; docs updated. Validation: Kanban CLI pytest, Ruff check/format-check, `bun run docs:build`, and `git diff --check` passed.
- Summarized root `LICENSE`: MIT License for 2026 pbi-agent contributors. Validation: read-only inspection.
- Corrected active Kanban-started continuation display order: SessionPage now treats API `history_items` as canonical chronological messages and appends only non-duplicate live snapshot work/current items, because Kanban task continuation snapshots can contain current turn first followed by prior task messages. Diagnosis used session `5154354980684b0585abe7847a55bb41` and `pbi-agent.html`; DB messages were chronological, but run snapshot `7f22...` was ordered current user then previous task turns. Validation: `bun run test:web -- SessionPage`, `bun run lint`, `bun run typecheck`, and `git diff --check` passed.
- Fixed review finding in saved-session timeline merge: live snapshot historical messages and the duplicated latest user prompt are removed without suppressing valid repeated assistant live messages. Validation: `bun run test:web -- SessionPage`, `bun run lint`, `bun run typecheck`, and `git diff --check` passed.
- Fixed saved-session display for dormant web-session projections: when session detail has no active live/run but includes a non-ended persisted web timeline, SessionPage now overlays the canonical `history_items`, dedupes already-persisted snapshot messages, and marks the hydration static so it does not attach a stale liveSessionId/retry event stream. Diagnosis used session `a77ac666484948119722c69fed418522` and `pbi-agent.html`: DB messages were correct, but a still-`running` web_session projection with `session_ended=false` replaced history and caused `Retrying...`/`Working`. Validation: `bun run test:web -- SessionPage`, `bun run lint`, `bun run typecheck`, and `git diff --check` passed.
- Fixed saved-session display for idle active live timelines: `SessionPage` now treats active snapshots with `input_enabled=true` and no processing/wait/questions/error as completed-turn projections, deduping all persisted user/assistant snapshot messages against canonical `history_items` while keeping work-trace items and the live session attached. Diagnosis used session `77dcd2fcc76f4795978fc147517cd0c1`: `pbi-agent.html` showed `hi`/assistant twice because the active live snapshot still contained persisted messages. Validation: `bun run test:web -- SessionPage`, `bun run lint`, `bun run typecheck`, `bun run web:build`, and `git diff --check` passed.
- Started opencode-inspired communication refactor: added SSE event endpoints with connected/heartbeat control events, `since` plus `Last-Event-ID` resume support, safer subscribe-before-snapshot replay, and moved frontend app/session event hooks from WebSocket to EventSource while keeping legacy WebSocket endpoints. Validation: focused backend event pytest, focused event/api web tests, `uv run pytest -q --tb=short -x tests/test_web_serve.py`, Ruff check/format-check, `bun run lint`, `bun run typecheck`, `bun run web:build`, `git diff --check`, and `bun run test:web -- --maxWorkers=2` passed; default `bun run test:web` timed out in unrelated UI tests that passed individually.
- Added canonical persisted message identity for web timelines: history/API payloads, replay, task prompts, shell commands, and persisted worker messages use `msg-<id>` ids and part ids; live optimistic/user/display-local assistant items are rekeyed with `message_rekeyed`, and frontend reducer/timeline dedupe tracks canonical `message_id`. Validation: `uv run pytest -q --tb=short -x tests/test_web_serve.py`, `uv run pytest -q --tb=short -x tests/test_session.py`, `uv run pytest -q --tb=short -x tests/test_session_store.py`, Ruff check/format-check, `bun run lint`, `bun run typecheck`, `bun run web:build`, `bun run test:web -- --maxWorkers=2`, and `git diff --check` passed.
- Fixed durable saved-session event replay: reconstructed session event streams now load persisted `web_event` records with their original sequence/timestamp instead of republishing into a renumbered in-memory stream, and SSE resume over persisted events honors the durable cursor. Validation: targeted persisted replay pytest, full `tests/test_web_serve.py`, Ruff check/format-check, and `git diff --check` passed.
- Added generated FastAPI/OpenAPI TypeScript contract slice: new `scripts/generate_api_types.py`, generated `webapp/src/api-types.generated.ts`, `bun run web:api-types`, and a pytest freshness guard; frontend API mutations now consume generated request types for session create/update/input/question/shell payloads. Validation: API type codegen pytest, Ruff check/format-check, `bun run lint`, `bun run typecheck`, `bun run test:web -- api`, `bun run web:build`, and `git diff --check` passed.
