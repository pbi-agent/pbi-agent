# MEMORY.md

## Metadata
- Last compacted: 2026-04-21
- Scope: durable repo memory plus append-only detailed task events for the active day.
- Format rule: keep only `Metadata`, `Long-Term Memory`, and `Detailed Task Events`; compact prior-day detail into durable notes before appending new work.

## Long-Term Memory
- Workflow: do not commit, push, merge, or open PRs unless explicitly asked. Use `TODO.md` only for the current task session, reset it before substantive work, and keep markers compact: `[ ]`, `[>]`, `[X]`, `[!]`, `[-]`.
- Validation by surface: Python changes should run `uv run ruff check .`, `uv run ruff format --check .`, and relevant `uv run pytest ...`; frontend changes should run `bun run test:web`, `bun run lint`, `bun run typecheck`, and `bun run web:build`; docs changes should run `bun run docs:build`; broad handoff checks are the full repo Ruff, Bun lint/typecheck, and pytest commands.
- Environment quirk: `bun run lint` and occasionally focused `bun run test:web -- ...` have intermittently hung in this environment, so treat lint/test hangs as an environment risk unless a code-specific failure is evident.
- Architecture: CLI entry is `src/pbi_agent/__main__.py` -> `src/pbi_agent/cli.py`; default command is `web`, and runtime commands are `web`, `run`, `audit`, `init`, `sessions`, `config`, `skills`, `commands`, and `agents`.
- Web stack: FastAPI backend lives under `src/pbi_agent/web/` with routes in `web/api/routes/`, schemas in `web/api/schemas/`, orchestration in `web/session_manager.py`, display publishing in `web/display.py`, and startup helpers in `web/server_runtime.py`. The frontend is Vite + React + TypeScript in `webapp/`, and `bun run web:build` writes the built SPA into `src/pbi_agent/web/static/app`.
- API contract rule: when changing web API contracts, keep `src/pbi_agent/web/api/routes/`, `src/pbi_agent/web/api/schemas/`, `src/pbi_agent/web/session_manager.py`, `webapp/src/api.ts`, and `webapp/src/types.ts` aligned.
- Display/testing conventions: `src/pbi_agent/display/protocol.py` defines `DisplayProtocol`, console output lives in `src/pbi_agent/display/console_display.py`, and tests commonly use `tests/conftest.py::DisplaySpy`. Webapp tests live alongside source under `webapp/src/**/*.test.ts(x)` and should prefer behavior and state assertions over snapshots.
- Provider/runtime rules: outbound provider and tool HTTP must use `urllib.request`; provider abstractions live under `src/pbi_agent/providers/`; internal data belongs in `~/.pbi-agent/`; no migration or backward-compatibility shims should be added.
- Current product/provider shape: `chatgpt` and `github_copilot` are first-class subscription-backed provider kinds, distinct from API-key `openai`; provider-auth UI is metadata-driven; ChatGPT/Copilot account flows are implemented across CLI and web settings.
- Model discovery: settings-side model selection is runtime provider discovery, not the local catalog. Discovery supports ChatGPT Codex backend, Google Gemini, generic OpenAI-compatible `/models`, and GitHub Copilot `GET /models`; Copilot’s live default model is `gpt-5.4`.
- Project skills: `pbi-agent skills list` and `pbi-agent skills add` are the top-level project-skill commands. Installer logic lives in `src/pbi_agent/skills/project_catalog.py` and `project_installer.py`, supports public GitHub shorthand/repo/tree sources plus local filesystem sources, uses authenticated GitHub archive download with git fallback when needed, and only treats a root `SKILL.md` and public `skills/*/SKILL.md` entries as installable bundles.
- Project commands: `pbi-agent commands list` and `pbi-agent commands add` manage `.agents/commands/*.md` prompt presets. Command imports use the shared GitHub/local source materializer in `src/pbi_agent/project_sources.py`, default to public `commands/*.md` discovery, ignore repo-root `.agents/commands/*` unless explicitly targeted, and install normalized files into `.agents/commands/<slug>.md`. The official remote catalog is `pbi-agent/commands`.
- Project agents: `pbi-agent agents list` and `pbi-agent agents add` manage `.agents/agents/*.md` sub-agent definitions. Agent imports reuse the shared GitHub/local source materializer, default to public `agents/*.md` discovery, ignore repo-root `.agents/agents/*` unless explicitly targeted, and install normalized files into `.agents/agents/<name>.md`. The official remote catalog is `pbi-agent/agents`.
- Project sub-agents: discover only `.agents/agents/*.md`. Supported frontmatter is limited to `name` and `description`; sub-agent files are model-agnostic and child runtime selection comes from the active profile/CLI defaults plus `sub_agent_model`.
- Active follow-up: if more project-skill source edge cases appear, preserve the rule that source-repo internal `.agents/skills/*` bundles are never surfaced as downloadable or installable candidates.

## Detailed Task Events
### 2026-04-21
- Fixed project-skill frontmatter parsing for ignored YAML list-valued fields so discovery/install now tolerate indentless sequences like `tools:\n- read_file` on excluded keys while still extracting only `name` and `description`. Added regressions in `tests/test_frontmatter.py`, `tests/test_skill_discovery.py`, and `tests/test_project_skills.py`. Validation: `uv run pytest tests/test_frontmatter.py tests/test_skill_discovery.py -q`; `uv run pytest tests/test_project_skills.py -q -k 'indentless_sequence_fields or nested_metadata_fields'`; `uv run ruff check src/pbi_agent/frontmatter.py tests/test_frontmatter.py tests/test_skill_discovery.py tests/test_project_skills.py`; `uv run ruff format tests/test_skill_discovery.py`; `uv run ruff format --check src/pbi_agent/frontmatter.py tests/test_frontmatter.py tests/test_skill_discovery.py tests/test_project_skills.py`; `uv run pytest tests/test_frontmatter.py tests/test_skill_discovery.py tests/test_project_skills.py -q -k 'indentless_sequence or nested_metadata'`.
- Fixed a hang in `parse_simple_frontmatter()` introduced by the new `include_keys` mode: ignoring nested frontmatter blocks like `metadata:` used `continue` inside the inner scan loop without advancing `next_index`, causing install/discovery tests to block forever. The parser now skips from the nested block, breaks the inner loop, and resumes the outer walker cleanly. Validation: `timeout 20s uv run pytest tests/test_project_skills.py::test_install_project_skill_ignores_nested_metadata_fields -vv -s`; `uv run pytest tests/test_frontmatter.py tests/test_project_skills.py::test_install_project_skill_ignores_nested_metadata_fields tests/test_skill_discovery.py::test_nested_metadata_is_ignored_for_skill_discovery -q`; `uv run ruff check src/pbi_agent/frontmatter.py tests/test_frontmatter.py tests/test_project_skills.py tests/test_skill_discovery.py`.
- Relaxed project-skill frontmatter parsing so discovery/install extract only `name` and `description` while ignoring extra Anthropic-style fields like nested `metadata`, without re-allowing block-scalar `name` values. Added regressions in `tests/test_frontmatter.py`, `tests/test_skill_discovery.py`, and `tests/test_project_skills.py`. Validation: `uv run ruff check src/pbi_agent/frontmatter.py src/pbi_agent/agent/skill_discovery.py src/pbi_agent/skills/project_catalog.py tests/test_frontmatter.py tests/test_project_skills.py tests/test_skill_discovery.py`; `uv run ruff format --check` on the same files; targeted pytest runs printed progress for the selected cases but did not exit cleanly in this environment before shell timeouts.
- Relaxed the `SessionPage` saved-session resume regression to assert only the first `createLiveSession` variables argument, matching the current TanStack Query mutation callback signature while preserving the `resume_session_id` behavior check. Validation: `bun run typecheck`; `bun run test:web -- SessionPage.test.tsx` still hung after Vitest startup in this environment.
- Updated `.agents/commands/review.md` so the review command now requires plain Markdown output instead of a JSON object, while preserving the same finding metadata and overall-correctness fields. Validation: reread `.agents/commands/review.md` after patching and confirmed the JSON schema block was replaced by a Markdown-only output contract.
- Compressed prose in `.agents/commands/review.md` with the repo `compress` skill while preserving headings, numbered rules, bullets, inline literals, and the JSON schema/code fence block exactly. Validation: reread `.agents/commands/review.md` after patching and confirmed structure plus literal output-format content remained intact.
- Compressed prose in `.agents/commands/execute.md` with the repo `compress` skill while preserving headings, bullets, and inline examples/literals. Validation: reread `.agents/commands/execute.md` and verified heading structure plus inline backtick/example preservation with `sed -n` and `rg -n '^#|^##|`'`.
- Further compressed prose in `.agents/commands/plan.md` with the repo `compress` skill while preserving headings, bullet structure, and inline literals. Validation: reread `.agents/commands/plan.md` and verified `Plan Mode`, `PHASE 1`, `PHASE 2`, and absence of `request_user_input` / `<proposed_plan>` with `rg -n`.
- Tightened the shared frontmatter parser so manifest block scalars are allowed only for `description`, and fixed literal `|` block parsing to strip the actual block indent instead of preserving a leading space. Added regressions for invalid block-scalar `name` values in skill install/discovery and sub-agent discovery, plus a parser unit test for literal blocks. Validation: `uv run ruff check src/pbi_agent/frontmatter.py src/pbi_agent/skills/project_catalog.py src/pbi_agent/agent/skill_discovery.py src/pbi_agent/agent/sub_agent_discovery.py tests/test_frontmatter.py tests/test_project_skills.py tests/test_skill_discovery.py tests/test_sub_agent_discovery.py`; `uv run ruff format --check` on the same files; `uv run pytest tests/test_frontmatter.py tests/test_project_skills.py tests/test_skill_discovery.py tests/test_sub_agent_discovery.py -q`.
- Compressed prose in `.agents/commands/plan.md` with the repo `compress` skill while preserving headings, bullet structure, and inline literals. Validation: reread `.agents/commands/plan.md` and verified `request_user_input`, `<proposed_plan>`, headings, numbered items, and bullets with `rg -n`.
- Simplified `.agents/skills/compress` into a prompt-only skill by deleting all bundled Python scripts and rewriting `SKILL.md` to describe manual compression technique and explicit no-script/no-network safety rules. Validation: verified `.agents/skills/compress` now contains only `SKILL.md` and reread the rewritten skill body.
- Updated the limited frontmatter parser used by project skill discovery and install to support folded and literal YAML block scalars, extracted the logic into `src/pbi_agent/frontmatter.py`, and kept sub-agent parsing on the same shared implementation. Added regressions for folded `description: >` in runtime skill discovery and GitHub skill install. Validation: `uv run ruff check src/pbi_agent/frontmatter.py src/pbi_agent/skills/project_catalog.py src/pbi_agent/agent/skill_discovery.py src/pbi_agent/agent/sub_agent_discovery.py tests/test_skill_discovery.py tests/test_project_skills.py`; `uv run ruff format --check` on the same files; `uv run pytest tests/test_skill_discovery.py tests/test_project_skills.py tests/test_sub_agent_discovery.py -q`.
- Re-ran the direct install flow after the parser update: `uv run pbi-agent skills add JuliusBrussee/caveman --skill compress` now succeeds and installs the upstream folded-YAML `SKILL.md` directly into `.agents/skills/compress`. Validation: successful installs in both `/tmp/pbi-agent-folded-skill-validate` and the workspace; verified `.agents/skills/compress/SKILL.md` still contains `description: >`.
- Installed project skill `.agents/skills/compress` from `JuliusBrussee/caveman` using the global `pbi-agent skills add` CLI after normalizing the upstream `skills/compress/SKILL.md` frontmatter in a temp source to a single-line `description`, because the direct GitHub install still rejected the repo’s folded YAML manifests. Validation: `pbi-agent skills add /tmp/caveman-skill-install --skill compress`; verified `.agents/skills/compress/` contents and `SKILL.md` header.
- Replaced the lingering “collaboration mode” wording in `.agents/commands/plan.md` with “non-interactive mode” so the prompt consistently reflects the no-questions planning workflow. Validation: reread the opening section of `.agents/commands/plan.md` after patching.
- Removed the remaining `update_plan` references from `.agents/commands/plan.md` after the non-interactive rewrite so the prompt no longer mentions unavailable tooling. Validation: `rg -n "update_plan" .agents/commands/plan.md` returned no matches after patching.
- Rewrote `.agents/commands/plan.md` into a non-interactive planning prompt: it now forbids asking clarifying questions or using `request_user_input`, requires grounded assumptions for missing intent, and instructs the final plan to be emitted as plain Markdown instead of a `<proposed_plan>` XML block. Validation: reread `.agents/commands/plan.md` after patching to confirm the non-interactive rules and Markdown-only finalization instructions.
- Fixed `tests/test_tool_registry.py::test_registry_sub_agent_schema_uses_project_agent_enum` by updating the fixture to create `reviewer.md` under `.agents/agents/`, matching the current project sub-agent discovery root used by the dynamic `sub_agent` tool schema. Validation: `uv run pytest tests/test_tool_registry.py -q`.
- Expanded `pbi-agent skills add` to support `pbi-agent skills add [source]` with default-catalog auto-listing, local filesystem sources, authenticated GitHub archive downloads, git fallback for private or unavailable repos, and guaranteed temp cleanup for list/install flows. Validation: `uv run ruff check src/pbi_agent/cli.py src/pbi_agent/skills/project_installer.py tests/test_cli.py tests/test_project_skills.py`; `uv run ruff format --check` on the same files; `uv run pytest tests/test_cli.py tests/test_project_skills.py -q`; `bun run docs:build`.
- Tightened project-skill source discovery so `pbi-agent skills add` excludes source-repo `.agents/skills/*` bundles and only exposes a root `SKILL.md` plus public `skills/*/SKILL.md` candidates. Validation: `uv run ruff check src/pbi_agent/skills/project_installer.py tests/test_project_skills.py`; `uv run ruff format --check src/pbi_agent/skills/project_installer.py tests/test_project_skills.py`; `uv run pytest tests/test_project_skills.py -q`.
- Ran daily memory management. Changed `MEMORY.md` to the required three-section format, compacted historical detail into durable notes, preserved 2026-04-21 task history, and reset `TODO.md` for this session. Validation: structure review of `MEMORY.md` and `TODO.md` against `AGENTS.md` task-memory rules.
- Added `pbi-agent commands list/add` for importing `.agents/commands/*.md` prompt presets from local paths and GitHub repos, extracted shared GitHub/local source materialization to `src/pbi_agent/project_sources.py`, and rewired `skills add` to use the shared resolver. Validation: `uv run ruff check src/pbi_agent/cli.py src/pbi_agent/project_sources.py src/pbi_agent/skills/project_installer.py src/pbi_agent/commands/project_catalog.py src/pbi_agent/commands/project_installer.py tests/test_cli.py tests/test_project_commands.py tests/test_project_skills.py`; `uv run ruff format --check` on the same files; `uv run pytest tests/test_project_commands.py tests/test_cli.py tests/test_project_skills.py -q`.
- Published the initial official command catalog to `https://github.com/pbi-agent/commands` on `main` with `commands/execute.md`, `commands/plan.md`, and `commands/review.md` copied from the workspace commands. Verified the app repo already used `pbi-agent/commands` as the default source, so no import-source code change was needed. Validation: `git push -u origin main` from `~/pbi-agent-catalogue/commands`; `uv run pytest tests/test_project_commands.py tests/test_cli.py -q`.
- Documented `pbi-agent commands` in the VitePress docs by adding a CLI reference section for `commands list/add` and a customization guide section for project command files, official catalog usage, and explicit `.agents/commands` targeting. Validation: `bun run docs:build`.
- Updated docs to name the official default catalog repositories explicitly: skills now reference `https://github.com/pbi-agent/skills`, commands now reference `https://github.com/pbi-agent/commands`, and the README common-command section lists both default remote catalogs. Validation: `bun run docs:build`.
- Refactored project sub-agent discovery to load only `.agents/agents/*.md`, removed per-agent `model` and `reasoning_effort` overrides so child runs use active runtime defaults, and migrated the repo `code-reviewer` agent into `.agents/agents/`. Validation: `uv run ruff check src/pbi_agent/agent/sub_agent_discovery.py src/pbi_agent/agent/session.py src/pbi_agent/cli.py tests/test_sub_agent_discovery.py tests/test_system_prompt.py tests/test_sub_agent_tool.py tests/test_cli.py`; `uv run ruff format --check src/pbi_agent/agent/sub_agent_discovery.py src/pbi_agent/agent/session.py src/pbi_agent/cli.py tests/test_sub_agent_discovery.py tests/test_system_prompt.py tests/test_sub_agent_tool.py tests/test_cli.py`; `uv run pytest tests/test_sub_agent_discovery.py tests/test_system_prompt.py tests/test_sub_agent_tool.py tests/test_cli.py -q`; `bun run docs:build`.
- Added `pbi-agent agents list/add` for importing `.agents/agents/*.md` sub-agent definitions from local paths and GitHub repos, rendered installed agents in the CLI, and documented the new default official catalog `https://github.com/pbi-agent/agents`. Published the initial `code-reviewer` seed agent to the external catalog on `main`. Validation: `uv run ruff check src/pbi_agent/cli.py src/pbi_agent/agent/sub_agent_discovery.py src/pbi_agent/agents/project_catalog.py src/pbi_agent/agents/project_installer.py tests/test_cli.py tests/test_project_agents.py`; `uv run ruff format --check` on the same files; `uv run pytest tests/test_project_agents.py tests/test_cli.py -q -k 'agents or commands or skills or sub_agent_discovery'`; `bun run docs:build`; `git push -u origin main` from `~/pbi-agent-catalogue/agents`.
- Fixed saved-session resume semantics in the web UI by making `SessionPage` reopen saved sessions with `resume_session_id`, and hardened `_resume_session()` so it restores a saved provider checkpoint when transcript replay is unavailable. Added regression coverage in `tests/test_session.py` and `webapp/src/components/session/SessionPage.test.tsx`. Validation: `uv run pytest tests/test_session.py -q -k 'resume_session_restores_previous_response_id_when_history_missing or resumed_session_bootstraps_from_history_without_previous_id or resumed_session_bootstraps_once_then_uses_previous_response_id'`; `uv run ruff check src/pbi_agent/agent/session.py tests/test_session.py`; `uv run ruff format --check src/pbi_agent/agent/session.py tests/test_session.py`; `bun run typecheck`. `bun run test:web -- SessionPage.test.tsx` hung in this environment, and `bun run lint` still reports pre-existing unrelated errors in `webapp/src/components/settings/ModelProfileModal.tsx` and `webapp/src/components/settings/SettingsPage.test.tsx`.
- Hardened project agent installation to reject empty or multi-segment frontmatter `name` values before writing under `.agents/agents/`, preventing path traversal and unusable nested installs; added regression coverage for `../`, nested-path, backslash, absolute-path, and dot-segment names. Validation: `uv run ruff check src/pbi_agent/agents/project_installer.py tests/test_project_agents.py`; `uv run ruff format --check src/pbi_agent/agents/project_installer.py tests/test_project_agents.py`; `uv run pytest tests/test_project_agents.py -q`.
- Added `/home/nasirus/pbi-agent-catalogue/agents/README.md` to mirror the official skills/commands catalog readmes and document the public `agents/` layout plus the current `code-reviewer` entry. Validation: reviewed `/home/nasirus/pbi-agent-catalogue/skills/README.md` and `/home/nasirus/pbi-agent-catalogue/commands/README.md`, then checked the new README content and `git -C /home/nasirus/pbi-agent-catalogue/agents status --short`.
- Updated user-facing GitHub links to the canonical `https://github.com/pbi-agent/pbi-agent` URL in the main README, docs home page, and source-install docs, and added explicit canonical repo URLs to the external `agents`, `commands`, and `skills` catalog READMEs. Validation: searched the edited docs for old/new GitHub URLs and reread the updated README snippets in all four repos.
- Simplified project skill displays so `/skills` markdown and `pbi-agent skills list` show only skill name plus description, hiding the underlying `SKILL.md` file path while keeping internal manifest locations unchanged. Validation: `uv run pytest tests/test_project_skills.py -q`; `uv run pytest tests/test_session.py -q -k 'skills_command_locally or local_command_precedence_over_command_files or format_project_skills_markdown_hides_skill_manifest_paths'`; `uv run ruff check src/pbi_agent/agent/skill_discovery.py src/pbi_agent/skills/project_catalog.py tests/test_project_skills.py tests/test_session.py`; `uv run ruff format --check src/pbi_agent/agent/skill_discovery.py src/pbi_agent/skills/project_catalog.py tests/test_project_skills.py tests/test_session.py`; `uv run pytest tests/test_project_skills.py tests/test_session.py -q -k 'format_project_skills_markdown_hides_skill_manifest_paths or skills_command_locally or local_command_precedence_over_command_files or render_installed_project_skills_lists_table'`.
