# MEMORY.md

## Metadata
- Last compacted: 2026-05-13
- Scope: durable repo memory + active-day task events.
- Format: only `Metadata`, `Long-Term Memory`, and `Detailed Task Events`.

## Long-Term Memory
- Memory: token-efficient. Keep durable decisions/follow-ups only; drop logs. On new day, promote useful prior-day outcomes, then delete old detail.
- Workflow: no commit/push/merge/PR unless asked. Use session-only `TODO.md`; markers `[ ]`, `[>]`, `[X]`, `[!]`, `[-]`. Shell from workspace root; direct Python uses `python3`.
- Validate touched surface: Python Ruff+basedpyright+pytest; frontend Bun tests/lint/typecheck/build; docs build. Note skipped/hung checks.
- Core architecture: CLI `src/pbi_agent/__main__.py` -> `src/pbi_agent/cli.py`; default command `web`; backend FastAPI `src/pbi_agent/web/`; frontend Vite React `webapp/`.
- API changes: keep routes/schemas/session manager aligned with `webapp/src/api.ts`, `webapp/src/types.ts`, and generated API types.
- Web API types: `bun run web:api-types` runs `scripts/generate_api_types.py`; `test_api_types_codegen.py` enforces current output.
- Constraints: provider/tool HTTP via `urllib.request`; internal data under `~/.pbi-agent/`; no migrations/backcompat.
- Tools: built-in `python_exec`, `list_files`, `search_files` removed; use shell (`rg --files`, `find`, `rg -n`) and Python as needed.
- Tool availability: V4A providers (`openai`, `chatgpt`) advertise `apply_patch` and hide duplicate `replace_in_file`/`write_file`; non-V4A providers invert; `read_web_url` only with `web_search`.
- OpenAI/ChatGPT `apply_patch`: Codex-compatible freeform custom tool; exact Lark grammar; raw V4A `custom_tool_call` input; local Lark validation/move support; reject empty `Update File`/move-only hunks; model-facing output is compact plain text (`Success. Updated the following files\nA/M/D path`), not `{ok,result}` JSON.
- Web `apply_patch` cards: raw V4A parsing carries per-file operation/path/diff/count and move context (`move_file old → new`); running/completed multi-op calls render one card per file and completed replacements preserve mixed tool order.
- Project imports: top-level `skills`, `commands`, `agents`; public catalogs under `pbi-agent/{skills,commands,agents}`; local install to `.agents/...`.
- Sub-agents: `.agents/agents/*.md`; frontmatter only `name`/`description`; child model from active profile/CLI + `sub_agent_model`.
- Local commands: `plan` no questions; `review` Markdown and runs autonomously without reviewer sub-agent; `fix-review` stops on `No findings.`; `ship-task` branch/merge helpers; `release` needs explicit publish wording and no history rewrite after PR unless approved.
- Project command `/create-task` creates Kanban cards through `pbi-agent kanban create` without implementation.
- Release docs: use `release-writing`; changelog index `docs/changelog/index.md`, files `docs/changelog/v<version>.md`, VitePress sidebar links releases; release workflow needs validation triage, publish verification, continuation gate safety.
- Branding: public copy = local coding agent; preserve `pbi-agent`, `pbi_agent`, `PBI_AGENT_*`, `~/.pbi-agent`; logo `src/pbi_agent/web/static/logo.jpg`.
- Providers: OpenAI Responses use `instructions` + `previous_response_id`; ChatGPT subscription prepends system prompt; Codex transport is WebSocket-only (`chatgpt_codex_backend.py`), no unsupported compression.
- Web/session: saved sessions use session-scoped APIs; no user-facing `/api/live-sessions/*` or `/sessions/live/:id`. Blank/saved sessions can start/continue without active live id; completed Kanban-bound runs detach live ids without ending saved conversation.
- Web session manager: `src/pbi_agent/web/session_manager.py` is thin facade; implementation mixins live under `src/pbi_agent/web/session/`. Patch worker/auth globals in `pbi_agent.web.session.workers` and `pbi_agent.web.session.provider_auth`.
- Web events: app/session streams use SSE over `GET /api/events/{stream}` and `/api/events/sessions/{session_id}` with `server.connected`/heartbeat and `since`/`Last-Event-ID`; WebSocket event routes/helpers removed. Frontend keeps defensive `parseSseEvent()` validation.
- Web message identity: saved history/live replay use canonical persisted ids `msg-<messages.id>` plus stable part ids; optimistic/display-local live items rekey via `message_rekeyed`.
- Web status: session/live API lifecycle statuses differ from persisted run-record statuses; keep contracts distinct.
- Web command input: direct saved-session shell commands disable `WebDisplay` input with `begin_direct_command()`/`finish_direct_command()`; `attachLiveSession(..., preserveEventCursor)` preserves input/processing when SSE cursor is equal/newer than submit response cursor.
- Web live snapshots: `LiveSessionSnapshotModel` preserves typed `processing` and pending user-question payloads; frontend boundary keeps `pending_user_questions` required nullable.
- Web frontend API boundary: add JSON API calls through generated `ApiOperationResponses`/`ApiJsonRequestBodies` via `apiRequest()` and `jsonBody()`.
- Web durable runs/replay: `run_sessions` stores web projection fields/status; `snapshot_json` caches timeline from persisted `web_event`; unfinished web runs become `stale`; `RunTracer` attaches canonical run ids; saved-session event streams replay original `seq`/`created_at`.
- Saved-session timelines: API `history_items` are canonical chronological messages; frontend overlays only non-duplicate live/work snapshot items and avoids stale live streams for ended/static projections.
- Session compaction: clear completed `previous_id`; resume restores persisted user/assistant history; include unanswered trailing user turns and same-turn tool exchanges.
- Saved-session fork: `POST /api/sessions/{session_id}/fork` duplicates messages/uploads through target message ids, resets usage/previous_id, stores fork metadata/title `Fork-{original-title}`, cleans copied uploads on failure.
- Web UI: prefer shadcn tokens/components; overlays use shared readable spacing; large modals need safe gutters + inner scroll; clamp edge-adjacent tooltips on both axes.
- Web timeline/composer UX: Composer highlights `@file` and `$skill`; `$skill` completions use `/api/skills/search`; user/assistant timelines support copy/fork; fenced code uses Shiki `CodeBlock`; Working summaries categorize read/search/shell/edit/sub-agent/question/other tools.
- Timeline: work runs coalesce thinking/tool activity between chat messages; stable active placeholder keys; historical work-run expansion must not reset auto-follow; expanded Working activity scroll follows latest and centers opened tool cards; one shadcn Accordion closes other tool cards.
- Web layout/session chrome: global `AppSessionsContextPanel` on Sessions/Kanban/Dashboard; sidebar head and session topbar share `--topbar-height: 48px`; collapsed sidebar 48px; New Session closes sessions sidebar.
- Frontend shadcn normalization: `FormDialog`/`ConfirmDialog` canonical for migrated modals/confirms; Badge variants include `success`/`warning`/`info`/`running`/`completed`/`failed`; status dots render except `asChild`; `StatusPill` preserves running/completed groups.
- Composer history: SessionPage passes main-session non-empty user messages; Composer ArrowUp/ArrowDown browses history with draft restore and resets browsing when history changes.
- Test hygiene: pytest isolates web upload root to per-test `tmp_path / "web_uploads"`.
- Maintenance: startup runs `run_startup_maintenance()` once per UTC day via SQLite `maintenance_state`; retention config `maintenance.retention_days` (default 30, min 1) exposed via CLI/web settings; purge removes old sessions/messages/run_sessions/observability_events/web_manager_leases and old unreferenced uploads while preserving referenced uploads; PyPI check uses `urllib.request`.
- CLI startup maintenance warning: PyPI check emits Rich stderr update warning only for newer versions; `MaintenanceResult.update_notice` stays plain string.
- Sandbox: `pbi-agent sandbox [web|run]` builds bundled-package Dockerfile from `src/pbi_agent/sandbox/Dockerfile.sandbox`, installs matching PyPI version, ignores workspace Dockerfiles, shares sessions/Kanban/runs with non-sandbox, and uses per-workspace writable `/home/pbi` Docker volume for user-local installs/caches.
- Sandbox validation: focused sandbox pytest, Ruff, basedpyright, docs build, `uv build`, and Docker build/smoke when available.
- Project install UI: Settings Project Skills/Commands/Agents support official/custom install, force-replace conflict flow, flat ProviderModal-style add dialogs, and API types/static rebuilds after contract changes.
- CLI web startup: server banner is sole `Serving on...`; Linux browser launch strips only legacy `atk-bridge` from `GTK_MODULES`.
- Shell tool timeout: default `timeout_ms` 30000, max 300000; reject non-positive/non-integer/bool before subprocess. ChatGPT shell schema is compact command-only; runtime/base schema still accepts optional overrides.
- Sub-agent web UI: parent timeline keeps child work in stable sub-agent cards; child routes own wait/input/processing/usage; aggregate child processing/interruptability controls parent Working without child transcript churn.
- Startup stale cleanup: orphan active turn/sub-agent run records in stale web-session intervals become stale; stale projections clear processing/wait/pending and downgrade running tool/sub-agent snapshots.

## Detailed Task Events
## 2026-05-13
- Smoke-tested a single `apply_patch` call with multiple operations: edited `TODO.md` and created `apply_patch_multi_task_smoke.txt`; verified both files with `read_file`.
- Smoke-tested another single `apply_patch` multi-task call: updated `TODO.md` and created `apply_patch_single_call_smoke_2026-05-13.txt`; verified both files with `read_file`.
- Fixed completed web `apply_patch` multi-operation cards: completed metadata now carries `operation_count`/`affected_paths` from structured results or raw V4A input, and frontend titles show secondary files compactly (for example `TODO.md + apply_patch_tool_smoke.txt`); focused backend/API-codegen/frontend tests, Ruff, basedpyright, Bun lint/typecheck, and `git diff --check` passed.
- Checked saved session `449cd48da1c84e6cb74f310859b2614d` and `pbi-agent.html`: historical run used Codex freeform `apply_patch` successfully with custom tool outputs, but persisted running web events still show `<missing operation_type> <missing path>` because the snapshot predates the running-summary fix; completed cards render as `Edit TODO.md` and hide add/update secondary files.
- Verified Codex rejects empty `Update File` hunks/move-only updates, added matching pbi-agent semantic checks plus focused tests; validation passed with Ruff, basedpyright, and focused apply_patch pytest.
- Ran final full gate for apply_patch freeform changes; fixed stale sandbox Dockerfile package expectation, then Ruff, format check, basedpyright, full pytest, Bun lint/typecheck, and `git diff --check` passed.
- Polished web `apply_patch` running display: raw V4A input now yields compact path/operation/count metadata and frontend renders a running file-edit card; validation passed with focused web-display pytest, API type codegen test, frontend ToolResult test, Bun lint/typecheck, Ruff, basedpyright, and `git diff --check`.
- Updated `.agents/commands/review.md` so `/review` explicitly runs autonomously in the current session and does not call/delegate to `.agents/agents/reviewer.md` or any reviewer sub-agent; validation was a direct content check.
- Fixed review finding for web `apply_patch` running summaries: raw V4A `*** Move to:` now displays move context as `move_file old → new` with diff/count metadata; focused web-display pytest, Ruff, format check, and basedpyright passed.
- Split web `apply_patch` multi-operation display into one card per file operation for running and completed events; raw V4A parsing now carries per-op path/operation/diff with synthetic call-id suffixes. Focused web-display pytest, frontend ToolResult test, Ruff, basedpyright, Bun lint/typecheck, and `git diff --check` passed.
- Fixed review finding for split web apply_patch cards: replacing running cards now reinserts completed cards at the first removed index, preserving mixed tool order; added focused mixed apply_patch+shell test and passed focused pytest, Ruff, format check, basedpyright, and `git diff --check`.
- Fixed review finding for single-file `apply_patch` moves: completion metadata now uses the parsed V4A `Move to` operation/path, so completed cards stay `move_file old → new`; focused web-display pytest, Ruff, format check, basedpyright, and `git diff --check` passed.
- Made run history dropdown content-sized/responsive by letting the menu/list use `max-content` with viewport caps; validation passed with `bun run typecheck` and `bun run lint`.
- Compressed `MEMORY.md` long-term durable notes and purged 2026-05-12 detailed events after deciding no extra durable follow-up was needed; validation was structure inspection only.
- Updated Run Detail status badge to reuse shared `StatusPill` styling instead of local variant mapping; validation passed with focused RunDetailModal Vitest, `bun run typecheck`, and `bun run lint`.
- Fixed Run Detail event `ok`/HTTP-status chips to use shared completed/failed Badge variants with status dots; focused RunDetailModal test, Bun typecheck/lint, and `git diff --check` passed.
- Corrected Run Detail badge styling by using compact `StatusPill` for header and event ok/status-code chips, allowing custom status-pill text, and rebuilding bundled static app; focused Vitest, Bun typecheck/lint/build, and `git diff --check` passed.
- Fixed session timeline follow-scroll for newly sent user messages with image attachments by clearing any stale "New messages below" badge before image-aware auto-scroll; focused SessionTimeline Vitest, Bun typecheck/lint/build, and `git diff --check` passed.
- Hardened image user-message autoscroll: latest user updates/rekeys now force user-scroll follow, bottom-align after image layout, cancel stale scroll requests, and suppress stale new-message badges; focused SessionTimeline Vitest, Bun typecheck/lint/build, and `git diff --check` passed.
- Justified Run Detail header stats across the full ribbon with responsive grid wrapping; focused RunDetailModal Vitest, Bun typecheck/lint/build passed, while full `bun run test:web` still has 4 known unrelated inline-rename failures.
- Added a subtle vertical separator between Run Detail header Duration and Cost metrics; focused RunDetailModal Vitest, Bun lint/typecheck/build, and `git diff --check` passed.
- Audited `~/codex` model-facing tool definitions and wrote `LIST_TOOLS.md` in this workspace; validation was source inspection plus file creation only.
- Removed the stray terminal/command icon from the Run Detail token Tool-use stat; focused RunDetailModal Vitest, Bun lint/typecheck/build, and git diff check passed.
- Restored the Run Detail Tool-use TerminalIcon and fixed header stats clipping by splitting core/token groups and letting tokens wrap/justify inside available space; focused RunDetailModal Vitest, Bun lint, web build, and git diff check passed; final typecheck rerun was blocked by unrelated `SessionPage.tsx` displayedProcessing ordering changes.
- Added a processing-only composer animation (sheen/rail + waveform) for locked session input; focused Composer Vitest, Bun typecheck/lint/build, and git diff check passed; full `bun run test:web` still fails on 4 known unrelated SessionSidebar inline-rename tests.
- Made session timeline Working blocks single-open at the outer block level; expanding one closes any other open Working block while inner tool accordion behavior stays unchanged. Focused SessionTimeline Vitest, Bun lint/typecheck/build, and git diff check passed; full `bun run test:web` still fails 4 known unrelated SessionSidebar inline-rename tests.
- Removed the visible composer processing dots/waveform spinner, keeping only the subtle processing rail/sheen plus screen-reader status; focused Composer Vitest, Bun typecheck/lint/build, and git diff check passed; full `bun run test:web` still fails known unrelated SessionSidebar inline-rename tests.
- Investigated Run Detail Tool-use metric: it displays `TokenUsage.tool_use_tokens`, currently populated only by Google provider from `usage.total_tool_use_tokens`; it is provider-reported token usage, not tool-call count, so OpenAI/ChatGPT/xAI/Anthropic/generic runs show 0 unless provider reports that field.
- Removed tool-use token metric end-to-end: TokenUsage fields, Google parsing, run-session schema/API/event payloads, web display serialization, frontend Run Detail/Run History/types/tests, and regenerated API/static assets. Focused Python/web tests plus Ruff, format check, basedpyright, Bun lint/typecheck/build passed.
- Made SessionSidebar conversation cards real Router links for browser open-in-new-tab/context-menu actions while preserving the edit/delete dropdown; added link/menu tests, fixed the edit menu label, rebuilt static app, and passed Bun test:web/lint/typecheck/build plus git diff check.
- Fixed stale local DB crash after removing `tool_use_tokens`: `RunSessionRecord` hydration now ignores extra DB columns, with regression coverage for leftover `tool_use_tokens`; session-store/web focused pytest, Ruff, format check, basedpyright, and `git diff --check` passed.
- Reordered Settings navigation to Models (Providers, Model Profiles), Project (Commands, Skills, Agents), Desktop (Appearance, Notifications), Data (Maintenance), with Providers as the default tab; updated SettingsPage tests and rebuilt static app. Validation passed: focused SettingsPage Vitest, full `bun run test:web`, `bun run lint`, `bun run typecheck`, and `bun run web:build`.
- Confirmed Settings default tab is Providers (`activeTab` initializes to `"providers"`) and added a regression test asserting the Providers panel/nav active state opens by default; focused SettingsPage Vitest passed.
- Fixed expanded Working block left guide line by moving the dashed border from the whole work-run body to the `working-items` list so it stops above the `+more` footer; focused SessionTimeline Vitest, Bun lint/typecheck/build, and `git diff --check` passed.
- Fixed reopened saved sessions dropping unpersisted assistant model output: historical timeline messages with no saved-history signature now render instead of being treated as duplicates; added SessionPage regression and passed full Bun web tests, lint, typecheck, web build, and `git diff --check`.
- Fixed active last Working block first-open page autoscroll by deferring user-open alignment until Collapsible content is mounted; added SessionTimeline first-open regression and rebuilt scoped static assets. Validation passed: focused/full Bun web tests, lint, typecheck, web build, and `git diff --check`.
- Re-fixed active last Working block first-open autoscroll: user-open follow now applies to active blocks even between tool phases, retries after first-open layout settles, keeps a composer-edge gutter, and rebuilds static app. Validation passed: focused/full SessionTimeline/web Vitest, Bun lint/typecheck/build, and `git diff --check`.
- Made Settings sidebar sections scroll inside a fixed header sidebar, preserving horizontal section scrolling on narrow layouts; focused SettingsPage Vitest, Bun lint/typecheck/web build, and `git diff --check` passed.
