# MEMORY.md

## Metadata
- Last compacted: 2026-04-26
- Scope: durable repo memory + append-only active-day task events.
- Format: only `Metadata`, `Long-Term Memory`, `Detailed Task Events`; compact prior-day detail into durable notes before new work.

## Long-Term Memory
- Workflow: no commit/push/merge/PR unless asked. Use session-only `TODO.md`; reset before work; markers `[ ]`, `[>]`, `[X]`, `[!]`, `[-]`.
- Validate touched surface: Python = Ruff check/format + focused pytest; frontend = `bun run test:web`, `bun run lint`, `bun run typecheck`, `bun run web:build`; docs = `bun run docs:build`; broad = repo Ruff/Bun/pytest. Env quirk: Bun/Node/npm or lint/test may hang; note gaps.
- Architecture: CLI entry `src/pbi_agent/__main__.py` -> `cli.py`; system prompt says `pbi-agent`, check `pbi-agent -h`. Commands: `web` default, `run`, `sessions`, `config`, `skills`, `commands`, `agents`. Web FastAPI under `src/pbi_agent/web/`; frontend Vite React TS in `webapp/`; build output `src/pbi_agent/web/static/app`.
- Web API change: keep routes/schemas/session manager + `webapp/src/api.ts` + `types.ts` aligned. Tests use `DisplaySpy`; web tests beside `webapp/src/**/*.test.ts(x)`, behavior > snapshots.
- Provider/runtime: HTTP via `urllib.request`; providers under `src/pbi_agent/providers/`; internal data in `~/.pbi-agent/`; no migrations/backcompat. `chatgpt` + `github_copilot` first-class subscription providers, separate from API-key `openai`.
- Model discovery live at runtime: ChatGPT Codex, Gemini, OpenAI-compatible `/models`, GitHub Copilot `GET /models`; Copilot live default `gpt-5.4`.
- Tools: built-in `python_exec` was removed; use shell for Python as needed. `shell` truncates stdout/stderr at 12k chars each with middle omission; shared `bound_output` default remains 1k.
- Project imports: `skills`, `commands`, `agents` top-level CLI groups. Shared GitHub/local materializer discovers public `skills/*/SKILL.md`, `commands/*.md`, `agents/*.md`; ignores source repo `.agents/*` internals unless explicit; installs to local `.agents/...`. Official catalogs: `pbi-agent/{skills,commands,agents}`.
- Project sub-agents only `.agents/agents/*.md`; frontmatter only `name`/`description`; child model from active profile/CLI + `sub_agent_model`. Shared frontmatter parser supports YAML block scalars for `description`, rejects block-scalar `name`; install rejects unsafe names.
- Local commands: `plan.md` noninteractive, no clarifying questions or `request_user_input`, plain Markdown; `review.md` plain Markdown not JSON; `execute.md` compact prose; `fix-review.md` fixes prior findings, stops on `No findings.`; `ship-task.md` action-oriented, prefixed kebab-case branches, deletes merged local/remote branches; `retrospective.md` compressed. Local `compress` skill prompt-only/no network.
- Branding: public copy = general local coding agent; preserve `pbi-agent`, `pbi_agent`, `PBI_AGENT_*`, `~/.pbi-agent`. Canonical logo `src/pbi_agent/web/static/logo.jpg` + favicon/logo fallbacks; banner dark `BI`, `work smart.`, centered `v{__version__}`.
- Web/session: saved sessions reopen via `resume_session_id`; `_resume_session()` restores provider checkpoints when no replay; `WebSessionManager` uses SQLite lease heartbeat with `BEGIN IMMEDIATE`/`busy_timeout`; managed startup normalizes running tasks.
- Provider ordering: standard OpenAI Responses use `instructions` + `previous_response_id`; ChatGPT subscription prepends system prompt to replay/current input. `x-codex-turn-state` = sticky routing state, not incremental input; no tool-result-only deltas from that header alone.
- Provider parsing/retry: direct OpenAI Responses and Anthropic Messages already scan all output/content blocks for text + tool calls; OpenAI-compatible/generic and GitHub Copilot chat-completions must merge text/tool calls/reasoning across all `choices`; OpenAI semantic `response.failed` code `server_is_overloaded` is retryable.
- Recent fixes: failed provider turns delete unanswered user messages; sub-agent parent context excludes current user turn; project sub-agent discovery temp-workspace covered. `read_file.start_line` unbounded positive; `max_lines` capped. Kanban first runnable stage sends slash command + task details; later stages command-only; slash detection splits whitespace. Done sort newest-first; packaged SPA may need rebuild after frontend changes.
- Settings/model fallback: profile blank `sub_agent_model` resolves to profile main model, while explicit CLI/env sub-agent overrides win; Settings modal should describe blank sub-agent as profile-main fallback.
- Web UI: `!command` composer input calls live-session shell endpoint; persisted shell output to `SessionStore`; shell mode warning UI + refocus after completion. Multiline user timeline uses `pre-wrap`; websocket-only sessions bind `eventLiveSessionId` before `input_state`.
- Web UI/design durability: shadcn/Tailwind v4 refactor uses ThemeProvider/theme switcher + `@/*` alias; global text-crop fixes rely on readable min-heights/line-heights, non-clipping badges, `object-fit: contain` attachment previews, centered header nav, and dense chrome buttons using compact `--text-xs`/normal leading per `DESIGN.md`.
- Apply-patch UI: live-session tool events carry V4A diff metadata plus `diff_line_numbers`; `GitDiffResult` renders apply_patch groups with real gutters, intraline highlights for 1:1/equal-count multi-row replacements, and conservative unpaired unequal-count blocks.
- Docs/artifacts: repo URL `https://github.com/pbi-agent/pbi-agent`; external catalog READMEs point there; `WEBAPP_CURRENT_STATE_SPEC.md` = web UX/UI redesign handoff.
- Git remote: default `origin` fetch/push URL should be `https://github.com/pbi-agent/pbi-agent.git`; legacy `nasirus/pbi-agent` is not canonical.
- Env repair: if VS Code pytest discovery breaks after partial `.venv`, `uv sync --reinstall` fixed missing `pygments.formatters`/`packaging.version`; verify `.venv/bin/python -m pytest --collect-only -q ...` + `uv pip check`.
- Active follow-up: source-repo internal `.agents/skills/*` bundles never surface as downloadable/installable candidates.
- Active follow-up: web overlay primitives (`TooltipContent`, `DropdownMenuContent`/items, `SelectContent`/items, submenus) need global readable defaults: collision padding, side offsets, min-height/line-height, and p-2-ish panel padding; document in `DESIGN.md` so new shadcn components inherit safe spacing.

## Detailed Task Events
- 2026-04-27: Fixed `tests/test_session.py` local `_DisplaySpy` after diagnostic `uv run pytest tests/test_session.py -x -vv` showed missing `assistant_start`/`assistant_stop`; added counters/no-op-compatible methods to match `DisplayProtocol`. Validation: `uv run pytest tests/test_session.py -x -vv`; `uv run ruff check tests/test_session.py`; `uv run ruff format --check tests/test_session.py`.

## 2026-04-26
- Fixed review finding for timeline auto-scroll: internal new-item target scroll now uses `behavior: "instant"` so programmatic-scroll suppression cannot expire during a long smooth scroll and mark first `GitDiffResult` as manual scroll. Validation: focused `SessionTimeline.test.tsx`; `bun run typecheck`; `bun run lint`; `git diff --check`.
- Fixed timeline auto-scroll after first apply_patch diff: `useAutoScroll` now suppresses scroll events triggered by programmatic timeline scrolls, and `SessionTimeline` marks target/bottom auto-scrolls before moving so first expanded `GitDiffResult` no longer flips `userScrolledRef` to manual-scrolled. Validation: focused `SessionTimeline.test.tsx`; `bun run typecheck`; `bun run lint`; `git diff --check`.
- Fixed streaming tool-display review finding: `build_tool_result_callback()` now guards `display_tool_result()` with a per-callback lock so parallel worker completions cannot concurrently mutate/publish display tool-group state while preserving completion-order streaming. Validation: `uv run pytest tests/test_tool_runtime.py`; `uv run ruff check src/pbi_agent/agent/tool_display.py tests/test_tool_runtime.py`; `uv run ruff format --check src/pbi_agent/agent/tool_display.py tests/test_tool_runtime.py`.
- Streamed per-tool completion for parallel batches: `execute_tool_calls(..., on_result=...)` invokes callbacks serially per call and in `as_completed` order while returning provider results in input order; providers now stream display via `build_tool_result_callback()` and finalize with `tool_execution_stop()` after the batch. Web display publishes tool-group updates after each `function_result`/`patch_result`, preserving item IDs/upserts. Validation: focused runtime/provider/web pytest (19 passed), `uv run ruff check .`, touched-file `uv run ruff format --check ...`. Repo-wide `ruff format --check .` still has unrelated pre-existing `src/pbi_agent/agents/__init__.py` formatting gap.
- Apply-patch diff gutters now show real file line numbers: `apply_diff.diff_line_numbers()` computes old/new gutters from matched V4A context, `ToolContext.display_metadata`/`ToolResult.display_metadata` carry display-only metadata outside provider output, web display publishes `diff_line_numbers`, and `GitDiffResult` uses them with fallback numbering. Validation: focused Python pytest; focused `SessionTimeline.test.tsx`; `bun run typecheck`; `bun run lint`; `bun run web:build`; Ruff touched files; `git diff --check`.
## 2026-04-26
- Fixed review finding: provider tool execution now stops the display tool-execution phase in exception paths so web processing state cannot remain stuck in local tool execution. Validation: `uv run ruff check .`; `bun run typecheck`; focused provider pytest still has only pre-existing google/xai default reasoning assertion failures (`medium` vs `high`).
- Live processing indicators: added display protocol hooks for assistant turn + local tool execution; web emits/snapshots `processing_state`, renders running tool groups before completion, and CLI shows a local-tool spinner. Validation: `uv run ruff check .`; `bun run typecheck`; `bun run test:web -- SessionTimeline`; `git diff --check`; focused provider/display pytest passed except pre-existing google/xai default reasoning assertions (`medium` vs expected `high`); repo format check still reports unrelated `src/pbi_agent/agents/__init__.py`.
- Fixed review finding in apply_patch diff UI: ambiguous multi-line delete/insert blocks no longer get paired for intraline highlighting; only one removed + one added line is treated as a replacement. Added regression coverage. Validation: `bun run test:web -- --run webapp/src/components/session/SessionTimeline.test.tsx`; `bun run typecheck`; `bun run lint`; `bun run web:build`.
- Apply-patch diff UI v2 implemented: `GitDiffResult` now groups V4A diff lines into semantic context/change/meta blocks, pairs removed/added replacement lines, highlights only intraline changed spans, collapses large unchanged context, uses focused-change footer copy, and labels failed cards as `Patch failed`; packaged SPA rebuilt. Validation: `bun run typecheck`; `bun run test:web -- --run webapp/src/components/session/SessionTimeline.test.tsx`; `bun run lint`; `bun run web:build`; `git diff --check`.
- Custom-command image attachments: allowed slash/custom commands in web composer to include pending image uploads (shell `!` still blocks images), uploads images before direct slash submission, and updated SessionPage test expectation. Validation: `bun run test:web -- SessionPage.test.tsx`; `bun run typecheck`.
- Dropdown audit/enforcement: verified all `DropdownMenuContent/Item` usages (theme, composer, run history, profile selector, session card); changed `overlays.css` selectors from zero-specificity `:where()` to direct `data-slot` selectors so shared dropdown spacing wins; removed session/composer item gap/padding/radius/font-size overrides that fought shared design. Validation: `bun run typecheck`; `bun run lint`; `bun run web:build`; `git diff --check`.
- Memory compaction repair: restored compacted 2026-04-25 durable outcomes into `Long-Term Memory` (provider parsing/retry, sub-agent model fallback, web UI/design, apply_patch UI, git remote) and clarified `AGENTS.md` compaction instructions to require promoting a compact resume before deleting dated sections. Validation: `git diff --check -- MEMORY.md AGENTS.md TODO.md`.
- Production dropdown consistency fix: removed one-off theme menu CSS/classes, added late-loaded `webapp/src/styles/overlays.css` using shadcn `data-slot` selectors for all dropdown content/items/labels/separators, imported after existing component CSS, and removed profile option min-height/padding override that fought shared spacing. Validation: `bun run typecheck`; `bun run lint`; `bun run web:build`; `git diff --check`.
- Theme dropdown visible spacing fix: added `theme-menu`/`theme-menu__item` classes to top-right theme menu with explicit CSS padding/min-height/gap after global dropdown defaults did not visibly change enough; rebuilt packaged SPA. Validation: `bun run typecheck`; `bun run lint`; `bun run web:build`; `git diff --check`.
- Dropdown menu margin follow-up: increased shared dropdown panel spacing (`sideOffset=10`, `collisionPadding=16`, `p-3`, `rounded-xl`, `min-w-40`) and row spacing (`min-h-9`, `px-3 py-2`, `gap-2`, `rounded-lg`) plus submenus/design doc; rebuilt packaged SPA. Validation: `bun run typecheck`; `bun run lint`; `bun run web:build`; `git diff --check`.
- Theme button consistency: removed custom Radix tooltip wrapper from top-right theme switch; theme trigger now uses `Button type="button" variant="ghost" size="icon-sm" title="Change theme" aria-label="Change theme"` like other icon buttons; reverted unwanted pill tooltip style while retaining safe tooltip offset/collision; updated `DESIGN.md`. Validation: `bun run typecheck`; `bun run lint`; `bun run web:build`; `git diff --check`.
- Tooltip pill follow-up: changed shared `TooltipContent` to match workspace badge style (`bg-background`, `border-border`, pill radius, `min-h-10`, `px-3 py-2`, `sideOffset=10`) and updated `DESIGN.md`; rebuilt packaged SPA. Validation: `bun run typecheck`; `bun run lint`; `bun run web:build`; `git diff --check`.
- Global web overlay spacing fix: updated shared shadcn/Radix tooltip, dropdown menu, and select primitives with `sideOffset=8`, `collisionPadding=12`, roomier panel padding, item min-heights/leading; documented Floating Overlays in `DESIGN.md`; rebuilt packaged SPA. Validation: `bun run typecheck`; `bun run lint`; `bun run web:build`; `git diff --check`.
- Diagnosed global web overlay clipping plan: inspected theme switch, shadcn tooltip/dropdown/select primitives, CSS text crop fixes, Radix docs, and DESIGN.md; root cause is primitive defaults still too tight (`TooltipContent sideOffset=0`/no collision padding; menu/select `p-1`, `py-1`, no min-height/leading), not just page-specific CSS. Validation: `bunx --bun shadcn@latest docs tooltip dropdown-menu`; no implementation yet.
- Planned apply_patch diff UI v2: current V4A renderer shows all removed then all added lines, making small code edits hard to spot. Next direction is semantic change groups with collapsed context, side-by-side/paired old-new rows, and intraline token highlights for exact edits; no code changes yet.
- Localised failed apply_patch diff-card description bug: `webapp/src/components/session/GitDiffResult.tsx` always maps `metadata.operation` through `OPERATION_LABELS`, so failed `update_file` still shows `Updated` below filename while badge uses `metadata.success === false`. Proposed fix only; add failed-state label helper/tests.
- Fixed failed apply_patch card description: `GitDiffResult` now maps failed `create_file`/`update_file`/`delete_file` to `Create failed`/`Update failed`/`Delete failed` instead of success labels or generic `Patch failed`; regression test updated. Validation: `bun run test:web -- --run webapp/src/components/session/SessionTimeline.test.tsx`; `bun run typecheck`.
- Kanban task card visual tweak: moved task status badge into a chrome row beside the grip, with title/prompt below; rebuilt packaged SPA. Validation: `bun run typecheck`; `bun run web:build`; `bun run lint` still fails on pre-existing redundant union constituents in `webapp/src/types.ts`.
- Fixed web lint cleanly: added explicit frontend status unions for processing phases and tool call/group statuses, with store parsers that accept only known values. Validation: `bun run lint`; `bun run typecheck`.
- Slash command menu consistency: composer `/` suggestions now render command descriptions inline as `command (description)` like `@` file suggestions, with regression coverage and packaged SPA rebuilt. Validation: `bun run test:web -- --run webapp/src/components/session/Composer.test.tsx`; `bun run typecheck`; `bun run lint`; `bun run web:build`; `git diff --check`.
- Fixed review finding: slash completion inline description CSS now uses existing `--weight-normal` token instead of undefined `--font-weight-normal`; rebuilt packaged SPA. Validation: `bun run test:web -- --run webapp/src/components/session/Composer.test.tsx`; `bun run typecheck`; `bun run lint`; `bun run web:build`; `git diff --check`.
- Run detail modal sizing/style fix: enlarged `RunDetailModal` glass surface, removed duplicate close control, made dialog header/content scroll correctly, added DESIGN.md-aligned summary/event surfaces, and made event rows wrap/scroll with safe min-width/line-height so metadata no longer crops. Validation: `bun run typecheck`; `bun run lint`; `bun run web:build`.
- Run detail KPI redesign: `RunSummary` is a responsive KPI grid (2 cols on narrow viewports, auto-fit cards on wider modal), with highlighted status/duration/cost cards, larger 1280px modal viewport usage, and packaged SPA rebuild. Validation: `bun run typecheck`; `bun run lint`; `bun run web:build`.
- Run detail 4-column KPI follow-up: widened modal to `min(1440px, calc(100vw - 16px))`, removed inherited max-width, made KPI grid fixed 4 columns above 720px with tighter medium spacing; mobile remains 2 columns. Validation: `bun run typecheck`; `bun run lint`; `bun run web:build`.
- Apply-patch unified diff/leading-blank fallback: kept one-file V4A tool unchanged externally, added create/update wrapper that first tries V4A, retries V4A once after stripping accidental leading blank lines, then converts standard single-file unified diff hunks to V4A; docs/schema mention unified fallback and leading-blank guidance, not full multi-file envelope. Validation: `uv run pytest tests/test_apply_patch_tool.py`; `uv run ruff check src/pbi_agent/tools/apply_patch.py tests/test_apply_patch_tool.py`; `uv run ruff format --check src/pbi_agent/tools/apply_patch.py tests/test_apply_patch_tool.py`; `git diff --check`.
- Apply-patch create behavior: `create_file` now replaces an existing file instead of failing, returning message `file exists and was replaced: '<path>'`; directories still fail. Validation: `uv run pytest tests/test_apply_patch_tool.py`; `uv run ruff check src/pbi_agent/tools/apply_patch.py tests/test_apply_patch_tool.py`; `uv run ruff format --check src/pbi_agent/tools/apply_patch.py tests/test_apply_patch_tool.py`.
- Apply-patch delete UI: successful `delete_file` metadata now renders a compact vertically-centered card with only the red strikethrough filename and Done badge; no diff viewport, operation label, stats, footer, or call id. Validation: focused `SessionTimeline.test.tsx`; `bun run typecheck`; `bun run lint`; `bun run web:build`; `git diff --check`.
- Apply-patch delete UI alignment fix: compact delete card header inherits card min-height and centers title row/actions/status on same horizontal axis so icon, red strikethrough filename, and Done badge align. Validation: focused `SessionTimeline.test.tsx`; `bun run typecheck`; `bun run lint`; `bun run web:build`; `git diff --check`.
- Apply-patch delete UI refinement: deleted filename/title copy gets 32px min-height and flex centering so text baseline visually centers beside the 32px file icon. Validation: focused `SessionTimeline.test.tsx`; `bun run typecheck`; `bun run lint`; `bun run web:build`; `git diff --check`.
- Apply-patch delete UI investigation: attempted several header-center fixes for deleted-file cards, but issue persisted/worsened; current useful signal is that the icon+filename block still needs direct vertical centering scrutiny in `webapp/src/components/session/GitDiffResult.tsx` and related card/header styles. Validation: visual inspection only so far.
- Fixed apply_patch delete-card vertical alignment by replacing the delete-only shadcn `CardHeader`/`CardAction` grid path with a simple `CardContent` flex body (`git-diff-result__delete-body`) so icon+filename and Done badge share one centered flex row; rebuilt packaged SPA. Validation: focused `SessionTimeline.test.tsx`; `bun run typecheck`; `bun run lint`; `bun run web:build`; `git diff --check`.
- Apply-patch diff highlight fix: `GitDiffResult` now pairs equal-count multi-row replacement blocks old/new by position and applies existing intraline token highlights to each pair, while unequal-count blocks remain unpaired; added focused regression and rebuilt SPA. Validation: focused `SessionTimeline.test.tsx`; `bun run typecheck`; `bun run lint`; `bun run web:build`.
- V4A apply_diff diagnostic: invalid-context errors now include a hint when parsed context starts with literal diff-prefix chars (`-`, `+`, or leading space), explaining `-- item`, `+- item`, and ` - item` escaping for Markdown/list lines. Validation: `uv run pytest tests/test_apply_diff.py`; `uv run ruff check src/pbi_agent/tools/apply_diff.py tests/test_apply_diff.py`; `uv run ruff format --check src/pbi_agent/tools/apply_diff.py tests/test_apply_diff.py`.
- Kanban task card actions centered: `.task-card__actions` now centers footer action buttons horizontally and vertically while preserving hover reveal/wrap; rebuilt packaged SPA. Validation: `bun run typecheck`; `bun run lint`; `bun run web:build`.
- Kanban task action hover: `.task-card__actions` buttons/links now use header-nav-like fast color/background transition and muted hover/focus background. Validation: `bun run typecheck`; `bun run lint`; `bun run web:build`.
- Kanban task action hover refinement: footer actions now use explicit `.task-card__action-button` class matching header nav spacing/colors/icon opacity; Delete uses same ghost nav styling, not destructive red. Validation: `bun run typecheck`; `bun run lint`; `bun run web:build`.
- Kanban Done-stage action row fit: compacted `.task-card__action-button` spacing/font/icon size and disabled wrapping so Edit/Session/Delete fit on one centered row. Validation: `bun run typecheck`; `bun run lint`; `bun run web:build`.
- Kanban task action footer spacing: added small bottom padding to `.task-card__actions` so compact action buttons have breathing room below. Validation: `bun run typecheck`; `bun run lint`; `bun run web:build`.
- Manual apply_patch UI test: added a test memory entry via `apply_patch` so refreshed web timeline can verify real diff gutter line numbers against `MEMORY.md`. Validation: visual web check pending.
- Task modal crop/accessibility fix: `.task-form-dialog` now uses safe viewport width/height, 24px desktop/16px mobile inner gutter, glass background, visible outer overflow, inner scroll body, readable labels, responsive textarea, and full-width native-select wrappers with extra input/select text inset plus Kanban-style modal footer action buttons; `TaskModal` controls have associated labels and `NativeSelect` no longer duplicates `id` on its wrapper so labels target the actual `<select>`. Added `TaskModal.test.tsx`; rebuilt packaged SPA. Validation: focused `TaskModal.test.tsx`; full `bun run test:web`; `bun run typecheck`; `bun run lint`; `bun run web:build`; `git diff --check`.
- Board stages editor style alignment: `modal-card--board-editor` now matches task modal glass/gutter/scroll treatment; stage rows use larger padded rounded surfaces, input/select labels and insets match task form, and reorder/remove/add/save buttons use modal-scaled ghost hover styling. Validation: focused `BoardStageEditorModal.test.tsx` + `TaskModal.test.tsx`; `bun run typecheck`; `bun run lint`; `bun run web:build`; `git diff --check`.
- Board stages checkbox visibility fix: auto-start checkbox gets explicit `board-stage-editor__checkbox` class with visible dark-surface border/background, cobalt checked state, hover/focus state, and disabled styling for fixed stages; rebuilt packaged SPA. Validation: focused `BoardStageEditorModal.test.tsx`; `bun run typecheck`; `bun run lint`; `bun run web:build`; `git diff --check`.
- Kanban stage header long-badge fix: `.board-column__header` has fixed `7.25rem` height/hidden overflow, heading/name are width-constrained, meta badges wrap into a capped two-line area with horizontal overflow hidden, and count/labels do not shrink; added `StageColumn.test.tsx`; rebuilt packaged SPA. Validation: focused `StageColumn` test; `bun run test:web`; `bun run typecheck`; `bun run lint`; `bun run web:build`; `git diff --check`.
- Kanban header actions hover: Edit Stages/Add Task now use shared `board-layout__action-button` ghost styling matching header-nav/task action hover (muted foreground/icons, muted hover background, fast transition); rebuilt packaged SPA. Validation: `bun run typecheck`; `bun run lint`; `bun run web:build`; `git diff --check`.
- Dashboard scope toggle styling: Workspace/Global use `dashboard-controls__scope-button` with ghost/header-nav-like spacing, muted text, fast hover/focus background transition, and selected muted state; rebuilt packaged SPA. Validation: `bun run typecheck`; `bun run lint`; `bun run web:build`; `git diff --check`.
- Dashboard date picker redesign: start/end date inputs are wrapped in labeled `dashboard-date-field` ghost chips with visible calendar icons, muted-to-foreground hover/focus states, hidden native picker overlay, and no input border/ring; rebuilt packaged SPA. Validation: `bun run typecheck`; `bun run lint`; `bun run web:build`; `git diff --check`.
- Dashboard date labels: date range now displays muted `From` before start date and `To` before end date instead of a dash separator; rebuilt packaged SPA. Validation: `bun run typecheck`; `bun run lint`; `bun run web:build`; `git diff --check`.
- Dashboard date picker centering: dashboard header filters now use a 3-column grid so the date range group is centered and Workspace/Global remains right-aligned, with mobile stacking centered; rebuilt packaged SPA. Validation: `bun run typecheck`; `bun run lint`; `bun run web:build`; `git diff --check`.
- Dashboard header alignment: header is now explicit 3-column grid sections: Observability left, date picker centered, Workspace/Global right; removed separator/filter wrapper and center-stacks on mobile; rebuilt packaged SPA. Validation: `bun run typecheck`; `bun run lint`; `bun run web:build`; `git diff --check`.
- Dashboard All Runs filters: moved status/provider/model filters into the panel header single row between title and count; provider/model are dropdowns populated from current run data, with ghost-style selects and responsive horizontal overflow. Validation: `bun run typecheck`; `bun run lint`; `bun run web:build`; `git diff --check`.
- Dashboard All Runs dropdown hover: filter dropdown buttons now match shared header/ghost controls with 2.5rem height, rounded md corners, muted text/icon defaults, muted hover/focus background, foreground hover text, and icon opacity transition. Validation: `bun run typecheck`; `bun run lint`; `bun run web:build`; `git diff --check`.
- Dashboard All Runs count badge: right-side total badge now has general-design spacing with right margin, 2rem min-height, md radius, muted background/text, and mono compact label. Validation: `bun run typecheck`; `bun run lint`; `bun run web:build`; `git diff --check`.
- Session markdown table rendering: assistant/thinking markdown now uses `remark-gfm` via shared `MarkdownContent`, so pipe tables parse as real tables; assistant content scrolls horizontally for wide tables with no wrapping in table cells. Added regression in `SessionTimeline.test.tsx`; rebuilt packaged SPA. Validation: focused `SessionTimeline.test.tsx`; `bun run typecheck`; `bun run lint`; `bun run web:build`.
