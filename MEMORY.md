# MEMORY.md

## Metadata
- Last compacted: 2026-09-12
- Scope: durable repo memory + active-day task events.
- Format: only `Metadata`, `Long-Term Memory`, and `Detailed Task Events`.

## Long-Term Memory
- Memory: keep durable rules, decisions, validation patterns, open follow-ups. New day: review/promote prior outcomes before deleting dated detail; active-day events append-only.
- Workflow: no commit/push/merge/PR unless asked. Session-only `TODO.md`: `- [ ]`, `- [>]`, `- [x]`, `- [!]`, `- [-]`. Shell from repo root; direct Python = `python3`; cap noisy output.
- Validation: Python = Ruff lint/format, basedpyright, scoped/full pytest. Frontend = `bun run test:web`, `bun run lint`, `bun run typecheck`, `bun run web:build`. Docs = `bun run docs:build`. Report skipped/hung/known-broken checks.
- Constraints: no migrations/backcompat. Provider/tool HTTP = `urllib.request`; internal data = `~/.pbi-agent/`. Default `pbi-agent web` uses saved web settings/profile; provider/model runtime flags apply to `run`.
- API contracts: align FastAPI routes/schemas/session manager, `webapp/src/api.ts`, `webapp/src/types.ts`, generated types, static bundle. `bun run web:api-types` runs `scripts/generate_api_types.py`; `test_api_types_codegen.py` enforces output. JSON calls use generated `ApiOperationResponses`/`ApiJsonRequestBodies`, `apiRequest()`, `jsonBody()`. Non-GCP provider views retain nullable GCP fields.
- Tools: `explore_workspace` replaces public read/search/list tools; wraps `codetool-explore==0.7.1`, sole `read` tool. Targets content/path/read/list; regex default true; root arrays except read/list; root = base, not boundary; workspace-confined; file root reads directly. `read_web_url` belongs to `web`.
- Explore recovery: ordinary missing `list` basename/stem → literal basename `path` search under the same directory root; exact listings win. No recovery for read/file roots/cursor-bearing calls/explicit regex/malformed queries. Preserve filters/ignores/limits; validate result confinement, return workspace-relative candidates and path-search continuation. `ToolOutput.trace_metadata` persists tool metadata separately from transient display metadata.
- Shell: `codetool-shell` compresses decoded streams before 12k per-stream bounds; `compression=false` bypasses; non-bool rejected before execution; compression failure falls back to decoded text; no compression metadata. Display-backed runs use interruptible process groups + bounded cleanup. Direct UI ownership atomic; Stop preserves command history. Model shell interruption targets sub-agent owner + provider call id, not whole turn; Interrupt uses shared small-button icon padding.
- `allowed_tools`: group allow-list for profiles/`run`/commands/sub-agents = `read`, `write`, `web`, `sub-agent`, `shell`; CLI `--allowed-tools`, frontmatter `allowed_tools`. Native search requires `web`; empty list disables configurable built-ins. Command-only `ask-user` maps to runtime `ask_user`; otherwise UI-only/non-configurable, stripped from sub-agents.
- Prompts: active `ToolSpec.prompt_usage`/settings/exclusions drive tool guidance; omit disabled skills/sub-agents. Persona-only preamble + `<tool_usage_rules>`; workspace `AGENTS.md` → `<project_rules>`; mutable `MEMORY.md` last as `<workspace_memory>` for cache stability. Global `user_profile` → `<user_profile>` in main/child prompts; edits apply on new session or `/reload`, including hook fallback. Children retain attached component commands.
- Patching: OpenAI/ChatGPT `apply_patch` = Codex freeform V4A `custom_tool_call`, exact Lark grammar + local validation/moves; reject empty `Update File`/move-only hunks; compact text result. Cards preserve mixed tool order and per-file op/path/diff/count/moves. Non-V4A providers use `replace_in_file`/`write_file` instead.
- Catalogs: top-level `skills`, `commands`, `agents`; public `pbi-agent/{skills,commands,agents}`; install `.agents/...`. Official/custom install supports force-replace conflicts. Disabled skills persist workspace-scoped in `sessions.db` (`disabled_project_skills`); explicit disabled `$skill` mentions add per-turn guidance with composer token boundaries. `.agents/agents/*.md`: `name`, `description`, `model_profile_id`, `allowed_tools`; list disabled agents but exclude at runtime unless tagged.
- Sub-agents: active profile/CLI + `sub_agent_model` supply defaults. Parent CLI/command tool overrides reach profiled children; parent profile does not mask child profiles. Caps 400 requests/1800s/no recursion. Stable parent UI cards; child routes own wait/input/processing/usage.
- Commands: `plan` no questions; `plan-interactive` uses `ask_user`; `review` autonomous Markdown; `code-quality-review` explicit paths/`@file`/globs mean full-file scope. `fix-review` consumes both reviews, stops on `No findings.` / `No code-quality findings.`. `/new` redirects fresh web session + remaps Telegram defaults; `refine-task` clarifies only. `orchestrate`: supplied plan skips `planner`, else planner first, then worker/review/code-quality gates. `ship-task` branch/merge helpers. Strict `allowed_tools`; profiles `worker-mini`, `planner`, `reviewer`, `worker`, `worker-pro`.
- Kanban: `/create-task` only creates cards via `pbi-agent kanban create`. Recoverable tool errors allow complete outcomes; fatal errors/interrupts fail.
- Releases: v0.32.0 shipped. Plain `/release` authorizes minor release push/merge/publish unless narrowed; no post-PR history rewrite without approval. Use `release-writing`, `docs/changelog/index.md`, `docs/changelog/v<version>.md`; require validation triage, publish verification, continuation gates. `release.yml` publishes PyPI + GitHub Release, no `publish.yml`. After repair merges retry `gh workflow run release.yml --ref master -f version=<current-version>`; reject non-master/mismatched/empty versions, serialize without cancellation, retain current-master/tag/draft/PyPI guards. Never rerun old-SHA release after master advances. GitHub notes = tagged changelog minus VitePress frontmatter.
- Branding: token-efficient local coding agent; browser UI primary, non-interactive CLI for scripts/terminal. Pillars: compact instructions, bounded workspace output, default omitted tool history. Preserve `pbi-agent`, `pbi_agent`, `PBI_AGENT_*`, `~/.pbi-agent`, logo `src/pbi_agent/web/static/logo.jpg`.
- Providers: shared `src/pbi_agent/providers/{transport,auth_strategies,endpoints,tool_execution,runtime}.py` + `src/pbi_agent/providers/protocols/`; OpenAI transport specialized, tool execution shared. Generic Chat Completions preserves `prompt_tokens_details.cached_tokens`/cache writes into `TokenUsage`; omit `tools`/`tool_choice` when empty. Parse `reasoning`, `reasoning_content`, `reasoning_details`, `<think>...</think>`; replay raw tagged reasoning, separate display text.
- xAI: API key or `xai_account` OAuth; OIDC/PKCE via `urllib.request`, fixed `http://127.0.0.1:56121/callback`, no port fallback; validate HTTPS `x.ai`/`*.x.ai`, proactive/401 discovery refresh. Merge OAuth-only `grok-composer-2.5-fast` missing from model-list endpoints. `ProviderView.supports_stt` = provider eligibility; xAI account lacks STT/usage-limits support.
- Models: longest case-insensitive exact/prefix catalog match; `gpt-5.6` aliases Sol. User-supplied `gpt-6-astra` standard short-context $/MTok: input 10, cached 1, both write buckets 12.50, output 50; context window unspecified. Responses `input_tokens_details.cache_write_tokens` → `TokenUsage` catalog write bucket.
- Reasoning: discovery advertises effort strings; profile suggestions intersect main/child compatibility, fallback `low`/`medium`/`high`/`xhigh`. Accept/forward arbitrary non-empty values; retain protocol mappings such as Anthropic `xhigh`→`max`. Classic OpenAI `/v1/models` lacks capability metadata; enrich via documented GPT-5/o-series/Codex patterns.
- OpenAI/ChatGPT: Responses use `instructions` + `previous_response_id`, include `reasoning.encrypted_content` alongside provider includes. OpenAI-only `reasoning.mode` (`standard`/`pro`) validates selected main/child GPT-5.6 models; never forward to ChatGPT subscription. Copilot Responses lacks `previous_response_id`: replay local user/tool history. ChatGPT prepends system prompt; Codex client/discovery minimum 0.153.4. WebSocket-only transport, split timeouts/retry events; write/first-event/close caps 30s/30s/1s; no unsupported compression.
- ChatGPT continuation: before child execution, parent socket release clears previous/branch IDs + turn-state header, retaining replay buffer. First post-child request sends full conversation + active-turn reasoning/tools/results ID-less; same-socket rounds then chain normally. Exact code-less 400 `invalid_request_error` / `Invalid \`previous_response_id\`.` gets one-shot missing-ID recovery only if rejected request had ID; replay without rerunning tools, then normal continuation. Exclude generic validation/ID-less failures; test multiple rounds, bounded retry, `max_retries=0`, mixed batches, encrypted reasoning, unchanged classic HTTP/ordinary-tool paths. Codex clears incremental state on reconnect; `store=false`, encrypted reasoning, stable cache key, no retention override. 300s stream idle ≠ response-cache TTL; no Astra-specific expiry evidence.
- Open validation: reactive invalid-ID recovery smoke-tested after short child without tool rerun; direct recovered-ID reuse, delayed/encrypted-reasoning recovery, proactive post-child replay remain unverified live. No general connection-generation/request-compatibility tracking.
- Saved replay: incompatible raw provider traces fall back to canonical messages/tools; Responses strips output-only fields, persists/replays final assistant output only (live display may show intermediates). Failed turns retain user rows; resume/refresh replay unfinished user/intermediate/complete tool-result pairs regardless of `include_tool_history`; completed turns respect it. Group checkpoints by traced run input/completion, not consecutive user roles; deduplicate HTTP retries per logical request. Match initial-request chronology for forks/repeated prompts; support Google steps/content, attachment suffixes/image-only inputs, surviving compaction-wrapped checkpoints. Compaction refresh skips raw tools; recovery excludes pre-summary events. Missing traces must not lose unanswered prompts. Stop/discard separate; no reconstruction of previously deleted prompts.
- Local shell provenance: persist/fork `messages.is_local_command`; `agent/conversation_context.py::model_context_messages` excludes flagged rows from provider replay, compaction, child snapshots, prompt enhancement. Never infer locality from `!`/adjacency; preserve CLI/Telegram bang turns, UI history, compaction boundaries, model tools. No automatic migration; explicit exception `scripts/migrate_messages_is_local_command.py` requires DB path + approval for user DB, unique SQLite/WAL-safe backup under write lock, no-op on rerun. Old rows default 0 without inference.
- Google Interactions: stateless/saved replay uses steps (`user_input`, `model_output`, tools/results), not role/content turn lists; fresh single turns may use string/content.
- `google_gcp`: Vertex/Gemini handlers for `generateContent`, Chat Completions, Responses/xAI, Anthropic Messages. GCP xAI/Responses use client history; no native search until shape-native tools exist. Same-shape runtime updates preserve history. Project/location: saved provider/CLI before env. API-key-like tokens use `x-goog-api-key` + Gemini express `v1beta1`; unsupported-token errors retry once via standard Vertex OAuth/ADC with saved project/location. Non-Gemini skips API-key-like auth for OAuth2/ADC; forced API key fails early. ADC `gcloud auth application-default print-access-token`, default 30s via `PBI_AGENT_GOOGLE_GCP_ACCESS_TOKEN_TIMEOUT`; cache bearer until expiry, refresh once after expiry errors. Strip Gemini `google/` prefix; Responses schemas drop unsupported combinators such as `oneOf`.
- Web sessions: session-scoped APIs, no public `/api/live-sessions/*` or `/sessions/live/:id`; blank/saved sessions work without active live id; completed Kanban runs detach live ids, retain conversation. `src/pbi_agent/web/session_manager.py` facade over `src/pbi_agent/web/session/`; follow-ups `web/session/follow_ups.py`, uploads `web/session/image_uploads.py`; patch globals in `pbi_agent.web.session.workers`/`pbi_agent.web.session.provider_auth`. Lease renewal reuses SQLite store, reconnects with stop-aware busy/locked backoff.
- Channels: workspace-scoped `sessions.db` configs/query cache. Telegram `urllib.request` polling, per-runner token leases, per-source queues/session mappings, best-effort 👀 after enqueue; text/photo/image-document input. Outbound Markdown formatting/chunking `src/pbi_agent/channels/telegram_markdown.py`. `pbi-agent channels` show/configure/restart: configure persists without runner; restart validates locally unless same-workspace web server confirmed.
- Web events/workspaces: SSE `GET /api/events/{stream}`, `/api/events/sessions/{session_id}` with `server.connected`, heartbeat, `since`/`Last-Event-ID`; no event WebSockets; defensive `parseSseEvent()`. Explicit workspace root/directory key across web/session/task/child/tool/extension/MCP; `workspace_switched` clears scoped state, redirects other tabs to Sessions. WSL picker uses `powershell.exe` + `wslpath`; `/api/sessions?q=` literal-LIKE current-workspace title/message search.
- Timeline/durability: persisted `msg-<messages.id>` + stable part ids; optimistic/local rekey via `message_rekeyed`. `history_items` canonical chronology; overlay nonduplicates only, avoid stale streams for ended/static projections. `run_sessions` projection/status; `snapshot_json` caches persisted `web_event`; `RunTracer` canonical run ids; replay original `seq`/`created_at`. Startup orphans → `stale`, clear processing/wait/pending, downgrade running snapshots.
- Compaction/fork/input: clear completed `previous_id`, restore persisted history including unanswered trailing turns + same-turn tools. `POST /api/sessions/{session_id}/fork` copies through target message, resets usage/previous_id. Direct shell disables `WebDisplay` input via `begin_direct_command()`/`finish_direct_command()`. `attachLiveSession(..., preserveEventCursor)` preserves input/processing at equal/newer cursor.
- Notifications: main sessions multi-turn; success never emits `live_session_ended`. `SessionFinishedNotificationEffects`: once per AFK working→ready edge; `SessionEndedNotificationEffects`: terminated/errored runs; `AskUserNotificationEffects`: prompts.
- UI: shadcn primitives/semantic tokens, `cn()`, `lucide-react`; no raw colors/manual dark-mode/custom overlay z-index/space utilities. Large modals need gutters + inner scroll; edge floats clamp both axes; Dialog/Sheet/Drawer accessible titles. Settings follow `ProviderModal`/`FormDialog`; add dialogs flat rows + parent scroll. `Badge` statuses: `success`/`warning`/`info`/`running`/`completed`/`failed`. Unlayered `webapp/src/styles/reset.css` beats Tailwind v4: scoped fixes in `webapp/src/styles/overlays.css` unless moving reset to `@layer base`. Bundled Geist; default `System`, manual Prism/Light/Dark.
- Composer: `@file`/`$skill` highlights, `/api/skills/search`, slash API `kind` badges. Ctrl+Space: empty → dictation, normal draft → hidden no-tool enhancement + wand. Preserve composer tokens/details/context; optional `web.prompt_enhancement_profile_id` per blank/saved request, `Current model`/null retains runtime fallback. ArrowUp/ArrowDown history restores draft. `AppSessionsContextPanel`; `--topbar-height: 48px`; shortcuts `Shift+Tab`, `Alt+Shift+H`, `Alt+Shift+E`.
- Follow-ups: live main-session checkpoint steering via backend/SSE, `CheckpointFollowUpDisplay`, provider `steer_user_input` at tool boundaries; FIFO after-finish fallback/auto-submit; block shell follow-ups. Menu defaults checkpoint, keyboard navigation. Open bug: interrupt after delivery can remove accepted user item from live timeline despite persisted `messages`/trace; remove only undelivered queued input or preserve accepted steer messages.
- Work UI: copy/fork; Shiki `CodeBlock`; `WorkingSummary` categories. Coalesce thinking/tools between messages; ended turns collapse behind `Intermediate results`, active work uncollapsed with stable placeholders. Expansion must preserve auto-follow; one shadcn Accordion closes other tool cards.
- Mentions: first dispatched search per token refreshes coalesced in-memory index; stale results usable. Global agent/file ranking; generation+revision scope existence caches across manager recreation. API distinguishes hard truncation from fuzzy approximation.
- Explorer/diff: dependency-free React tree; Material Icon Theme via `webapp/src/lib/workspaceFileIcons.ts`; closed preview opens on file click, 72%/28% split, selected file scrolls to tree top. `CodeBlock` flex/max-height override. `git status --porcelain=v1 -z --untracked-files=all`; `/api/files/tree` git metadata; `/api/files/diff` unified/synthetic diffs. `GitDiffResult` `stacked`/`split`, synced split scroll; hide metadata/hunk headers. Ignore rules apply even tracked/statused; stdlib `.gitignore` fallback handles non-git + leading `**/`. Deterministic Rolldown: exclude duplicate SVG bytes, alias `angular-resolver`→`angular-guard`, `dtx`→`doctex`, `sty`→`latex-package`.
- Observability/maintenance: active run headers overlay persisted non-`web_event` metrics; persist cached/reasoning usage from canonical `TokenUsage` at trace time, never web decoding; terminal aggregates stored. `run_startup_maintenance()` once/UTC day via SQLite `maintenance_state`; `maintenance.retention_days` default 30/min 1; preserve referenced uploads, batch deletes below SQLite variable limits. PyPI check via `urllib.request`; newer-version warning only, Rich stderr.
- Hooks: `src/pbi_agent/hooks/`; configs `~/.pbi-agent/hooks.json`, `<repo>/.agents/hooks.json`; state `~/.pbi-agent/hooks_state.json`. Skip untrusted/modified hooks until trusted; startup warns. Events `SessionStart`, `UserPromptSubmit`, `PreToolUse`, `PostToolUse`, `PreCompact`, `PostCompact`, `Stop`, `SubagentStart`, `SubagentStop`. Settings → Hooks, `/api/hooks`, `pbi-agent hooks`, `/hooks`. Trust bypass `--dangerously-bypass-hook-trust` / `PBI_AGENT_DANGEROUSLY_BYPASS_HOOK_TRUST=1` never enables disabled hooks. Global/project JSON cannot self-declare managed trust; reserved for policy sources. Local Stop example `.agents/hooks.json` + `.agents/hooks/session_end.py`; test trust helper `.agents/hooks/trust_project_hooks.py`.
- Sandbox/init/extensions: `pbi-agent sandbox [web|run]`: bundled Dockerfile, matching PyPI version, shared sessions/Kanban/runs, per-workspace `/home/pbi`, executable writable `/tmp` tmpfs, name `pbi-agent-{normalized-workspace-folder}`. `pbi-agent init`/`/init`: starter `AGENTS.md`, default official commands/agents, workspace-scoped disabled agent state; per-item failures, rollback newly installed agents if disable persistence fails; `--force`/`--overwrite` replace. Extensions report failures, capture/bound fd streams, lock setup per extension, require `/`, dynamically reserve built-ins/project commands/MCP tools.
- Test hygiene: uploads isolated to `tmp_path / "web_uploads"`; FastAPI/Starlette TestClient pinned `httpx2==2.4.0`. Checkpoint tests gate worker drain with explicit event after pending assertions; avoid queue-consuming sleep polling.

## Detailed Task Events

## 2026-09-12
- Investigated Astra invalid response IDs against local Codex HEAD/tag, official WebSocket/cache docs, and the reported session read-only. Confirmed stale continuation after deliberate parent socket closure plus unrecognized code-less 400; request settings and tool-call IDs were unchanged/matched. Validation: 93 OpenAI provider tests and in-memory close/classifier/replay reproduction passed. Only TODO/memory updated; runtime/database untouched. Connection-scoped continuation and bounded invalid-ID recovery remain open.
- Implemented narrowly matched one-shot invalid-ID 400 recovery via existing full-active-turn replay; added 10 provider regression cases for transcript preservation, no tool re-execution, bounded retries, and normal continuation. Validation: 103 focused provider tests, full Python suite, Ruff lint/format, basedpyright, and both sequential CLI review gates passed without findings. Timeout/reconnect policy unchanged; no live delayed-sub-agent recovery reproduction.
- Inspected live smoke-test session `f9f2afb7b96243fa9c175d2838bc58ad` read-only: parent run `35565513830b48548c960e75f885aed3` hit exact code-less 400 after an 11s sub-agent, then attempt 2 succeeded ID-less with identical initial input, tool call/result, and settings; no tool rerun. Recovery ended the turn; next turn started ID-less then chained normally, so direct reuse of recovered ID and delayed/encrypted-reasoning recovery remain unverified live. Only TODO/memory changed.
- Published v0.32.0 from all seven unreleased local commits via PR #350; bumped version/lockfile, added changelog/navigation, and repaired pre-existing stale static hashes (reference-only changes, repeat build clean). Validation: 1,782 Python + 720 frontend tests, Ruff/format/types/dead-code, API/static checks, docs/web/package builds, PR/master/release/docs CI passed; tag `f7f08219`, normalized GitHub notes, and PyPI wheel/sdist verified after brief metadata propagation. Local master synced; uncommitted orchestrator profile edit preserved/excluded, session bookkeeping remains local, dependency PRs untouched.
- Reviewed d8840418 and made sub-agent socket release proactively clear continuation state while retaining full replay history; expanded regression tests for first-attempt replay, encrypted reasoning, mixed batches, single tool execution, resumed chaining, and unaffected HTTP/ordinary-tool paths. Validation: regression failed before fix; 107 provider tests, Ruff/format/basedpyright, diff check, and full 1,786-test suite passed. Initial full run hit a dashboard SQLite-lock flake; isolated test and full rerun passed. No live-provider smoke test.
- Compressed long-term memory: removed completed release/validation histories, merged duplicate outcomes, shortened prose; retained durable constraints/open follow-ups and all active-day entries. Validation: manual readback for structure/meaning; no commands/tests (memory-only).
- Implemented guarded missing-list filename recovery, explicit path-vs-list guidance, search continuation, symlink checks, and persisted recovery metadata; added 75 regression cases. Validation: reported call succeeds against this workspace; 175 scoped tests, full Python suite, Ruff lint/format, basedpyright, diff check, and final specialist review passed. Initial full run hit the unrelated after-finish FIFO queue timing test; isolated test and full rerun passed. No commit/push.
- Tightened explore ToolSpec descriptions and prompt examples; removed repeated path/list guidance and redundant enum prose, retained defaults/recovery caveats. Runtime/schema structure unchanged; guidance assertions updated. Validation: 153 tool/system-prompt tests, Ruff lint/format, basedpyright, diff check passed.