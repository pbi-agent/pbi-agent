# MEMORY.md

## Metadata
- Last compacted: 2026-05-10
- Scope: durable repo memory + active-day task events.
- Format: only `Metadata`, `Long-Term Memory`, and `Detailed Task Events`.

## Long-Term Memory
- Memory: token-efficient; keep durable decisions/follow-ups only, not logs.
- Workflow: no commit/push/merge/PR unless asked. Use session-only `TODO.md`; markers `[ ]`, `[>]`, `[X]`, `[!]`, `[-]`. Shell from workspace root; use `python3` for direct Python shell commands.
- Validate touched surface: Python Ruff+basedpyright+pytest; frontend Bun tests/lint/typecheck/build; docs build. Note skipped/hung checks.
- Architecture: CLI `src/pbi_agent/__main__.py` -> `src/pbi_agent/cli.py`; default command `web`; backend FastAPI `src/pbi_agent/web/`; frontend Vite React `webapp/`.
- API changes: align routes/schemas/session manager with `webapp/src/api.ts` + `webapp/src/types.ts`.
- Web API contract: `bun run web:api-types` runs `scripts/generate_api_types.py` to regenerate `webapp/src/api-types.generated.ts` from FastAPI OpenAPI plus extra SSE models; output includes schema types, SSE event unions, `ApiOperationResponses`, and `ApiJsonRequestBodies`. Pytest `test_api_types_codegen.py` enforces generated output is current.
- Constraints: provider/tool HTTP via `urllib.request`; internal data in `~/.pbi-agent/`; no migrations/backcompat.
- Tools: built-in `python_exec`, `list_files`, `search_files` removed; use shell (`rg --files`, `find`, `rg -n`) and Python as needed.
- Project imports: top-level `skills`, `commands`, `agents`; public catalogs under `pbi-agent/{skills,commands,agents}`; local install to `.agents/...`.
- Sub-agents: `.agents/agents/*.md`; frontmatter only `name`/`description`; child model from active profile/CLI + `sub_agent_model`.
- Local commands: `plan` no questions; `review` Markdown; `fix-review` stops on `No findings.`; `ship-task` branch/merge helpers; `release` needs explicit publish wording and no history rewrite after PR unless approved.
- Release docs: `release-writing` skill governs releases; changelog index `docs/changelog/index.md`, files `docs/changelog/v<version>.md`, and VitePress sidebar links releases; release retrospective added validation triage + publish verification + continuation gate safety.
- Branding: public copy = local coding agent; preserve `pbi-agent`, `pbi_agent`, `PBI_AGENT_*`, `~/.pbi-agent`; logo `src/pbi_agent/web/static/logo.jpg`.
- Web/session: saved sessions use session-scoped APIs; `/api/live-sessions/*` and `/sessions/live/:id` are removed from user-facing test expectations. Blank/saved sessions can start/continue without an active live id; completed Kanban-bound runs detach live ids without ending the saved conversation.
- Web session manager: `src/pbi_agent/web/session_manager.py` is a thin public facade for `WebSessionManager`/startup lease; implementation mixins live under `src/pbi_agent/web/session/` by utility scope. Patch worker/auth globals in `pbi_agent.web.session.workers` and `pbi_agent.web.session.provider_auth`.
- Web events: frontend app/session streams use SSE over `GET /api/events/{stream}` and `/api/events/sessions/{session_id}` with `server.connected`/heartbeat and `since`/`Last-Event-ID`; web UI WebSocket event routes/helpers are removed. SSE event payloads are generated TS unions, but the frontend stream boundary keeps defensive `parseSseEvent()` validation.
- Web message identity: saved history/live replay use canonical persisted item/message ids `msg-<messages.id>` plus stable part ids; optimistic/display-local live items are replaced by `message_rekeyed` once persisted.
- Web status contracts: session/live API models use lifecycle statuses (`idle`/`starting`/`running`/`waiting_for_input`/`ended`/`failed`/`stale`); run history/detail/all-runs use persisted run-record statuses (`started`/`completed`/`interrupted`/`failed`, etc.). Keep them distinct.
- Web command input hardening: direct saved-session shell commands hold `WebDisplay` input disabled with `begin_direct_command()`/`finish_direct_command()`, and frontend `attachLiveSession(..., preserveEventCursor)` preserves current input/processing state when the applied SSE cursor is equal to or newer than the submit response cursor.
- Web live snapshots: `LiveSessionSnapshotModel` preserves typed `processing` and pending user-question payloads; frontend boundary types derive from generated models and keep `pending_user_questions` required nullable.
- Web frontend API boundary: `webapp/src/api.ts` should use generated `ApiOperationResponses`/`ApiJsonRequestBodies` via `apiRequest()` and `jsonBody()` when adding JSON API calls.
- Web durable runs: `run_sessions` has web projection fields/status, `snapshot_json` caches timeline from persisted `web_event` records, unfinished web runs become `stale` on startup, and web `RunTracer` attaches to canonical run ids.
- Web durable event replay: saved-session event streams reconstruct from persisted `web_event` records with original `seq`/`created_at`, so SSE `since` and `Last-Event-ID` cursors remain valid after restart.
- Saved-session timelines: API `history_items` are canonical chronological messages; frontend overlays only non-duplicate live/work snapshot items and avoids opening stale live streams for ended/static projections.
- Session compaction: clear completed `previous_id`; resume restores persisted user/assistant history; compaction includes unanswered trailing user turns and same-turn tool exchanges.
- Providers: OpenAI Responses use `instructions` + `previous_response_id`; ChatGPT subscription prepends system prompt; Codex transport is WebSocket-only (`chatgpt_codex_backend.py`), no unsupported compression.
- Web UI: prefer shadcn tokens/components; overlays use shared readable spacing; large modals need safe gutters + inner scroll; Radix tooltip `collisionPadding`/`sideOffset` may not protect cross-axis viewport gutters, so clamp both axes for edge-adjacent tooltips.
- Timeline: work runs coalesce thinking/tool activity between chat messages; active placeholders have stable keys; completed historical work-run expansion must not reset auto-follow; new tool content inside expanded active work runs scrolls to the inner latest item.
- Test hygiene: pytest isolates web upload root to per-test `tmp_path / "web_uploads"` to prevent fixture uploads leaking into `~/.pbi-agent/web_uploads`.
- Maintenance: startup calls `run_startup_maintenance()` once per UTC day via SQLite `maintenance_state`; retention lives in internal config `maintenance.retention_days` (default 30, min 1) and is editable via `pbi-agent config maintenance` plus web Settings/API. Purge removes old sessions/messages/run_sessions/observability_events/web_manager_leases and old unreferenced `~/.pbi-agent/web_uploads` files while preserving uploads referenced by retained messages/Kanban tasks; PyPI update check uses `urllib.request` and prints `uv tool install pbi-agent --upgrade` notice when newer.
- Sandbox: `pbi-agent sandbox [web|run]` builds a bundled package Dockerfile from `src/pbi_agent/sandbox/Dockerfile.sandbox`, installs matching `pbi-agent==${PBI_AGENT_VERSION}` from PyPI, ignores workspace Dockerfiles, uses versioned default image tags, per-workspace hashed container mount/home volume, host-side browser open for web, always-created host `~/.pbi-agent` bind mount, and an Alpine runtime image with minimal `apk` packages (`bash`, `ca-certificates`, `curl`, `git`, `github-cli`, `libstdc++`, `patch`, `ripgrep`, `unzip`) plus site-packages cleanup. Sandbox passes host workspace identity/display envs while tools execute in the container cwd, so sessions/Kanban/runs are shared with non-sandbox. User-local sandbox installs/caches persist per project via writable `/home/pbi` Docker volume; Docker PATH stays generic with `/home/pbi/.local/bin`, while entrypoint/local-source/default-shell/shell-tool bootstrap loads user profiles, discovers `$HOME/**/bin`, and refreshes discovery on missing commands so tools like Bun/Cargo are visible without per-tool ENV hardcoding. Validate with focused sandbox pytest, Ruff, basedpyright, docs build, `uv build`, and Docker build/smoke when available.
- Project install UI: Settings Project Skills/Commands/Agents support official/custom candidate install, force-replace conflict flow, flat ProviderModal-style add dialogs, and API types/static rebuilds after contract changes.
- CLI web startup: server banner is single source of `Serving on...`; Linux browser launch strips only legacy `atk-bridge` from `GTK_MODULES` to suppress Ubuntu noise.
- Shell tool timeout policy: default `timeout_ms` 30000, max 300000; reject non-positive/non-integer/bool before subprocess; schema descriptions carry bounds, not unsupported strict-schema defaults.
- Sub-agent web UI: parent timeline keeps child work in stable sub-agent cards; child routes own wait/input/processing/usage state; aggregate child processing/interruptability controls parent Working state without leaking child transcript churn.
- Startup stale cleanup: orphan active turn/sub-agent run records covered by stale web-session intervals become stale; stale projections clear processing/wait/pending state and downgrade running tool/sub-agent snapshots.
- Saved-session fork: `POST /api/sessions/{session_id}/fork` duplicates messages/uploads through target message ids, resets usage/previous_id, stores fork metadata/title `Fork-{original-title}`, and cleans copied uploads on failure.
- Project command `/create-task` creates Kanban cards through `pbi-agent kanban create` without implementation work.

## Detailed Task Events
## 2026-05-10
- Added special tag highlighting for web chat input/history: Composer overlays non-layout-shifting highlights for `@file` and `$skill` tokens, and user timeline now highlights both persisted file paths and `$skill` tags. Validation: focused Composer/TimelineEntry Vitest, lint, typecheck, web build, and `git diff --check` passed.
- Implemented `$` skill tag completions in web composer: added `/api/skills/search`, skill mention ranking helper, generated API types/static assets, Composer skill mode/parser/UI, and system prompt `$<skill-name>` explicit invocation rule. Validation: focused skill/API/system pytest, API type codegen pytest, focused Composer/API Vitest, lint, typecheck, web build, Ruff check/format, basedpyright passed.
- Fixed fork-feature startup crash on existing session databases by having `SessionStore._ensure_schema()` add missing fork metadata columns with defaults; added regression coverage for older `sessions` table hydration. Validation: `uv run pytest -q --tb=short -x tests/test_session_store.py`, focused web fork endpoint pytest, and Ruff check on touched files passed.
- Refactored Web UI fork shortcut: replaced per-message dropdown with a single Lucide `GitForkIcon` ghost-button rendered outside the assistant card at the bottom-left, gated to assistant role only (no user messages, no sub-agent items, persisted msg-* only); removed `MoreHorizontal`/`DropdownMenu*` imports and `.timeline-entry__actions-trigger` CSS, added `.timeline-entry__fork`/`__fork-button` styles, wrapped trigger in shadcn `Tooltip`. Tests use `TooltipProvider` wrapper; added regression hiding the action for user messages. Validation: focused `TimelineEntry.test.tsx` and full `webapp/src/components/session` Vitest (158/158), `bun run lint`, `bun run typecheck`, `bun run web:build` passed.
- Added Web UI session copy shortcuts: hover/focus-only Lucide copy actions for user/assistant turns, fork now shares the hidden action row, and assistant markdown supports snippet copy for code, tables (TSV), blockquotes, and lists with copied/failed feedback; rebuilt static assets. Validation: focused TimelineEntry tests, SessionTimeline tests, full session Vitest, full `bun run test:web`, `bun run lint`, `bun run typecheck`, `bun run web:build`, and `git diff --check` passed.
- Updated copy shortcuts to switch from Lucide Copy to Check for 3 seconds after a successful copy, preserving tooltip/ARIA feedback and static asset rebuild. Validation: focused `TimelineEntry.test.tsx`, `bun run lint`, `bun run typecheck`, `bun run web:build`, and `git diff --check` passed.
- Fixed Web UI session fenced-code rendering: assistant markdown code fences now extract `language-*`, render with language header and the existing Shiki-backed `CodeBlock`, while snippet copy still copies raw code; rebuilt static assets. Validation: focused/full web tests, `bun run lint`, `bun run typecheck`, `bun run web:build`, and `git diff --check` passed.
- Fixed Session timeline collapsed Working summary coverage: `read_web_url` now counts as a read, built-in tool categories include search/shell/edit/sub-agent/question/other, metadata-less class/text fallbacks are normalized, and static assets were rebuilt. Validation: focused SessionTimeline Vitest, full `bun run test:web`, `bun run lint`, `bun run typecheck`, `bun run web:build`, and `git diff --check` passed.
- Prepared release PR v0.5.0 from merged PR #285: bumped `pyproject.toml`/`uv.lock`, added changelog page/index/sidebar entries, committed `63f3b34`, pushed `chore/release-v0.5.0`, and opened PR #286. Validation: Ruff check/format, basedpyright, dead_code, full pytest, docs build, diff check, and GitHub `test` check passed.
- Published release v0.5.0: merged PR #286 to `master` as `7437478`, Release workflow run `25617483498` passed including PyPI upload and GitHub Release publish, stripped VitePress frontmatter from GitHub Release notes, verified tag/release target and PyPI wheel+sdist via project JSON. Remote release branch was deleted; only local `MEMORY.md`/`TODO.md` bookkeeping remained.
- Ran daily MEMORY.md maintenance: compacted 2026-05-09 detailed entries into durable Long-Term Memory bullets, removed old task-log details, and updated metadata. Validation: structure checked by script; no code tests needed.
- Added command `model_profile_id` frontmatter metadata: command discovery/API now expose it, web project slash-command submissions use the configured model profile over the UI-selected profile, frontend API types/tests and docs updated. Validation: focused pytest/Vitest, `bun run web:api-types`, Ruff, basedpyright, `bun run lint`, `bun run typecheck`, `bun run docs:build`, and `git diff --check` passed.
- Fixed review findings for command `model_profile_id`: remote command install preserves frontmatter, command profile overrides use non-persisted transient runtime changes and restore selected UI/session runtime, and local `/compact` bypasses project override lookup. Validation: focused project/web pytest, Ruff check/format, src basedpyright, and `git diff --check` passed; full test-file basedpyright still has pre-existing unrelated errors in `tests/test_web_serve.py`.
- Normalized project command Markdown manifests to skill-style frontmatter (`name`, `description`, optional `model_profile_id`), updated installed/remote command API metadata and Settings command cards/candidates to the skill/agent card pattern, documented the schema, normalized local `.agents/commands/*.md`, and rebuilt static assets. Validation: focused/full web tests, focused pytest, Ruff check/format, source basedpyright, lint, typecheck, docs build, and web build passed.
- Updated `~/pbi-agent-catalogue/commands` command catalog files to normalized command frontmatter (`name`, `description`, `model_profile_id: analysis`) and README alias wording. Validation: parsed all catalog commands with `parse_command_markdown()`/`CommandConfig.validate()`; catalog git status shows only expected tracked files plus pre-existing untracked `.agents/`.
- Fixed review finding for command profile restore: command turns now restore to the submitted UI/session profile instead of the stale live runtime, and existing saved sessions persist that selected profile while keeping command override transient. Validation: focused web pytest, Ruff check/format, source basedpyright, and `git diff --check` passed.
- Fixed review finding for transient command profile persistence: `QueuedRuntimeChange` can carry a saved-session runtime, and `run_session_loop()` writes session metadata with that saved runtime while the actual command turn uses the transient runtime. Validation: focused session/web pytest, Ruff check/format, source basedpyright, and `git diff --check` passed.
- Added local project command `/check-confidence` with normalized command frontmatter and `model_profile_id: worker`; it instructs the agent to assess production shipping confidence with calibrated score, comfort reasons, residual risks, and final validation gate. Validation: parsed command with `parse_command_markdown()` and `CommandConfig.validate()`.

## Detailed Task Events

## 2026-05-10
- Implemented `$` skill tag completions in web composer: added `/api/skills/search`, skill mention ranking helper, generated API types/static assets, Composer skill mode/parser/UI, and system prompt `$<skill-name>` explicit invocation rule. Validation: focused skill/API/system pytest, API type codegen pytest, focused Composer/API Vitest, lint, typecheck, web build, Ruff check/format, basedpyright passed.
- Added project sub-agent `model_profile_id` frontmatter support and Settings agent profile badges/candidate metadata; delegated sub-agents with that key resolve and use the saved model profile for the child run only. Validation: focused sub-agent/project-agent/web pytest, full `bun run test:web`, Ruff check/format, source basedpyright, lint, typecheck, docs build, web build.
- Fixed review finding for sub-agent profile overrides: missing/deleted configured profiles now return structured failed sub-agent results with display lifecycle instead of bubbling as generic tool execution failures. Validation: focused session pytest, Ruff check/format, source basedpyright, and `git diff --check` passed; full `basedpyright tests/test_session.py` still has pre-existing unrelated fixture/decode errors.
- Added Settings Preview buttons for installed skills and agents, reusing command preview dialog styling; API skill/agent views now include body `instructions`, generated frontend types and static web assets updated. Validation: focused web config pytest, focused/full Settings/web Vitest, Ruff check/format, source basedpyright, lint, typecheck, web build, and `git diff --check` passed.
- Updated ask-user question UI to require an explicit suggestion selection for every question before Send enables, and changed the note field into an additional custom note sent alongside the selected suggestion; backend combines suggestion + note for tool output and static assets were rebuilt. Validation: focused UserQuestionsPanel/SessionPage Vitest, focused web/ask_user pytest, Ruff check/format on touched Python, lint, typecheck, web build, and git diff check passed.
- Fixed ask-user panel autofocus and remaining separators: unanswered prompts now focus the first suggestion so arrow-key navigation works immediately without clicking, and both the panel top border plus card footer divider were removed; static assets rebuilt. Validation: focused UserQuestionsPanel Vitest, lint, typecheck, web build, and git diff check passed.
- Updated `/orchestrate` command into a PLAN.md/REVIEW.md manager loop using plan/execute/review/fix-review/check-confidence/commit sub-agents, and added orchestrate artifact duties to those sub-agents. Validation: command/agent discovery parse script and `git diff --check` passed.
- Shortened local project sub-agent frontmatter descriptions while preserving instruction bodies/behavior. Validation: custom frontmatter parse/length check passed.
- Renamed local project sub-agents from slash-command-style names to action-role names (`planner`, `worker`, `reviewer`, `fixer`, `confidence-checker`, `committer`) and updated `/orchestrate` to delegate to them while preserving PLAN.md/REVIEW.md artifact behavior. Validation: sub-agent/command discovery parse script and `git diff --check` passed.
- Compressed prose in all local project sub-agent Markdown files while preserving frontmatter, headings, commands, code fences, and behavior. Validation: custom frontmatter/code-fence parse and `git diff --check` passed.
- Implemented gitignore-aware web file mention scanning via `pbi_agent.web.scan`, non-blocking `WorkspaceFileIndex` snapshots, `/api/files/search` scan-state payload, Composer scan-state UI/token parser, generated API/static assets, and focused scan/API/Composer coverage. Validation: focused Python pytest, full web Vitest/lint/typecheck/build, Ruff check/format, basedpyright passed; full pytest still stops at unrelated `tests/cli/test_catalogs.py::DefaultWebCommandTests::test_main_commands_list_lists_project_commands_without_settings`.
- Fixed review finding for Composer file mention scans: active `@` searches now poll while `scan_status == "scanning"` for cold/stale index states and Escape invalidates retries; focused Composer Vitest, lint, and typecheck passed.
- Fixed `@` file suggestions to include non-gitignored dot paths such as `.agents/commands/*.md`; fuzzy search no longer hides dot directories after the scan includes them. Validation: focused scan/input mention pytest, targeted Ruff check/format, and basedpyright passed.
- Compressed `.agents/commands/orchestrate.md` prose with `$compress`, preserving frontmatter, headings, inline code, and prompt-template code block. Validation: read-back inspection only per compress-skill no-CLI rule.
