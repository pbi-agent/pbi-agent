# MEMORY.md

## Metadata
- Last compacted: 2026-06-06
- Scope: durable repo memory + active-day task events.
- Format: only `Metadata`, `Long-Term Memory`, and `Detailed Task Events`.

## Long-Term Memory
- Memory: token-light. Keep durable decisions/conventions, reusable validation, active follow-ups. New day: promote useful prior-day outcomes, delete dated detail.
- Workflow: no commit/push/merge/PR unless asked. Use session-only `TODO.md` with `- [ ]`, `- [>]`, `- [x]`, `- [!]`, `- [-]`. Shell from repo root; direct Python uses `python3`; cap noisy output.
- Validation: Python = Ruff, format check, basedpyright, focused/full pytest as scope needs. Frontend = `bun run test:web`, `bun run lint`, `bun run typecheck`, `bun run web:build`. Docs = `bun run docs:build`. Note skipped/hung checks.
- Core constraints: no migrations/backcompat; provider/tool HTTP uses `urllib.request`; internal data under `~/.pbi-agent/`; `pbi-agent web` default command and saved web settings/profile, while provider/model runtime flags are for `run`.
- API/frontend contract changes: keep FastAPI routes/schemas/session manager aligned with `webapp/src/api.ts`, `webapp/src/types.ts`, generated API types, and static bundle. `bun run web:api-types` runs `scripts/generate_api_types.py`; `test_api_types_codegen.py` enforces output.
- Tool surface: `python_exec`, `list_files`, `search_files`, public `read_file`/`search_workspace` removed. Use shell (`rg --files`, `find`, `rg -n`) or `explore_workspace`. `read_web_url` is in `web` group.
- `allowed_tools`: one group allow-list for profiles, `run`, commands, sub-agents (`read`, `write`, `web`, `sub-agent`, `shell`). Project commands can use command-only `ask-user` for runtime `ask_user`. CLI flag `--allowed-tools`; command/sub-agent frontmatter key `allowed_tools`; native web search only via `web`; empty list disables configurable built-ins; `ask_user` UI-only/non-configurable outside command frontmatter and stripped from sub-agents.
- Tool UI/providers: Settings → Model Profiles exposes profile `allowed_tools` as Tool visibility; API/profile schemas include profile + resolved runtime `allowed_tools`. V4A providers (`openai`, `chatgpt`) advertise `apply_patch` and hide duplicate `replace_in_file`/`write_file`; non-V4A invert.
- System prompts: compose from active tool specs/settings/exclusions (`ToolSpec.prompt_usage`); disabled sub-agent/skill guidance omitted; direct/factory fallbacks use same filtered defaults; base prompt = preamble + active `<tool_usage_rules>`.
- OpenAI/ChatGPT `apply_patch`: Codex-compatible freeform custom tool with exact Lark grammar, raw V4A `custom_tool_call` input, local Lark validation/move support, rejection of empty `Update File`/move-only hunks, compact plain-text result. Web cards parse per-file op/path/diff/count + moves and preserve mixed tool order.
- Project catalogs/install: top-level `skills`, `commands`, `agents`; public catalogs under `pbi-agent/{skills,commands,agents}`; local install to `.agents/...`. Settings Project Skills/Commands/Agents support official/custom install, force-replace conflict flow, ProviderModal-style add dialogs, generated API types, and static rebuilds.
- Skill/agent enablement: disabled project skills stored workspace-scoped in `sessions.db` (`disabled_project_skills`); explicit disabled `$skill` mentions add per-turn instructions with composer token boundaries. `.agents/agents/*.md` supports `name`, `description`, `model_profile_id`, `allowed_tools`; catalogs list disabled agents, runtime filters them unless explicitly tagged.
- Sub-agents: child defaults from active profile/CLI + `sub_agent_model`; parent CLI/command tool overrides flow to profiled sub-agents but parent profile settings do not mask child profiles. Runtime caps: 400 provider requests, 1200s, no recursion. Web UI keeps child work in stable parent cards; child routes own wait/input/processing/usage.
- Local commands: `plan` no questions; `plan-interactive` uses `ask_user`; `review` Markdown/autonomous; `fix-review` stops on `No findings.`; `refine-task` clarifies only; `ship-task` branch/merge helpers; `release` needs explicit publish wording and no history rewrite after PR unless approved. Commands pin strict `allowed_tools`; complexity profiles: `worker-mini`, `planner`, `reviewer`, `worker`, `worker-pro`.
- Kanban/release: `/create-task` only creates cards via `pbi-agent kanban create`. Kanban worker returns complete outcomes despite recoverable tool errors; fatal exceptions/interrupts fail. Release docs use `release-writing`; changelog index `docs/changelog/index.md`, files `docs/changelog/v<version>.md`; release workflow needs validation triage, publish verification, continuation-gate safety.
- Recent release outcomes: v0.12.0 (PR #309) and v0.12.1 (PR #310) were published to GitHub Releases/PyPI; v0.12.1 was bookkeeping-only and GitHub Release notes were frontmatter-stripped after publish.
- Branding/providers: public copy = local coding agent. Preserve `pbi-agent`, `pbi_agent`, `PBI_AGENT_*`, `~/.pbi-agent`, logo `src/pbi_agent/web/static/logo.jpg`. OpenAI Responses use `instructions` + `previous_response_id`; ChatGPT subscription prepends system prompt; Codex transport is WebSocket-only with split timeouts/retry events, no unsupported compression.
- Provider modularization: shared backend helpers live in `src/pbi_agent/providers/{transport,auth_strategies,endpoints,tool_execution,runtime}.py`; protocol helpers live in `src/pbi_agent/providers/protocols/`. Existing providers use these where safe; OpenAI transport remains specialized, but tool execution is shared. `google_gcp` is a provider wrapper with shape-specific Vertex/Gemini handlers for Gemini `generateContent`, OpenAI chat completions, OpenAI Responses/xAI, and Anthropic Messages; it uses client-side history for GCP xAI/Responses, strips `google/` for Gemini endpoint paths, must not advertise native web search unless shape-native tools are implemented, uses API keys via `x-goog-api-key`/express endpoints and falls back to OAuth/ADC when Gemini express rejects the key type, caches ADC bearer tokens until expiry, derives Vertex project/location from saved provider or CLI settings before env fallback, and resolves ADC via `gcloud auth application-default print-access-token` with a 30s default timeout overrideable by `PBI_AGENT_GOOGLE_GCP_ACCESS_TOKEN_TIMEOUT`.
- OpenAI Responses requests always include `reasoning.encrypted_content` alongside provider-specific include values.
- Cross-provider saved-session replay falls back to canonical message/tool history when provider raw traces are incompatible; Responses replay strips output-only fields before sending request history.
- Google Interactions: saved-session/stateless history replay must use steps input (`user_input`, `model_output`, tool/result steps), not role/content turn-list objects; single fresh turns may still use simple string/content input.
- Web sessions/routes: saved sessions use session-scoped APIs; no user-facing `/api/live-sessions/*` or `/sessions/live/:id`. Blank/saved sessions start/continue without active live id; completed Kanban-bound runs detach live ids without ending saved conversation.
- Web session internals: `src/pbi_agent/web/session_manager.py` is thin facade over mixins in `src/pbi_agent/web/session/`; patch worker/auth globals in `pbi_agent.web.session.workers` and `pbi_agent.web.session.provider_auth`.
- Web events: app/session streams use SSE `GET /api/events/{stream}` and `/api/events/sessions/{session_id}` with `server.connected`, heartbeat, `since`/`Last-Event-ID`; WebSocket event routes/helpers removed; frontend keeps defensive `parseSseEvent()`.
- Web workspace switching/search: workspace APIs/picker use explicit workspace root/directory key across web/session/task/sub-agent/tool/extension/MCP contexts. `workspace_switched` SSE clears workspace-scoped UI/query/session state and redirects other tabs to Sessions. In WSL, picker prefers Windows dialog via `powershell.exe` and maps with `wslpath`. `/api/sessions?q=` literal-LIKE searches current-workspace titles/messages.
- Web message/timeline identity: persisted ids `msg-<messages.id>` + stable part ids; live optimistic/display-local items rekey through `message_rekeyed`. `history_items` are canonical chronological messages; frontend overlays only non-duplicate live/work items and avoids stale live streams for ended/static projections.
- Web run durability: `run_sessions` stores projection fields/status; `snapshot_json` caches timeline from persisted `web_event`; unfinished runs become `stale`; `RunTracer` attaches canonical run ids; saved-session streams replay original `seq`/`created_at`.
- Web compaction/fork/input: session compaction clears completed `previous_id`, restores persisted history, includes unanswered trailing user turns and same-turn tool exchanges. `POST /api/sessions/{session_id}/fork` duplicates through target message ids and resets usage/previous_id. Direct saved-session shell commands disable `WebDisplay` input with `begin_direct_command()`/`finish_direct_command()`; `attachLiveSession(..., preserveEventCursor)` preserves input/processing for equal/newer cursor.
- Web live/status/stale: `LiveSessionSnapshotModel` preserves typed `processing` and nullable required `pending_user_questions`; session/live API lifecycle statuses differ from persisted run-record statuses. Startup stale cleanup marks orphan active turn/sub-agent runs stale, clears processing/wait/pending, and downgrades running tool/sub-agent snapshots.
- Web API boundary: add JSON calls through generated `ApiOperationResponses`/`ApiJsonRequestBodies` via `apiRequest()` and `jsonBody()`.
- Web UI: prefer shadcn primitives/tokens, `cn()`, `lucide-react`; avoid raw colors/manual dark-mode/custom overlay z-index/space utilities. Large modals need safe gutters + inner scroll; floating UI near edges must clamp both axes when needed. Dialog/Sheet/Drawer need accessible title.
- Web forms/catalog styling: settings modals follow `ProviderModal`/`FormDialog` pattern; add dialogs use flat rows and parent scroll. Catalog panels keep standard headers/actions; tab-level notices removed; installed metadata badges live in preview dialog header. `FormDialog`/`ConfirmDialog` canonical; Badge variants include `success`/`warning`/`info`/`running`/`completed`/`failed`.
- Web CSS/fonts: unlayered reset in `webapp/src/styles/reset.css` can beat Tailwind v4 utilities; fix scoped unlayered overrides in `webapp/src/styles/overlays.css` unless wider `@layer base` move desired. UI typography uses bundled Geist, not external Google Fonts.
- Appearance: default theme is `System`, resolving to OS/browser light/dark with live preference updates; manual Prism/Light/Dark choices remain available.
- Web composer/timeline: Composer highlights `@file`/`$skill`; skill completions use `/api/skills/search`; user/assistant timelines support copy/fork; fenced code uses Shiki `CodeBlock`; `WorkingSummary` categorizes activity. Work runs coalesce thinking/tools between chat messages; stable active placeholders; expansion must not reset auto-follow; one shadcn Accordion closes other tool cards. Composer history browses main-session non-empty user messages with ArrowUp/ArrowDown and draft restore.
- Web layout/topbar: global `AppSessionsContextPanel`; sidebar head and session topbar use `--topbar-height: 48px`; collapsed sidebar 48px; New Session closes sessions sidebar. Main topbar uses interactive `UsageBar`; sub-agent/new-session routes use standalone gauge. Shortcuts: `Shift+Tab`, `Alt+Shift+H`, `Alt+Shift+E`.
- Workspace Explorer/file preview: local dependency-free React tree, Material Icon Theme assets via `webapp/src/lib/workspaceFileIcons.ts`, right split fills panel. Preview closed by default, opens from file-row clicks, has top-right `app-close-icon-button`, 72%/28% default split, no first-mount imperative resize. `CodeBlock` must flex and override global max-height; file-row clicks scroll selected file to tree viewport top.
- Workspace diff/git: backend `src/pbi_agent/web/git_files.py` uses `git status --porcelain=v1 -z --untracked-files=all`; `/api/files/tree` includes `git_status` repo metadata; `/api/files/diff` returns unified/synthetic diffs. `GitDiffResult` supports `stacked` default and `split`; Workspace Explorer exposes Diff/Raw and Split/Stacked toggles; split panes sync scroll. Git ignore rules apply even for tracked/statused paths; stdlib `.gitignore` fallback handles non-git and leading `**/`.
- Web static determinism: `workspaceFileIcons.ts` excludes byte-identical SVG duplicates and aliases canonical filenames (`angular-resolver`→`angular-guard`, `dtx`→`doctex`, `sty`→`latex-package`) to avoid unstable Rolldown asset hashes.
- Run detail/maintenance: shared `StatusPill`/Badge status styling; event rows avoid duplicate model/status chips; payload cards use `CopyShortcut`; header stats wrap safely. Startup `run_startup_maintenance()` runs once per UTC day via SQLite `maintenance_state`; retention config `maintenance.retention_days` default 30/min 1; purge preserves referenced uploads. PyPI check uses `urllib.request`; CLI update warning is Rich stderr only for newer versions.
- Sandbox/init/extensions: `pbi-agent sandbox [web|run]` uses bundled Dockerfile, matching PyPI version, shared sessions/Kanban/runs, per-workspace `/home/pbi` volume, `/tmp` writable executable tmpfs, explicit container name `pbi-agent-{normalized-workspace-folder}`. `pbi-agent init` and `/init` create starter `AGENTS.md`; `--force`/`--overwrite` replace. Extensions report setup/call failures, capture/truncate fd stdout/stderr, lock per-extension setup, require leading `/`, and dynamically reserve built-ins/project commands/MCP tools. Local test extension `.agents/extensions/hello_word.py` echoes `input["text"]`.
- `explore_workspace`: wraps `codetool-explore==0.7.0`; read group only this tool. Targets content/path/read/list; regex default true; search raw text, read/list plain/tree, image reads attachments; root arrays and unambiguous whitespace split supported; existing paths with spaces preserved; read/list single-root; root is base not containment boundary; workspace-confined; file root reads file directly.
- Test hygiene: pytest isolates web upload root to per-test `tmp_path / "web_uploads"`.

## Detailed Task Events

## 2026-06-07
- Fixed init bootstrap review finding: optional official command/agent installs now catch filesystem `OSError`s and report failed items without aborting successful `AGENTS.md` handling; added regression coverage. Validation: focused init_agents pytest, Ruff check/format check on touched files, and basedpyright on touched files passed.
- Enhanced `pbi-agent init` and `/init`: shared bootstrap now creates starter `AGENTS.md`, installs default official commands and `code-reviewer`, skips/overwrites per `--force`, reports per-item failures, refreshes provider state after interactive `/init`, and updates CLI/session docs. Validation: focused init/session/parser/web-command tests, Ruff, basedpyright, docs build, CLI help, and `git diff --check` passed; full pytest remains blocked by an unrelated `test_config_provider_and_profile_list_update_delete_endpoints` provider payload mismatch for existing `google_cloud_*` fields.
- Fixed review findings for `google_gcp`: Gemini `generateContent` endpoints strip the supported `google/` model alias prefix before URL quoting, and config metadata no longer reports native web search support for `google_gcp`. Validation: focused Google GCP/internal config pytest, Ruff check/format, basedpyright, and `git diff --check` passed.
- Fixed review Finding 1 for `google_gcp`: same-shape runtime settings now update the active shape/protocol in place so conversation history survives model/max-token/tool changes; endpoints/auth are refreshed on the next request. Validation: `uv run pytest -q --tb=short -x tests/test_google_gcp_provider.py`, Ruff check/format check, and basedpyright on touched files passed.
- Updated `google_gcp` auth: API-key-like tokens/envs use `x-goog-api-key` and Gemini express endpoints, ADC fallback uses `gcloud auth application-default print-access-token`, and cached ADC bearer tokens refresh once after expiry errors. Validation: focused Google GCP pytest, Ruff check/format, and basedpyright on touched provider/test files passed.
- Added user-facing Google GCP project/location settings: saved providers, runtime/CLI/env resolution, endpoint derivation, config API schemas, generated frontend API types, and Provider modal fields now carry Vertex project/location for non-Gemini/API-key-express shapes. Validation: focused Google GCP/internal config/API type pytest, Ruff check/format, basedpyright, `bun run typecheck`, `bun run lint`, targeted new SettingsPage test, and `git diff --check` passed; full SettingsPage test file still has an unrelated nav-order assertion mismatch.
- Fixed `google_gcp` Grok/OpenAI-compatible auth: API-key auth is now limited to Gemini express mode, while xAI/OpenAI/Anthropic Vertex shapes skip API-key-like tokens/envs and resolve OAuth2 bearer/ADC instead, with an early error if API-key auth is forced. Validation: focused Google GCP pytest, Ruff check/format, basedpyright on touched files, and `git diff --check` passed.
- Fixed Google GCP ADC timeout for session `79224097f57944a592a25c204c1e278f`: `gcloud auth application-default print-access-token` default timeout increased from 5s to 30s and can be overridden with `PBI_AGENT_GOOGLE_GCP_ACCESS_TOKEN_TIMEOUT`; local command needed ~10.5s. Validation: focused Google GCP pytest, Ruff check/format, basedpyright on touched files, and `git diff --check` passed.
- Fixed Google GCP Grok 4.1 Fast session `aa6b54288a2a4a0783d90a23f43ee61d`: route `grok-4.1-fast-*` models to Vertex OpenAI Chat Completions instead of Responses, and fail HTTP-200 Responses model-error payloads visibly. Validation: focused Google GCP pytest, related config pytest, Ruff check/format, basedpyright on touched files, and `git diff --check` passed.
- Corrected Google GCP Grok 4.1 Fast routing after current docs showed Responses API compatibility: `grok-4.1-fast-*` now defaults to Vertex OpenAI Responses again, and Google GCP Responses tool schemas drop unsupported JSON-Schema combinators like `oneOf` while preserving descriptions. Validation: focused Google GCP pytest, Ruff check/format, basedpyright on touched files, and `git diff --check` passed.
- Fixed Google GCP Gemini API-key session `75c0d167407548e8a7217180f91d545a`: Vertex express-mode Gemini `generateContent` endpoints now use `v1beta1` like `google-genai`, while standard project/location OAuth endpoints remain `v1`. Validation: focused Google GCP pytest, Ruff check/format, basedpyright on touched files, and `git diff --check` passed.
- Fixed Google GCP Gemini API-key session `3e52e7c77e9049a1a5f80a9ecd19cd0a`: `ACCESS_TOKEN_TYPE_UNSUPPORTED`/API-key-unsupported errors from Gemini express now retry once with standard Vertex OAuth/ADC using the saved project/location, then keep the bearer cached. Validation: focused Google GCP pytest, Ruff check/format, basedpyright on touched files, and `git diff --check` passed.
- Updated documentation and CLI help for the new `google_gcp`/Google Cloud Vertex AI provider: provider matrix/section, env vars, CLI flags/defaults, intro/index/settings copy, and image-input notes. Validation: `bun run docs:build`, `uv run pbi-agent -h`, Ruff check/format and basedpyright on `src/pbi_agent/cli/parser.py`, focused parser/internal-config pytest, and `git diff --check` passed.
- Hid git diff metadata/hunk headers from the session preview diff renderer and added WorkspaceFileTreePanel coverage. Validation: focused WorkspaceFileTreePanel test, `bun run lint`, `bun run typecheck`, `bun run web:build`, and `git diff --check` passed; full `bun run test:web` still fails the known unrelated SettingsPage nav-order assertion.
- Fixed session `2ea4c73c036d4f3a94826260fc8285aa` duplicate final display: Responses-style providers now persist only the last assistant output message while still rendering intermediate messages live, and SessionPage drops cached live assistant suffixes covered by a persisted combined final. Validation: focused provider pytest, targeted SessionPage test, Ruff check/format, source basedpyright, `bun run lint`, `bun run typecheck`, `bun run web:build`, and `git diff --check` passed; basedpyright on provider test files remains blocked by pre-existing test typing issues.
- Corrected session `2ea4c73c036d4f3a94826260fc8285aa` fix per user request: removed the SessionPage cached/snapshot cleanup and its test, leaving only Responses-style provider parsing so future persisted sessions store the final assistant message. Validation: focused provider pytest, Ruff check/format, source basedpyright, `bun run web:build`, and `git diff --check` passed.
- Confirmed and fixed ChatGPT Codex backend coverage for duplicate final display: Codex WebSocket parsing already uses OpenAIProvider last-message text; Codex completed-turn replay now also keeps only the final assistant message while live display still shows intermediate + final. Added regression `test_openai_chatgpt_codex_persists_and_replays_only_final_message`. Validation: focused pytest, Ruff check/format, basedpyright on touched source, and `git diff --check` passed.
- Fixed review Finding 1 for Google GCP Responses replay: client-side `history_output_items` now drops intermediate assistant messages and replays only the final assistant text while preserving live display; strengthened second-turn replay coverage. Validation: focused Google GCP pytest, Ruff check/format check, basedpyright on touched source, and `git diff --check` passed.
