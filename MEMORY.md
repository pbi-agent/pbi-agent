# MEMORY.md

## Metadata
- Last compacted: 2026-05-04
- Scope: durable repo memory + active-day task events.
- Format: only `Metadata`, `Long-Term Memory`, and `Detailed Task Events`.

## Long-Term Memory
- Memory: token-efficient; keep durable decisions/follow-ups only, not logs.
- Workflow: no commit/push/merge/PR unless asked. Use session-only `TODO.md`; markers `[ ]`, `[>]`, `[X]`, `[!]`, `[-]`. Shell from workspace root; use `python3` for direct Python shell commands.
- Validate touched surface: Python Ruff+pytest; frontend Bun tests/lint/typecheck/build; docs build. Note skipped/hung checks.
- Architecture: CLI `src/pbi_agent/__main__.py` -> `src/pbi_agent/cli.py`; default command `web`; backend FastAPI `src/pbi_agent/web/`; frontend Vite React `webapp/`.
- API changes: align routes/schemas/session manager with `webapp/src/api.ts` + `webapp/src/types.ts`.
- Web API contract: `bun run web:api-types` runs `scripts/generate_api_types.py` to regenerate `webapp/src/api-types.generated.ts` from FastAPI OpenAPI plus extra SSE models; output includes schema types, SSE event unions, `ApiOperationResponses`, and `ApiJsonRequestBodies`. Pytest `test_api_types_codegen.py` enforces generated output is current.
- Constraints: provider/tool HTTP via `urllib.request`; internal data in `~/.pbi-agent/`; no migrations/backcompat.
- Tools: built-in `python_exec`, `list_files`, `search_files` removed; use shell (`rg --files`, `find`, `rg -n`) and Python as needed.
- Project imports: top-level `skills`, `commands`, `agents`; public catalogs under `pbi-agent/{skills,commands,agents}`; local install to `.agents/...`.
- Sub-agents: `.agents/agents/*.md`; frontmatter only `name`/`description`; child model from active profile/CLI + `sub_agent_model`.
- Local commands: `plan` no questions; `review` Markdown; `fix-review` stops on `No findings.`; `ship-task` branch/merge helpers; `release` needs explicit publish wording and no history rewrite after PR unless approved.
- Release docs: `release-writing` skill governs releases; changelog index `docs/changelog/index.md`, files `docs/changelog/v<version>.md`, VitePress sidebar links releases; release retrospective added validation triage + publish verification + continuation gate safety.
- Branding: public copy = local coding agent; preserve `pbi-agent`, `pbi_agent`, `PBI_AGENT_*`, `~/.pbi-agent`; logo `src/pbi_agent/web/static/logo.jpg`.
- Web/session: saved sessions use session-scoped APIs; `/api/live-sessions/*` and `/sessions/live/:id` are removed from user-facing test expectations. Blank/saved sessions can start/continue without an active live id; completed Kanban-bound runs detach live ids without ending the saved conversation.
- Web session manager: `src/pbi_agent/web/session_manager.py` is a thin public facade for `WebSessionManager`/startup lease; implementation mixins live under `src/pbi_agent/web/session/` by utility scope. Patch worker/auth globals in `pbi_agent.web.session.workers` and `pbi_agent.web.session.provider_auth`.
- Web events: frontend app/session streams use SSE over `GET /api/events/{stream}` and `/api/events/sessions/{session_id}` with `server.connected`/heartbeat and `since`/`Last-Event-ID`; web UI WebSocket event routes/helpers are removed. SSE event payloads are generated TS unions, but the frontend stream boundary keeps defensive `parseSseEvent()` validation.
- Web message identity: saved history/live replay use canonical persisted item/message ids `msg-<messages.id>` plus stable part ids; optimistic/display-local live items are replaced by `message_rekeyed` once persisted.
- Web status contracts: session/live API models use lifecycle statuses (`idle`/`starting`/`running`/`waiting_for_input`/`ended`/`failed`/`stale`); run history/detail/all-runs use persisted run-record statuses (`started`/`completed`/`interrupted`/`failed`, etc.). Keep them distinct.
- Web live snapshots: `LiveSessionSnapshotModel` preserves typed `processing` and pending user-question payloads; frontend boundary types derive from generated models and keep `pending_user_questions` required nullable.
- Web frontend API boundary: `webapp/src/api.ts` should use generated `ApiOperationResponses`/`ApiJsonRequestBodies` via `apiRequest()` and `jsonBody()` when adding JSON API calls.
- Web durable runs: `run_sessions` has web projection fields/status, `snapshot_json` caches timeline from persisted `web_event` records, unfinished web runs become `stale` on startup, and web `RunTracer` attaches to canonical run ids.
- Web durable event replay: saved-session event streams reconstruct from persisted `web_event` records with original `seq`/`created_at`, so SSE `since` and `Last-Event-ID` cursors remain valid after restart.
- Saved-session timelines: API `history_items` are canonical chronological messages; frontend overlays only non-duplicate live/work snapshot items and avoids opening stale live streams for ended/static projections.
- Session compaction: clear completed `previous_id`; resume restores persisted user/assistant history; compaction includes unanswered trailing user turns and same-turn tool exchanges.
- Providers: OpenAI Responses use `instructions` + `previous_response_id`; ChatGPT subscription prepends system prompt; Codex transport is WebSocket-only (`chatgpt_codex_backend.py`), no unsupported compression.
- Web UI: prefer shadcn tokens/components; overlays use shared readable spacing; large modals need safe gutters + inner scroll; Radix tooltip `collisionPadding`/`sideOffset` may not protect cross-axis viewport gutters, so clamp both axes for edge-adjacent tooltips.
- Timeline: work runs coalesce thinking/tool groups; running closed by default; final assistant closes current collapsibles; file-edit results share `FILE_EDIT_TOOL_NAMES`.
- Syntax highlighting: `read_file` uses lazy Shiki code block; root `package.json` pins exact `shiki` `4.0.2`.
- v0.1.0 shipped.
- Kanban CLI: `pbi-agent kanban create` and `pbi-agent kanban list` are backed by `SessionStore`; lane/stage filters resolve existing board stages; `--json` includes image attachment metadata.
- Kanban task images: stored as task `image_attachments_json`, task create/update accepts `image_upload_ids`, uploads reuse `~/.pbi-agent/web_uploads` + `/api/uploads/{id}` previews, initial task run sends images only with first full prompt.
- Provider history restore preserves user message image attachments for image-capable providers (OpenAI/ChatGPT, Anthropic, Google) by reloading upload IDs from `~/.pbi-agent/web_uploads`.
- Web server startup: same-workspace active lease is rejected; implicit default port `8000` may auto-select a free port, explicit `--port` stays strict.
- Web UI session context gauge: compact donut uses top-level `session_usage.context_tokens` with turn fallback over runtime `compact_threshold`; tooltip is custom fixed-position with both-axis viewport clamping.
- Kanban: task cards omit technical project/session metadata and image counts; active task sessions show a Stop shortcut whose pending state is tied to the interrupt mutation.
- Saved sessions: sidebar supports inline title edit via `PATCH /api/sessions/{session_id}` with cache/app-event invalidation.
- Interactive ask-user: Web per-message `interactive_mode` exposes `ask_user`; pending questions persist in snapshots and are answered via session-scoped question-response endpoints; topbar uses shadcn `Toggle` and `UserQuestionsPanel`.
- UserQuestionsPanel UX: one question at a time with shadcn Card, selected filled Button state, keyboard nav, auto-focus active option, Prev/Next + dots, and custom textarea retained in fieldset.
- Ask-user notifications: frontend-only localStorage prefs for desktop/sound; AppShell effect fires hidden/unfocused browser notifications/sound, deduped by live/session + prompt id, click focuses/navigates to waiting session.
- Web production readiness hardening completed through Phase 8: documented sync/lifecycle invariants, durable SSE replay from persisted `web_event`, replay gap recovery, lifecycle ownership split, stale web-run startup handling, reducer/routing gap recovery, strict generated SSE/API contracts, long-stream replay coverage, refresh/reconnect regressions, ask-user/image restore, recovery states/logging/debug hooks, and fatal/interrupted surfacing. Release docs and manual browser smoke remain pending until explicit workflow/served UI.
- Saved-session timeline refresh must preserve persisted work-item anchors: merge canonical `history_items` with snapshot messages in order, keeping `thinking`/`tool_group` items between their original surrounding messages so prior turns retain separate `Working` blocks.

## Detailed Task Events
## 2026-05-05
- Fixed task PATCH/move into an `auto_start` Kanban stage by comparing against the pre-move stage; added backend regression for moved task auto-start completion. Validation: focused task tests and Ruff check/format-check on touched Python passed.
- Guarded SSE `EventStream.publish()` against closed subscriber loops by removing stale subscribers on `RuntimeError`; added backend regression for closed-loop publish. Validation: focused test, Ruff check/format-check on touched Python, and `git diff --check` passed.
- Fixed shutdown lease finalization so live-session worker threads keep the workspace lease active after shutdown join timeout, then release it when the live worker reaches idle/finalization; added non-cooperative live worker regression while preserving task-worker shutdown coverage. Validation: focused `tests/test_web_serve.py::test_shutdown_keeps_lease_until_live_worker_finishes` + task-worker regression, Ruff check/format-check on touched Python, and `git diff --check` passed.
- Fixed root web manager access for Kanban task sessions with subdirectory `project_dir`: task-created saved sessions now use root directory ownership while live/run records retain `project_dir` for execution context; added backend regression for root session detail + saved-session event replay. Validation: focused `tests/test_web_serve.py::test_run_task_precreated_session_is_accessible_from_root_manager`, Ruff check/format-check on touched Python, and `git diff --check` passed.
- Fixed session timeline Working fragmentation for interleaved concurrent sub-agents: frontend now groups work by message-boundary turns, shows collapsed header context (`Working · Researcher` / `Working · 3 agents`), and passes per-item sub-agent metadata inside expanded work. Validation: focused `SessionTimeline`, full `bun run test:web -- --maxWorkers=2`, `bun run typecheck`, `bun run lint`, `bun run web:build`, and `git diff --check` passed.
- Fixed refreshed saved-session timeline merging so duplicate canonical messages are replaced in-place while surrounding `thinking`/`tool_group` items remain anchored to their original turns; added multi-turn SessionPage regression and updated ordering expectations. Validation: `bun run test:web -- SessionPage`, full `bun run test:web -- --maxWorkers=2`, `bun run typecheck`, `bun run lint`, `bun run web:build`, and `git diff --check` passed.
- Fixed missing prior-run `Working` blocks for saved session refresh: `get_session_detail()` now aggregates all persisted web-session snapshots in chronological order instead of returning only the latest web run, namespacing non-message item ids per run; frontend merge skips replayed persisted message ids while preserving work anchors. Verified session `117d99e477064c8d8bdd4e13fc6e5d70` now combines 4 web runs into 41 items with prior `thinking`/`tool_group` blocks. Validation: focused aggregate backend test, full `tests/test_web_serve.py`, full `bun run test:web -- --maxWorkers=2`, `bun run typecheck`, `bun run lint`, Ruff check/format-check on touched Python, `bun run web:build`, and `git diff --check` passed.
- Fixed dormant saved-session profile switching after restart: the UI enables the profile selector whenever the composer can start a future turn, and `PUT /api/sessions/{session_id}/profile` updates the persisted session runtime without creating a live run; live bound profile changes also persist. Validation: focused backend/profile and SessionPage tests, full `tests/test_web_serve.py`, full `bun run test:web -- --maxWorkers=2`, `bun run typecheck`, `bun run lint`, Ruff check/format-check, and `bun run web:build` passed.
- Fixed out-of-order historical `Working` blocks when aggregated run snapshots replay duplicate same-content messages: frontend message matching now never falls back from a known persisted `messageId` to content signature after that id was consumed, preventing an older duplicate assistant message from consuming a later same-content assistant. Reproduced from session `665d83dcabf24e00bdd2da8ad2b8f615`. Validation: focused `SessionPage`, full `bun run test:web -- --maxWorkers=2`, `bun run typecheck`, `bun run lint`, `bun run web:build`, and `git diff --check` passed.
- Split oversized web session manager into utility-scoped `pbi_agent.web.session` modules (`state`, `serializers`, `catalogs`, `saved_sessions`, `live_sessions`, `events`, `tasks`, `workers`, `configuration`, `provider_auth`) with `session_manager.py` as facade; updated test monkeypatch paths to new worker/auth modules. Validation: worker block parity scripts passed before migration; `uv run ruff check .`, `uv run ruff format --check .`, `uv run pytest -q --tb=short -x tests/test_web_serve.py`, and `git diff --check` passed. Full `uv run pytest -q --tb=short -x` first failed from ambient `PBI_AGENT_REASONING_EFFORT=low` in Google defaults; rerun with that env unset reached an order-dependent web test failure that passes isolated.
- Updated saved-session timeline collapsed work headers to put context first (`Researcher · Working` / `3 agents · Working`) while keeping no-agent label as `Working`; rebuilt static web assets. Validation: focused `SessionTimeline`, `bun run typecheck`, `bun run lint`, `bun run web:build`, and `git diff --check` passed.
- Fixed review finding: work-run collapsed header now renders the `·` separator visibly between agent context and `Working`, and tests assert visible text instead of only accessible name. Validation: focused `SessionTimeline`, `bun run typecheck`, `bun run lint`, `bun run web:build`, and `git diff --check` passed.
- Fixed frontend SSE snapshot recovery so stream resets can preserve the active `liveSessionId`/index during recovery, keeping store-driven hooks mounted and reconnecting from cursor zero after `server.replay_incomplete`. Validation: focused `bun run test:web -- useLiveSessionEvents store`, `bun run typecheck`, and `git diff --check` passed.
- Fixed startup stale marking for unbound web-session run rows: `SessionStore.mark_web_runs_stale()` now also marks active `session_id IS NULL` web runs stale when metadata has `source=web` and matching `directory`, while preserving other-directory unbound runs. Validation: focused stale startup backend test, Ruff check/format-check on touched Python, and `git diff --check` passed.
- Bounded SSE subscriber queues to retained history size and made slow-subscriber overflow drop oldest queued live events without crashing event-loop callbacks; replay history remains preserved for reconnect. Validation: focused overflow regression, neighboring SSE event-stream tests, and Ruff check/format-check on touched Python passed.
- Fixed no-ID session timeline merge ambiguity by allowing content-signature matching only when unique in both saved history and snapshot; repeated same-content live messages remain visible with their work anchors. Validation: focused `bun run test:web -- SessionPage`, `bun run typecheck`, and `git diff --check` passed.
- Fixed saved-session profile switching so existing saved sessions use only `setSessionProfile()` and no longer mutate the global active profile; blank `/sessions` selector still updates the active default. Validation: focused `bun run test:web -- SessionPage`, `bun run typecheck`, and `git diff --check` passed.
- Completed final review-finding hardening pass and rebuilt static web assets. Validation: `uv run ruff check .`, `uv run ruff format --check .`, `uv run pytest -q --tb=short -x`, `bun run test:web -- --maxWorkers=2`, `bun run lint`, `bun run typecheck`, `bun run web:build`, and `git diff --check` passed.
- Fixed SSE slow-subscriber overflow so bounded queues enqueue `server.replay_incomplete` and the SSE iterator sends it then disconnects instead of silently crossing seq gaps; replay history/persistence remains unchanged. Validation: focused SSE overflow/replay tests and Ruff check/format-check on touched Python passed.
- Fixed active saved-session timeline merging so no-id live/current snapshot messages no longer content-match older history; explicit historical overlays and persisted `messageId` matches still merge, and current no-id messages anchor after canonical history. Validation: focused `bun run test:web -- SessionPage`, `bun run typecheck`, and `git diff --check` passed.
- Fixed web manager worker creation gates so new live sessions and task runs reject once shutdown is requested or the manager is not started, while existing live-session reuse/read APIs remain unchanged. Validation: focused shutdown rejection tests plus Ruff check/format-check on touched Python passed.
- Hardened unbound web stale-run cleanup for malformed/missing metadata: `mark_web_runs_stale()` now falls back to exact normalized `project_dir` only for unbound `web_session` rows, preserving other-directory rows. Validation: focused stale startup backend test plus Ruff check/format-check on touched Python passed.
- Fixed terminal task/live-session ordering so completed/failed/interrupted task updates are not visible before the bound live-session terminal projection is persisted, and saved-session continuations do not reuse terminal task live sessions. Validation: focused continuation/interruption tests and full `tests/test_web_serve.py` passed.
- Completed final production-readiness hardening validation after review findings. Validation: `uv run ruff check .`, `uv run ruff format --check .`, `uv run pytest -q --tb=short -x`, `bun run test:web -- --maxWorkers=2`, `bun run lint`, `bun run typecheck`, `bun run web:build`, and `git diff --check` passed.
- Verified final rebuilt static web assets are tracked/staged: `bun run web:build` regenerated hashed chunks, `git add -A -- src/pbi_agent/web/static/app` staged new assets plus old deletions, and `git ls-files --stage` confirmed referenced chunks are in the index. No code changes.
- Fixed `run_task()` shutdown setup race: shutdown finalization now treats `_running_task_ids` as active setup/running work, and task worker registration refuses to start after the manager is no longer started; added regression for shutdown during task setup preserving the lease and registering the worker. Validation: focused shutdown/task tests plus Ruff check/format-check on touched Python passed.
- Fixed `run_task()` setup-failure cleanup: a broad pre-worker-registration guard now discards `_running_task_ids` on runtime/prompt/projection setup exceptions, while registered workers retain cleanup ownership; added regression that fails runtime resolution then reruns the same task. Validation: focused task setup tests, Ruff check/format-check on touched Python, and `git diff --check` passed.
- Fixed SSE slow-subscriber overflow cursor semantics: bounded queues enqueue an internal overflow marker, and `_iter_sse_events()` emits public `server.replay_incomplete` with current `last_sent_seq` cursor fields, latest seq, oldest available seq, and SSE id for Last-Event-ID recovery. Validation: focused/neighboring SSE backend tests, Ruff check/format-check on touched Python, and `git diff --check` passed.
- Fixed frontend SSE replay parser to validate optional `oldest_available_seq` on `server.replay_incomplete`, accepting absent/null/integer and rejecting malformed values; added event parser tests for subscriber queue overflow replay-incomplete. Validation: focused `bun run test:web -- events`, `bun run typecheck`, and `git diff --check` passed.
- Completed final follow-up validation and rebuilt/staged static app assets. Validation: `uv run ruff check .`, `uv run ruff format --check .`, `uv run pytest -q --tb=short -x`, `bun run test:web -- --maxWorkers=2`, `bun run lint`, `bun run typecheck`, `bun run web:build`, and `git diff --check` passed.
- Fixed `run_task()` startup-failure cleanup after persisted running state: failures before successful `worker.start()` now mark the task failed and finalize any created task live-session projection as failed without taking over worker-owned cleanup after start. Validation: focused task startup-failure tests, `tests/test_web_serve.py -k run_task`, and Ruff check/format-check on touched Python passed.
- Hardened shutdown for non-cooperative web workers: after the join timeout, live/task projections are persisted as interrupted/failed, active snapshots are cleared, and manager lease renewal/release no longer waits on stuck worker threads; late worker completion cannot overwrite interrupted state. Validation: focused shutdown tests and Ruff check/format-check on touched Python passed.
- Fixed cross-target frontend SSE recovery: replay-incomplete/sequence-gap recovery only closes/reconnects the current hook stream for same-session targets; cross-target recovery resets/invalidates the target and leaves it disconnected for refetch/target hook ownership. Validation: focused `bun run test:web -- useLiveSessionEvents`, `bun run typecheck`, and `git diff --check` passed.
- Completed final production-readiness validation after lifecycle/SSE/task hardening and rebuilt static web assets. Validation: `uv run ruff check .`, `uv run ruff format --check .`, `uv run pytest -q --tb=short -x`, `bun run test:web -- --maxWorkers=2`, `bun run lint`, `bun run typecheck`, `bun run web:build`, and `git diff --check` passed.
- Closed final production-readiness review findings: app SSE recovery resets/invalidation, shutdown lease waits for real worker stop, late terminal events are ignored, live worker start failure finalizes failed projections, running task PATCH is rejected, saved-session SSE cursors include run identity, stale saved-session live ids detach on refresh, static app serving has smoke coverage, and v0.2.0 release metadata/changelog is prepared. Validation: `uv run ruff check .`, `uv run ruff format --check .`, `uv run pytest -q --tb=short -x`, `bun run test:web -- --maxWorkers=2`, `bun run lint`, `bun run typecheck`, `bun run web:build`, `bun run docs:build`, and `git diff --check` passed.
- Fixed final orchestrated production-readiness findings: task setup now refuses worker registration after shutdown request, backlog auto-stage setup failures mark/publish failed state, saved-session hydration clears stale live indexes and stale fallback events cannot reattach old live ids, `user_questions_requested` SSE payloads are validated, and CI/release workflows now gate Ruff/pytest/frontend/docs/generated/static checks with changelog-backed release notes. Validation: `uv run ruff check .`, `uv run ruff format --check .`, `uv run pytest -q --tb=short -x`, `bun run test:web -- --maxWorkers=2`, `bun run lint`, `bun run typecheck`, `bun run web:api-types`, `bun run web:build`, `bun run docs:build`, and `git diff --check` passed.
- Fixed last production-readiness review findings: active saved-session deletion now rejects bound live workers, publish/release workflows verify release target SHA before publishing, web run projections can clear persisted `session_id`, stale saved-session events with old live ids are rejected, saved-session hydration clears pending ask-user state, and `message_rekeyed` SSE payloads are strictly validated. Validation: `uv run ruff check .`, `uv run ruff format --check .`, `uv run pytest -q --tb=short -x` (rerun passed after one transient web test), `bun run test:web -- --maxWorkers=2`, `bun run lint`, `bun run typecheck`, `bun run web:api-types`, `bun run web:build`, `bun run docs:build`, and `git diff --check` passed.
- Fixed final orchestrated production-readiness blockers: lost web-manager lease now triggers shutdown/finalization, task update/delete reserve task ids against start races, live-session events serialize snapshot/persistence and deliver to SSE only after persistence with rollback on failure, release workflows require changelog notes and fail existing PyPI artifact collisions, and release docs keep manual-smoke signoff explicit with changelog traceability. Validation: `uv run ruff check .`, `uv run ruff format --check .`, `uv run pytest -q --tb=short -x`, `bun run test:web -- --maxWorkers=2`, `bun run lint`, `bun run typecheck`, `bun run web:build`, `bun run docs:build`, and `git diff --check` passed.
- Fixed latest production-readiness findings excluding manual smoke: live web-event insert + run snapshot/seq update now commit atomically, saved-session deletion is locked against live creation and task-start setup, release publishing moved into `release.yml` with unchanged-version skip plus release/tag/PyPI collision guards before PyPI then GitHub release, and app SSE handles `session_created` plus sequence-gap snapshot recovery. Validation: `uv run ruff check .`, `uv run ruff format --check .`, `uv run pytest -q --tb=short -x` (rerun passed after one transient task timing failure), `bun run test:web -- --maxWorkers=2`, `bun run lint`, `bun run typecheck`, `bun run web:build`, `bun run docs:build`, and `git diff --check` passed.
- Fixed latest production-readiness blockers: terminal live-session finalization rolls back on persistence failure and no longer fails completed tasks, partial task-start retry avoids duplicate prompts and startup fails running tasks without live runs, shutdown holds the workspace lease while non-cooperative worker threads remain alive, live SSE recovery keeps state/retries after snapshot invalidation failure, and release workflow uses draft GitHub releases for PyPI/GitHub partial recovery while manual smoke remains a human gate outside CI. Validation: `uv run ruff check .`, `uv run ruff format --check .`, `uv run pytest -q --tb=short -x`, `bun run test:web -- --maxWorkers=2`, `bun run lint`, `bun run typecheck`, `bun run web:build`, `bun run docs:build`, and `git diff --check` passed. Follow-up tweak removed CI enforcement of manual smoke/signoff from `release.yml`; focused `tests/test_workflows.py` and Ruff checks passed.
- Fixed regression from strict SSE parsing: frontend now accepts/validates the existing `welcome` event and generated SSE unions include `WelcomeSseEventModel`, preventing new saved-session streams from dropping seq 5, detecting a false sequence gap, and reconnecting forever from `since=0`. Validation: focused `useLiveSessionEvents`/`events` tests, `bun run typecheck`, `bun run lint`, `bun run web:build`, API type codegen test, Ruff check/format-check on touched schema, and `git diff --check` passed.
