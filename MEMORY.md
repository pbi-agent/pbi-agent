# MEMORY.md

## Metadata
- Last compacted: 2026-05-07
- Scope: durable repo memory + active-day task events.
- Format: only `Metadata`, `Long-Term Memory`, and `Detailed Task Events`.

## Long-Term Memory
- Memory: token-efficient; keep durable decisions/follow-ups only, not logs.
- Workflow: no commit/push/merge/PR unless asked. Use session-only `TODO.md`; markers `[ ]`, `[>]`, `[X]`, `[!]`, `[-]`. Shell from workspace root; use `python3` for direct Python shell commands.
- Validate touched surface: Python Ruff+basedpyright+pytest; frontend Bun tests/lint/typecheck/build; docs build. Note skipped/hung checks.
- Architecture: CLI `src/pbi_agent/__main__.py` -> `src/pbi_agent/cli.py`; default command `web`; backend FastAPI `src/pbi_agent/web/`; frontend Vite React `webapp/`.
- API changes: align routes/schemas/session manager with `webapp/src/api.ts` + `webapp/src/types.ts`.
- Web API contract: `bun run web:api-types` runs `scripts/generate_api_types.py` to regenerate `webapp/src/api-types.generated.ts` from FastAPI OpenAPI plus extra SSE models; output includes schema types, SSE event unions, `ApiOperationResponses`, and `ApiJsonRequestBodies`. Pytest `test_api_types_codegen.py` enforces generated output is current.
- Constraints: provider/tool HTTP via `urllib.request`; internal data in `~/.pbi-agent/`; no migrations/backcompat.
- Tools: built-in `python_exec`, `list_files`, `search_files` removed; use shell (`rg --files`, `find`, `rg -n`) and Python as needed.
- Project imports: top-level `skills`, `commands`, `agents`; public catalogs under `pbi-agent/{skills,commands,agents}`; local install to `.agents/...`.
- Sub-agents: `.agents/agents/*.md`; frontmatter only `name`/`description`; child model from active profile/CLI + `sub_agent_model`.
- Local commands: `plan` no questions; `review` Markdown; `fix-review` stops on `No findings.`; `ship-task` branch/merge helpers; `release` needs explicit publish wording and no history rewrite after PR unless approved.
- Release docs: `release-writing` skill governs releases; changelog index `docs/changelog/index.md`, files `docs/changelog/v<version>.md`, and VitePress sidebar links releases; release retrospective added validation triage + publish verification + continuation gate safety.
- Branding: public copy = local coding agent; preserve `pbi-agent`, `pbi_agent`, `PBI_AGENT_*`, `~/.pbi-agent`; logo `src/pbi_agent/web/static/logo.jpg`.
- Web/session: saved sessions use session-scoped APIs; `/api/live-sessions/*` and `/sessions/live/:id` are removed from user-facing test expectations. Blank/saved sessions can start/continue without an active live id; completed Kanban-bound runs detach live ids without ending the saved conversation.
- Web session manager: `src/pbi_agent/web/session_manager.py` is a thin public facade for `WebSessionManager`/startup lease; implementation mixins live under `src/pbi_agent/web/session/` by utility scope. Patch worker/auth globals in `pbi_agent.web.session.workers` and `pbi_agent.web.session.provider_auth`.
- Web events: frontend app/session streams use SSE over `GET /api/events/{stream}` and `/api/events/sessions/{session_id}` with `server.connected`/heartbeat and `since`/`Last-Event-ID`; web UI WebSocket event routes/helpers are removed. SSE event payloads are generated TS unions, but the frontend stream boundary keeps defensive `parseSseEvent()` validation.
- Web message identity: saved history/live replay use canonical persisted item/message ids `msg-<messages.id>` plus stable part ids; optimistic/display-local live items are replaced by `message_rekeyed` once persisted.
- Web status contracts: session/live API models use lifecycle statuses (`idle`/`starting`/`running`/`waiting_for_input`/`ended`/`failed`/`stale`); run history/detail/all-runs use persisted run-record statuses (`started`/`completed`/`interrupted`/`failed`, etc.). Keep them distinct.
- Web live snapshots: `LiveSessionSnapshotModel` preserves typed `processing` and pending user-question payloads; frontend boundary types derive from generated models and keep `pending_user_questions` required nullable.
- Web frontend API boundary: `webapp/src/api.ts` should use generated `ApiOperationResponses`/`ApiJsonRequestBodies` via `apiRequest()` and `jsonBody()` when adding JSON API calls.
- Web durable runs: `run_sessions` has web projection fields/status, `snapshot_json` caches timeline from persisted `web_event` records, unfinished web runs become `stale` on startup, and web `RunTracer` attaches to canonical run ids.
- Web durable event replay: saved-session event streams reconstruct from persisted `web_event` records with original `seq`/`created_at`, so SSE `since` and `Last-Event-ID` cursors remain valid after restart.
- Saved-session timelines: API `history_items` are canonical chronological messages; frontend overlays only non-duplicate live/work snapshot items and avoids opening stale live streams for ended/static projections.
- Session compaction: clear completed `previous_id`; resume restores persisted user/assistant history; compaction includes unanswered trailing user turns and same-turn tool exchanges.
- Providers: OpenAI Responses use `instructions` + `previous_response_id`; ChatGPT subscription prepends system prompt; Codex transport is WebSocket-only (`chatgpt_codex_backend.py`), no unsupported compression.
- Web UI: prefer shadcn tokens/components; overlays use shared readable spacing; large modals need safe gutters + inner scroll; Radix tooltip `collisionPadding`/`sideOffset` may not protect cross-axis viewport gutters, so clamp both axes for edge-adjacent tooltips.
- Timeline: work runs coalesce thinking/tool groups; running closed by default; final assistant closes current collapsibles; file-edit results share `FILE_EDIT_TOOL_NAMES`.
- Web Working UI: OpenCode-inspired nested grouping uses collapsed Working summaries with animated counts, simple Thinking/Activity rows, friendly one-word tool labels, minimal sub-agent cards, read-only hidden child routes with back link, completed-run sub-agent id namespacing, and child processing suppressed after final child assistant response. Active Working label uses text shimmer instead of spinner.
- Syntax highlighting: `read_file` uses lazy Shiki code block; root `package.json` pins exact `shiki` `4.0.2`.
- v0.1.0 shipped.
- Kanban CLI: `pbi-agent kanban create` and `pbi-agent kanban list` are backed by `SessionStore`; lane/stage filters resolve existing board stages; `--json` includes image attachment metadata.
- Kanban task images: stored as task `image_attachments_json`, task create/update accepts `image_upload_ids`, uploads reuse `~/.pbi-agent/web_uploads` + `/api/uploads/{id}` previews, initial task run sends images only with first full prompt.
- Provider history restore preserves user message image attachments for image-capable providers (OpenAI/ChatGPT, Anthropic, Google) by reloading upload IDs from `~/.pbi-agent/web_uploads`.
- Web server startup: same-workspace active lease is rejected; implicit default port `8000` may auto-select a free port, explicit `--port` stays strict.
- Web UI session context gauge: compact donut uses top-level `session_usage.context_tokens` with turn fallback over runtime `compact_threshold`; tooltip is custom fixed-position with both-axis viewport clamping.
- Kanban: task cards omit technical project/session metadata and image counts; active task sessions show a Stop shortcut whose pending state is tied to the interrupt mutation.
- Saved sessions: sidebar supports inline title edit via `PATCH /api/sessions/{session_id}` with cache/app-event invalidation.
- Interactive ask-user: Web per-message `interactive_mode` exposes `ask_user`; pending questions persist in snapshots and are answered via session-scoped question-response endpoints; topbar uses shadcn `Toggle` and `UserQuestionsPanel`.
- UserQuestionsPanel UX: one question at a time with shadcn Card, selected filled Button state, keyboard nav, auto-focus active option, Prev/Next + dots, and custom textarea retained in fieldset.
- Ask-user notifications: frontend-only localStorage prefs for desktop/sound; AppShell effect fires hidden/unfocused browser notifications/sound, deduped by live/session + prompt id, click focuses/navigates to waiting session.
- Web production readiness hardening completed through Phase 8: documented sync/lifecycle invariants, durable SSE replay from persisted `web_event`, replay gap recovery, lifecycle ownership split, stale web-run startup handling, reducer/routing gap recovery, strict generated SSE/API contracts, long-stream replay coverage, refresh/reconnect regressions, ask-user/image restore, recovery states/logging/debug hooks, and fatal/interrupted surfacing. Release docs and manual browser smoke remain pending until explicit workflow/served UI.
- Saved-session timeline refresh must preserve persisted work-item anchors: merge canonical `history_items` with snapshot messages in order, keeping `thinking`/`tool_group` items between their original surrounding messages so prior turns retain separate `Working` blocks.
- Web lifecycle hardening: startup/shutdown/task setup races, stale unbound web runs, SSE persistence/cursors/recovery, saved-session deletion/live creation locks, and release workflow collision guards were production-readiness validated before v0.2.0 prep.
- Saved-session timelines: aggregate persisted web-run snapshots chronologically, namespace non-message work ids per run, preserve `Working` anchors around duplicate canonical messages, and avoid content-signature fallback once a persisted `messageId` is consumed.
- Timeline UI: work-run headers show context first (`Researcher · Working` / `3 agents · Working`); expanded Working uses strict nested grouping (summary counts → compact tool rows → one detail card). Main-session sub-agent output is collapsed to one status card per sub-agent id; clicking opens `/sessions/:parent/sub-agents/:id`, a hidden read-only child view filtered from parent timeline with no composer/interrupt/questions and a back link.
- Settings UI: opencode-inspired two-pane settings surface split into modular sections; command cards show compact metadata and open markdown previews through shared `MarkdownContent`; dialog close buttons use shared `app-close-icon-button`.
- Test hygiene: pytest isolates web upload root to per-test `tmp_path / "web_uploads"` to prevent fixture uploads leaking into `~/.pbi-agent/web_uploads`.
- Sandbox: `pbi-agent sandbox [web|run]` builds a bundled package Dockerfile from `src/pbi_agent/sandbox/Dockerfile.sandbox`, installs matching `pbi-agent==${PBI_AGENT_VERSION}` from PyPI, ignores workspace Dockerfiles, uses versioned default image tags, per-workspace hashed container mount/home volume, host-side browser open for web, always-created host `~/.pbi-agent` bind mount, and an Alpine runtime image with minimal `apk` packages (`bash`, `ca-certificates`, `curl`, `git`, `libstdc++`, `patch`, `ripgrep`, `unzip`) plus site-packages cleanup. Sandbox passes host workspace identity/display envs while tools execute in the container cwd, so sessions/Kanban/runs are shared with non-sandbox. User-local sandbox installs persist per project via writable `/home/pbi` Docker volume; Docker PATH stays generic with `/home/pbi/.local/bin`, while shell-tool commands source the sandbox bootstrap to load user profiles, discover `$HOME/**/bin`, and refresh discovery on missing commands; `/home/pbi/.cache` remains tmpfs. Validate with `uv run pytest -q --tb=short -x tests/test_cli.py -k sandbox`, Ruff, docs build, `uv build`, and Docker build/smoke when available.

## Detailed Task Events
## 2026-05-07
- Fixed web session startup Working flicker by keeping the active no-anchor WorkRun on a stable placeholder key and showing a muted `Preparing…` summary until counts exist; active empty connected sessions now show Working instead of Welcome. Validation: `bun run test:web -- SessionTimeline`, `bun run lint`, `bun run typecheck`, `bun run web:build`, `bun run test:web` (first full run had a transient `SessionWelcome` timeout, rerun passed), and `git diff --check` passed.
- Slowed the active web Working `TextShimmer` wave by increasing shimmer duration and rebuilt static web assets. Validation: `bun run test:web -- SessionTimeline`, `bun run lint`, `bun run typecheck`, `bun run web:build`, and `git diff --check` passed.
- Simplified expanded web Working groups by removing the intermediate Activity/In motion collapsible level; Thinking, tool rows, and sub-agent cards now share the first expanded level while tool rows still expand to detail. Validation: `bun run test:web -- SessionTimeline`, `bun run lint`, `bun run typecheck`, `bun run web:build`, and `git diff --check` passed.
- Added `.agents/commands/commit.md` to standardize local-only commit workflow with explicit staging, validation freshness, and stop conditions. Validation: `git diff --check` passed.
- Raised the sub-agent provider request cap default from 100 to 200 in `src/pbi_agent/agent/session.py` and updated `AGENTS.md`. Validation: `uv run pytest -q --tb=short -x tests/test_cli.py` passed; attempted `tests/test_sub_agent.py` but no such file exists.
- Hid generic web session timeline retry messages matching `Retrying... (n/n)` for notice/error roles while preserving contextual retry notices and real errors; rebuilt static web assets. Validation: `bun run test:web -- SessionTimeline`, `bun run lint`, `bun run typecheck`, `bun run web:build`, `bun run test:web` (first full run had a transient `BoardStageEditorModal` timeout, targeted rerun and full rerun passed) passed.
- Removed the full-width outer background around the interactive `UserQuestionsPanel` while keeping the ask-question card background intact; rebuilt static web assets. Validation: `bun run test:web -- UserQuestionsPanel`, `bun run lint`, `bun run typecheck`, `bun run web:build`, and `git diff --check` passed.
- Fixed review finding by staging the rebuilt static app asset renames referenced by `src/pbi_agent/web/static/app/index.html`, leaving no untracked generated assets from the web build. Validation: `bun run web:build`, `git diff --check`, and `git diff --cached --check` passed.
- Fixed image-attached user message timeline scroll by bottom-aligning image messages only when needed without decreasing `scrollTop`, while preserving text-only top alignment; rebuilt static web assets. Validation: `bun run test:web -- SessionTimeline`, `bun run lint`, `bun run typecheck`, `bun run web:build`, `bun run test:web` (passed with existing React act warnings in Settings tests), and `git diff --check` passed.
- Fixed review finding by staging the rebuilt static app assets referenced by `index.html`, so the packaged SPA entrypoint no longer points at untracked bundles. Validation: `bun run web:build`, `git diff --check`, and `git diff --cached --check` passed.
- Diagnosed session `367664d3e6674fcd9c1120f5d7b40983` last failure: run `fb8f9d0053044eefb2bd290ccec68efb` ended on raw ChatGPT Codex WebSocket `SSLEOFError` after a long tool call; no retry happened because send-time socket errors are not wrapped as `ChatGPTCodexWebSocketError(retryable=True)`. Validation: sqlite observability queries and code inspection only.
- Fixed ChatGPT Codex WebSocket send-side EOF retry handling by wrapping request send/PONG send `OSError`/timeouts as retryable `ChatGPTCodexWebSocketError` and added SSL EOF regression coverage. Validation: `uv run pytest -q --tb=short -x tests/test_openai_provider.py`, `uv run ruff check .`, `uv run ruff format --check .`, and `git diff --check` passed.
- Diagnosed recurring `PBI_AGENT_REASONING_EFFORT=low` pytest contamination: no workspace `.env`; shell tools inherit pbi-agent process env synthesized by CLI web `_temporary_env_overrides(_settings_env(runtime.settings))`, while config resolution prioritizes `PBI_AGENT_*` env over saved profiles. Current saved active profile is `worker` (`medium`), so stale/injected process env requires restarting web/agent or avoiding global env injection.
- Removed `pbi-agent web` runtime env synthesis so `server.serve()` no longer creates/stales `PBI_AGENT_*` in process env; real parent env vars still work and current running agents need restart to clear already-inherited values. Validation: `tests/test_cli.py::DefaultWebCommandTests`, sanitized Google defaults test, Ruff, and `git diff --check` passed.
- Re-ran unsanitized full pytest in the still-running agent process after removing web env synthesis; it still failed at `test_resolve_settings_uses_google_defaults` because the current process already inherited `PBI_AGENT_REASONING_EFFORT=low`. Targeted test passes when unsetting that variable, confirming a restart/clean parent env is needed for the structural fix to take effect. Validation: `uv run pytest -q --tb=short -x` failed at the expected assertion; `env | grep '^PBI_AGENT_'` confirmed stale env; sanitized targeted pytest passed.
- After restarting the server, verified the structural env fix end-to-end: current tool env has no `PBI_AGENT_*` variables, and unsanitized `uv run pytest -q --tb=short -x` passed fully without the reasoning-effort default failure.
- Added Kanban task titles to successful finished browser notifications by passing bootstrap tasks into `SessionEndedNotificationEffects`, using task-title bodies for task live sessions, preserving generic fallback, and rebuilding static web assets. Validation: `bun run test:web -- SessionEndedNotificationEffects`, `bun run typecheck`, `bun run lint`, `bun run web:build`, and `git diff --check` passed.
- Shipped remaining ChatGPT Codex WebSocket PONG send regression coverage in PR #265; tests verify SSL EOF during ping response send is retryable and closes the socket. Validation: full Python suite, full web suite/lint/typecheck/build, Ruff, dead-code, and GitHub `test` check passed.- Reconciled local `master` divergence by rebasing local-only memory commit onto `origin/master`; `git pull --tags origin master` now reports up to date while local `master` remains ahead by one local-only commit.
- Optimized sandbox image: switched bundled Dockerfile to `python:3.12.12-alpine3.22`, minimal `apk` packages, dynamic site-packages cleanup, and PyArrow dev artifact removal; benchmark kept best option at 560 MB vs 887 MB baseline (Debian cleanup was 735 MB, Alpine untrimmed 567 MB). Validation: Docker build/import/CLI smoke, focused sandbox pytest, Ruff check/format-check, docs build, `uv build`, and wheel/sdist Dockerfile inspection passed.
- Added sandbox web browser launch grace period with Rich spinner status: `pbi-agent sandbox web` now waits for the host port, keeps showing a console spinner, then pauses briefly before opening the host browser; default `pbi-agent web` behavior remains unchanged. Validation: `python3 -m compileall -q src/pbi_agent/cli.py tests/test_cli.py` passed; pytest unavailable (`uv` missing, `pytest` module not installed).
- Changed default local/sandbox web port from common dev port 8000 to uncommon unprivileged port 7424, shared via `pbi_agent.web.defaults.DEFAULT_WEB_PORT`, and updated CLI help, sandbox image EXPOSE, docs, and CLI tests. Validation: compileall, focused unittest port/sandbox tests, and `git diff --check` passed; Ruff/docs build unavailable (`uv`/`ruff`/`bun` missing); one broad unittest remains environment/profile-dependent (`chatgpt` saved profile vs expected `openai`).

## 2026-05-07
- Prepared release v0.3.0 on branch `chore/release-v0.3.0`: bumped `pyproject.toml`/`uv.lock`, added `docs/changelog/v0.3.0.md`, updated changelog index/sidebar, committed `a0ee421`, pushed, and opened PR #269. Validation: Ruff check/format-check, dead-code, full pytest, docs build passed; PR check queued at handoff. Next: merge/publish only with explicit approval.
- Published release v0.3.0 after approval: reran failed PR CI (transient `test_interrupted_task_run_session_can_be_manually_continued`) to success, merged PR #269 to `master` at `a8e7b71`, and verified Release workflow run `25497350364` succeeded, including PyPI publish and GitHub Release publish. Release URL: https://github.com/pbi-agent/pbi-agent/releases/tag/v0.3.0.
- Fixed GitHub Release v0.3.0 description by removing the changelog frontmatter that appeared at the beginning and adding the `Full changelog: docs/changelog/v0.3.0.md` footer. Validation: `gh release view v0.3.0 --json body,url` confirmed the body now starts with `# v0.3.0`.
- Applied the release malformed-body retrospective: updated `release-writing` skill with GitHub Release Notes Body criteria and changed `.github/workflows/release.yml` to strip VitePress frontmatter, append a full-changelog footer, update reused drafts, and fail when sanitized notes do not start with `# v<version>`. Validation: local notes-generation smoke check for v0.3.0 and `git diff --check` passed.
- Reverted the release malformed-body control references from `release-writing` and removed the durable memory note; `.github/workflows/release.yml` already had no remaining frontmatter-control diff. Validation: `rg` found no control references in skill/workflow and `git diff --check` passed.
- Added skill-only Publish Mode instruction to inspect GitHub Release body formatting (`gh release view --json body,url`), reject frontmatter/generic bodies, and edit from a frontmatter-stripped draft when needed; no release workflow controls were added. Validation: `git diff --check -- .agents/skills/release-writing/SKILL.md .github/workflows/release.yml` passed and diff touched only the skill.
- Made sandbox user-local installs work under the read-only rootfs: `/home/pbi/.local` is a writable executable tmpfs owned by uid/gid 1000, `/home/pbi/.local/bin` is on PATH, and docs show installing uv inside the sandbox. Validation: focused sandbox CLI pytest, related run-command pytest, Ruff check, docs build, and `git diff --check` passed.
- Fixed sandbox web startup display issue by replacing the background Rich spinner with a one-time stderr status line before `docker run`, so container server/banner output is not interleaved with spinner redraws. Validation: focused pytest for sandbox browser launch, compileall for touched Python files, and `git diff --check` passed.
- Made sandbox user-local tools persistent per project: `/home/pbi/.local` now mounts a project-scoped Docker volume instead of tmpfs, while `/home/pbi/.cache` remains tmpfs; docs and CLI tests updated. Validation: focused sandbox pytest, Ruff check/format-check for touched Python files, docs build, and `git diff --check` passed.
- Added curl to the sandbox Alpine image package list and updated sandbox Dockerfile tests to expect curl while keeping other removed packages absent. Validation: `uv run pytest -q --tb=short -x tests/test_cli.py -k sandbox`, `uv run ruff check tests/test_cli.py`, and `git diff --check` passed.
- Fixed uv installer receipt failure in sandbox by adding project-scoped persistent `/home/pbi/.config` volume and creating it in the image; `/home/pbi/.local` remains persistent and `/home/pbi/.cache` tmpfs. Validation: `uv run pytest -q --tb=short -x tests/test_cli.py -k sandbox`, Ruff check/format-check on touched Python files, docs build, and `git diff --check` passed.
- Made the sandbox user's whole `/home/pbi` writable and persistent per project via a project-scoped Docker volume, retained `/home/pbi/.cache` as tmpfs, and added `/home/pbi/.bun/bin` to the sandbox PATH for Bun installs. Validation: sandbox CLI pytest, Ruff check/format-check, docs build, and `git diff --check` passed.
- Clarified and tightened sandbox writable-home implementation: Dockerfile no longer precreates installer-specific home subdirs (`.local`, `.config`, `.bun`); `/home/pbi` is the generic per-project persistent volume and docs/tests now state it is not a folder-by-folder allowlist. Validation: focused sandbox pytest, Ruff check/format-check, docs build, and `git diff --check` passed.
- Removed sandbox Bun-specific PATH wiring from the generic writable-home design: image PATH now includes only standard `/home/pbi/.local/bin`; `/home/pbi` remains the per-project persistent home volume and tool-specific paths are not hard-coded. Validation: focused sandbox pytest, Ruff check/format-check for `tests/test_cli.py`, docs build, and `git diff --check` passed; an attempted Ruff run against Dockerfile failed because Ruff parses it as Python.
- Added ripgrep (`rg`) to the sandbox Alpine image package list and updated Dockerfile tests/docs to expect it. Validation: focused sandbox pytest, Ruff check/format-check, docs build, and `git diff --check` passed.
- Hardened sandbox image/shell for public user-local tool installs by adding `libstdc++`/`unzip` and a generic shell bootstrap (`PBI_AGENT_SHELL_BOOTSTRAP` + bash executable) that sources user profiles, discovers `$HOME/**/bin`, and uses Bash `command_not_found_handle` to refresh discovery for same-command install-and-run flows instead of hardcoding tool-specific PATH entries. Validation: sandbox pytest, shell bootstrap unit test, Ruff check/format-check for touched files, docs build, `git diff --check`, Docker image build, and fresh Bun install plus same-shell `bun --version` smoke passed.
- Added sandbox detached mode via top-level `-d/--detach`: Docker run gets `--detach`, skips TTY, captures/prints container id, and detached web waits/opens the host browser after container start. Docs describe stopping with `docker stop <container-id>`. Validation: focused sandbox pytest, Ruff check/format-check for touched CLI/tests, docs build, and `git diff --check` passed.
- Fixed sandbox Bun PATH issue for images that still install an older PyPI `pbi-agent`: final Docker image wraps `/bin/sh -c` to source the sandbox shell bootstrap through Bash, then delegates other `/bin/sh` use to BusyBox. This makes old Python `subprocess.run(..., shell=True)` calls pick up user-installed `$HOME/**/bin` paths. Validation: sandbox pytest, Ruff check/format-check for `tests/test_cli.py`, Docker build, and exact two-command Python shell smoke (`install bun`, then separate `bun --version`) passed.
- Split sandbox workspace execution path from host workspace identity/display: sandbox passes `PBI_AGENT_WORKSPACE_KEY`, `PBI_AGENT_WORKSPACE_DISPLAY_PATH`, and `PBI_AGENT_SANDBOX`; web/session/Kanban/lease scoping uses the host key while tools use the container cwd, host `~/.pbi-agent` is always bind-mounted, and the web header shows `Sandbox · <host folder>`. Validation: focused Python tests for workspace/sandbox/session/web/API types, `bun run test:web -- AppShell`, Ruff check/format-check, frontend lint/typecheck/build, docs build, and `git diff --check` passed.
- Fixed review finding for `sandbox run --project-dir`: exported workspace key/display now point at the host project subdirectory while Docker mount/home volume still use the repository root. Validation: `uv run pytest -q --tb=short -x tests/test_cli.py -k sandbox`, Ruff check/format-check for touched files, and `git diff --check` passed.
- Added sandbox local source mode via `pbi-agent sandbox --local-source ...`: container run exposes `PBI_AGENT_LOCAL_SOURCE`, overrides entrypoint to `/bin/bash`, installs the mounted checkout with `python -m pip install --user --prefer-binary -e "$PBI_AGENT_LOCAL_SOURCE"`, then execs `pbi-agent` for both web/run. Docs explain using `uv run pbi-agent sandbox --local-source web/run` to test unpublished changes. Validation: focused sandbox pytest, Ruff check/format-check for touched CLI/tests, docs build, and `git diff --check` passed.
- Added read-only sandbox mounts for standard host Git/GitHub account files when present: `~/.gitconfig`, `~/.config/git`, `~/.git-credentials`, `~/.ssh`, and `~/.config/gh`. Validation: focused sandbox CLI pytest, Ruff check/format-check for touched Python files, docs build, Docker nested mount smoke, and `git diff --check` passed.
- Diagnosed session `ee5f63efde2b4af2bc4b3970060e9439`: resumed run `a9760a30f85f48a28e74766b69306595` failed during a 4-tool iteration because `ThreadPoolExecutor.submit()` raised `RuntimeError: can't start new thread`. Fixed shared tool runtime to warm worker threads before submitting tool calls, retry with fewer workers on thread-start exhaustion, then fall back to serial execution if needed. Validation: `uv run pytest -q --tb=short -x tests/test_tool_runtime.py`, Ruff check/format-check for touched files, and `git diff --check` passed.
- Removed `SettingsPage.test.tsx` React `act(...)` warning noise by wrapping settings-dialog Zustand open/close setup in `act`; product code unchanged. Validation: `bun run test:web -- SettingsPage`, `bun run test:web` (one transient unrelated first-run timeout, rerun passed), `bun run lint`, `bun run typecheck`, and `git diff --check` passed.
- Added GitHub CLI (`gh`, Alpine package `github-cli`) to the sandbox image package list and updated sandbox docs/tests to mention it. Validation: focused sandbox pytest, Ruff check/format-check for `tests/test_cli.py`, docs build, and `git diff --check` passed.
- Prepared BasedPyright dev workflow for shipping without re-running BasedPyright per user request; local validation used Ruff, format, dead-code, full Python pytest, web tests/lint/typecheck/build, and docs build. Next: GitHub checks pending after PR.
- Diagnosed session `867bfab4205048d797df19d88481af96`: last parent run `180200ca6d9e4348a1e98567a2b5eaf7` remained `started` after launching 20 sub-agents; all children completed, and last persisted events were UI status updates `sending tool results to model...` / `waiting for model to process tool results...` at 2026-05-07T20:47:55Z. No final model_call/run_end/fatal_error/assistant message was persisted, so it likely stopped due to process interruption or provider wait before the synthesis response was written.
- Continued BasedPyright cleanup from `basedpyright-analysis.md`: actual remaining diagnostics were 18 `reportUnusedFunction` errors in split CLI modules, fixed with targeted pyright ignores for intentionally external private helpers. Validation: `uv run basedpyright` passed with 0 errors.
- Checked merged PR #275 description: content lists summary/validation/skipped changes, but body starts with stray `$` before `## Summary` (`$## Summary`).
- Fixed merged PR #275 body typo by replacing leading `$## Summary` with `## Summary` via `gh api graphql` because `gh pr edit` hit deprecated projectCards GraphQL failure. Validation: mutation response showed corrected body.
## 2026-05-08
- Refactored web navigation into shared sidebar shortcuts: AppShell header no longer renders Sessions/Kanban/Dashboard; Board/Dashboard use `AppSidebarLayout`; SessionSidebar includes shared app nav and removed old settings footer/collapsed settings. Validation: `bun run test:web -- AppShell SessionSidebar`, `bun run lint`, `bun run typecheck`, `bun run web:build`, and `git diff --check` passed. Full `bunx vitest run --config webapp/vitest.config.ts --maxWorkers=2` printed existing-looking API mock failures while exiting 0, so focused tests were used for acceptance.
- Moved SessionSidebar Settings to bottom footer/bottom collapsed rail by splitting `AppSidebarSettings` from `AppSidebarNav`; rebuilt static assets. Validation: `bun run test:web -- SessionSidebar`, `bun run lint`, `bun run typecheck`, and `bun run web:build` passed.
- Unified web shell into one persistent collapsible sidebar (brand+toggle, workspace badge, primary nav, route context-panel slot, footer with theme + settings) used by `/sessions` (via SessionPage), `/board`, `/dashboard`. Added `webapp/src/hooks/useSidebar.ts` with zustand+persist store (`pbi-agent.sidebar`, version 1, partialize `isOpen`) and `useSidebarShortcut` (Cmd/Ctrl+B; ignores Shift/Alt). New `AppSidebarLayout` exported from `webapp/src/components/AppSidebar.tsx` wraps `<AppSidebar contextPanel>` + main; `SessionSidebar` reduced to a pure session-list panel and rendered via the context slot. Removed `.session-layout`/`.sidebar*` CSS and replaced with `.session-panel-wrapper` + `.session-sidebar*` plus new `.app-shell-layout`/`.app-sidebar*` rules. New tokens: `--sidebar-width: 280px`, `--sidebar-width-collapsed: 56px`, `--topbar-height: 56px`. Added tests in `webapp/src/hooks/useSidebar.test.tsx` and updated `AppShell.test.tsx`/`SessionSidebar.test.tsx`. Validation: `bun run test:web` (365 passed), `bun run lint`, `bun run typecheck`, `bun run web:build` all green.
- Evaluated review findings for sidebar commit `c82fa391`: static asset finding was false because every `index.html` asset ref is tracked in the commit; mobile sidebar expansion and board height/topbar findings are valid. Validation: git tree/static ref checks and CSS/source inspection only.
- Fixed review findings for sidebar commit: mobile open sidebar now keeps an expanded grid width/context panel under 768px; board layout fills the sidebar main container instead of reserving removed topbar height; rebuilt static web assets. Validation: `bun run test:web -- AppShell`, `bun run lint`, `bun run typecheck`, `bun run web:build`, index asset existence check, and `git diff --check` passed.
- Diagnosed shipping frontend test timeouts as worker-contention/flakiness rather than deterministic per-test failures: the three named tests and the full suite passed on rerun, and `bun run test:web` now runs Vitest with `--maxWorkers=2` for stable local shipping. Validation: focused named tests, full `bun run test:web`, `bun run lint`, `bun run typecheck`, `bun run web:build`, and docs build passed.
- Diagnosed session `497759fa73dd4e1d9dce112ee4878940`: after auto compaction, combined web-run snapshots included stale pre-compaction message ids, so active timeline merge treated old run messages as unanchored and rendered saved history (`/review` + findings) before the old/new Working blocks. Fixed combined snapshot serialization to namespace/mark persisted run messages as historical and updated `timelineForDisplay` to signature-match historical messages with stale ids. Validation: `bun run test:web -- SessionPage`, focused `test_web_serve` pytest, Ruff check/format-check for touched Python files, `bun run lint`, `bun run typecheck`, and `git diff --check` passed.
- Fixed compacted-session display history gap: `timelineForDisplay` now preserves historical snapshot messages with stale/deleted persisted ids before a compaction boundary instead of treating them as duplicates of the rewritten active-context tail. Model context compaction logic unchanged. Validation: `bun run test:web -- SessionPage`, `bun run lint`, `bun run typecheck`, and `git diff --check` passed.
- Fixed `/fix-review` compacted-session UI regression: post-compaction stale historical snapshot messages now signature-anchor unless they are identified as pre-compaction by timestamp or pre-boundary work context; added frontend regression. Validation: `bun run test:web -- SessionPage`, `bun run lint`, `bun run typecheck`, and `git diff --check` passed.
- Replaced the exact assistant `[compacted context]` timeline marker card with a plain shadcn `Separator`, leaving the reference summary message as the normal assistant card; added `TimelineEntry` coverage. Validation: `bun run test:web -- TimelineEntry SessionPage`, `bun run lint`, and `bun run typecheck` passed.
- Added a centered `compacted context` label below the compacted-context timeline separator. Validation: `bun run test:web -- TimelineEntry`, `bun run lint`, `bun run typecheck`, and `git diff --check` passed.
- Fixed compacted-session duplicate initial user display: when a pre-compaction historical snapshot message is preserved and uniquely matches a rewritten post-compaction history tail message, the UI now treats the rewritten tail as already displayed while keeping model compaction context untouched. Validation: `bun run test:web -- SessionPage`, `bun run lint`, `bun run typecheck`, `bun run web:build`, and `git diff --check` passed.
- Fixed remaining compacted-session duplicate initial user display when the same rewritten-tail message also appears in a later historical run snapshot: pre-compaction duplicate detection now consumes a unique matching history item even when snapshot signatures are not unique. Model compaction context unchanged. Validation: `bun run test:web -- SessionPage`, `bun run lint`, `bun run typecheck`, `bun run web:build`, and `git diff --check` passed.
- Updated compacted-context marker UI to render the centered `compacted context` label between two separators (before and after the compaction block) and rebuilt static assets. Validation: `bun run test:web -- TimelineEntry`, `bun run lint`, `bun run typecheck`, `bun run web:build`, and `git diff --check` passed.
- Corrected compacted-context separator placement: marker now shows only the opening separator + label, while the closing separator renders after the reference summary assistant block. Validation: `bun run test:web -- TimelineEntry`, `bun run lint`, `bun run typecheck`, `bun run web:build`, static index asset check, and `git diff --check` passed.
- Moved web theme selection out of the unified sidebar footer into Settings > Appearance using the existing `ThemeProvider`/`themeOptions`; rebuilt static assets. Validation: focused `bun run test:web -- AppShell SettingsPage`, `bun run lint`, `bun run typecheck`, `bun run web:build`, and `git diff --check` passed. Full `bun run test:web` hit existing flaky timeout failures on first/second runs; focused changed tests and the later timed-out Composer test passed independently.
- Updated Settings > Appearance theme cards so each option uses its own theme palette background via per-option preview CSS variables/data attributes, and rebuilt static assets. Validation: `bun run test:web -- SettingsPage`, `bun run lint`, `bun run typecheck`, `bun run web:build`, and `git diff --check` passed.
- Changed Settings > Appearance theme options from a vertical stack to a responsive horizontal grid using auto-filled columns for future themes, and rebuilt static assets. Validation: `bun run test:web -- SettingsPage`, `bun run lint`, `bun run typecheck`, `bun run web:build`, static index asset check, and `git diff --check` passed.
- Amended HEAD commit `a0816ebb7f7acb1b47c3e9755fdc8a2b2c398f21` message from placeholder text to `chore: remove basedpyright analysis notes`, producing new commit `24dbc24`. Validation: `git log --oneline -n 5` confirmed new HEAD; no code checks needed for message-only amend.
- Removed session UI separators/background chrome: session topbar border, sidebar context/footer separators, and readonly session footer border/background. Validation: `bun run lint`, `bun run typecheck`, and `git diff --check` passed.
- Removed the remaining session composer/footer chrome by dropping `.composer` wrapper background and top border while keeping the chat input row styling. Validation: `bun run lint`, `bun run typecheck`, and `git diff --check` passed.
- Removed the saved-session delete trash icon from the session top toolbar, including obsolete styling and test expectation; session deletion remains available through existing session delete flows. Validation: targeted `bun run test:web -- SessionPage -t "does not render a saved-session delete action"`, `bun run lint`, and `bun run typecheck` passed; broader `bun run test:web -- SessionPage` hit a pre-existing/flaky timeout in `dedupes persisted messages from an idle active live timeline` while the changed test passed.
- Added app sidebar outside-click collapse from the main content pane, preserving inside-sidebar clicks/toggle/shortcut behavior; rebuilt static assets. Validation: `bun run test:web -- AppShell` passed on rerun after one flaky toggle timeout, focused toggle test passed, `bun run lint`, `bun run typecheck`, `bun run web:build`, static asset ref check, and `git diff --check` passed.
