# MEMORY.md

## Metadata
- Last compacted: 2026-05-13
- Scope: durable repo memory + active-day task events.
- Format: only `Metadata`, `Long-Term Memory`, and `Detailed Task Events`.

## Long-Term Memory
- Memory: token-efficient; keep durable decisions/follow-ups only, not logs.
- Workflow: no commit/push/merge/PR unless asked. Use session-only `TODO.md`; markers `[ ]`, `[>]`, `[X]`, `[!]`, `[-]`. Shell from workspace root; use `python3` for direct Python shell commands.
- Validate touched surface: Python Ruff+basedpyright+pytest; frontend Bun tests/lint/typecheck/build; docs build. Note skipped/hung checks.
- Architecture: CLI `src/pbi_agent/__main__.py` -> `src/pbi_agent/cli.py`; default command `web`; backend FastAPI `src/pbi_agent/web/`; frontend Vite React `webapp/`.
- API changes: align routes/schemas/session manager with `webapp/src/api.ts` + `webapp/src/types.ts`.
- Web API contract: `bun run web:api-types` runs `scripts/generate_api_types.py` to regenerate `webapp/src/api-types.generated.ts` from FastAPI OpenAPI plus extra SSE models; output includes schema types, SSE event unions, `ApiOperationResponses`, and `ApiJsonRequestBodies`. Pytest `test_api_types_codegen.py` enforces generated output is current.
- Constraints: provider/tool HTTP via `urllib.request`; internal data in `~/.pbi-agent/`; no migrations/backcompat.
- Tools: built-in `python_exec`, `list_files`, `search_files` removed; use shell (`rg --files`, `find`, `rg -n`) and Python as needed.
- Tool availability: V4A providers (`openai`, `chatgpt`) advertise `apply_patch` and hide duplicate `replace_in_file`/`write_file`; non-V4A providers do the inverse; `read_web_url` is advertised only when `web_search` is enabled.
- Project imports: top-level `skills`, `commands`, `agents`; public catalogs under `pbi-agent/{skills,commands,agents}`; local install to `.agents/...`.
- Sub-agents: `.agents/agents/*.md`; frontmatter only `name`/`description`; child model from active profile/CLI + `sub_agent_model`.
- Local commands: `plan` no questions; `review` Markdown; `fix-review` stops on `No findings.`; `ship-task` branch/merge helpers; `release` needs explicit publish wording and no history rewrite after PR unless approved.
- Release docs: `release-writing` skill governs releases; changelog index `docs/changelog/index.md`, files `docs/changelog/v<version>.md`, and VitePress sidebar links releases; release retrospective added validation triage + publish verification + continuation gate safety.
- Branding: public copy = local coding agent; preserve `pbi-agent`, `pbi_agent`, `PBI_AGENT_*`, `~/.pbi-agent`; logo `src/pbi_agent/web/static/logo.jpg`.
- Web/session: saved sessions use session-scoped APIs; `/api/live-sessions/*` and `/sessions/live/:id` are removed from user-facing test expectations. Blank/saved sessions can start/continue without an active live id; completed Kanban-bound runs detach live ids without ending the saved conversation.
- Web session manager: `src/pbi_agent/web/session_manager.py` is a thin public facade for `WebSessionManager`/startup lease; implementation mixins live under `src/pbi_agent/web/session/` by utility scope. Patch worker/auth globals in `pbi_agent.web.session.workers` and `pbi_agent.web.session.provider_auth`.
- Web events: frontend app/session streams use SSE over `GET /api/events/{stream}` and `/api/events/sessions/{session_id}` with `server.connected`/heartbeat and `since`/`Last-Event-ID`; web UI WebSocket event routes/helpers are removed. SSE event payloads are generated TS unions, but the frontend stream boundary keeps defensive `parseSseEvent()` validation.
- Web message identity: saved history/live replay use canonical persisted item/message ids `msg-<messages.id>` plus stable part ids; optimistic/display-local live items are replaced by `message_rekeyed` once persisted.
- Web status contracts: session/live API models use lifecycle statuses (`idle`/`starting`/`running`/`waiting_for_input`/`ended`/`failed`/`stale`); run history/detail/all-runs use persisted run-record statuses (`started`/`completed`/`interrupted`/`failed`, etc.). Keep them distinct.
- Web command input hardening: direct saved-session shell commands hold `WebDisplay` input disabled with `begin_direct_command()`/`finish_direct_command()`, and frontend `attachLiveSession(..., preserveEventCursor)` preserves current input/processing state when the applied SSE cursor is equal to or newer than the submit response cursor.
- Web live snapshots: `LiveSessionSnapshotModel` preserves typed `processing` and pending user-question payloads; frontend boundary types derive from generated models and keep `pending_user_questions` required nullable.
- Web frontend API boundary: `webapp/src/api.ts` should use generated `ApiOperationResponses`/`ApiJsonRequestBodies` via `apiRequest()` and `jsonBody()` when adding JSON API calls.
- Web durable runs: `run_sessions` has web projection fields/status, `snapshot_json` caches timeline from persisted `web_event` records, unfinished web runs become `stale` on startup, and web `RunTracer` attaches to canonical run ids.
- Web durable event replay: saved-session event streams reconstruct from persisted `web_event` records with original `seq`/`created_at`, so SSE `since` and `Last-Event-ID` cursors remain valid after restart.
- Saved-session timelines: API `history_items` are canonical chronological messages; frontend overlays only non-duplicate live/work snapshot items and avoids opening stale live streams for ended/static projections.
- Session compaction: clear completed `previous_id`; resume restores persisted user/assistant history; compaction includes unanswered trailing user turns and same-turn tool exchanges.
- Providers: OpenAI Responses use `instructions` + `previous_response_id`; ChatGPT subscription prepends system prompt; Codex transport is WebSocket-only (`chatgpt_codex_backend.py`), no unsupported compression.
- Web UI: prefer shadcn tokens/components; overlays use shared readable spacing; large modals need safe gutters + inner scroll; Radix tooltip `collisionPadding`/`sideOffset` may not protect cross-axis viewport gutters, so clamp both axes for edge-adjacent tooltips.
- Timeline: work runs coalesce thinking/tool activity between chat messages; active placeholders have stable keys; completed historical work-run expansion must not reset auto-follow; expanded Working activity uses a five-row scroll viewport that follows latest items and centers opened tool cards; Working tool cards use a single shadcn Accordion so opening one closes the others.
- Test hygiene: pytest isolates web upload root to per-test `tmp_path / "web_uploads"` to prevent fixture uploads leaking into `~/.pbi-agent/web_uploads`.
- Maintenance: startup calls `run_startup_maintenance()` once per UTC day via SQLite `maintenance_state`; retention lives in internal config `maintenance.retention_days` (default 30, min 1) and is editable via `pbi-agent config maintenance` plus web Settings/API. Purge removes old sessions/messages/run_sessions/observability_events/web_manager_leases and old unreferenced `~/.pbi-agent/web_uploads` files while preserving uploads referenced by retained messages/Kanban tasks; PyPI update check uses `urllib.request` and prints `uv tool install pbi-agent --upgrade` notice when newer.
- Sandbox: `pbi-agent sandbox [web|run]` builds a bundled package Dockerfile from `src/pbi_agent/sandbox/Dockerfile.sandbox`, installs matching `pbi-agent==${PBI_AGENT_VERSION}` from PyPI, ignores workspace Dockerfiles, uses versioned default image tags, per-workspace hashed container mount/home volume, host-side browser open for web, always-created host `~/.pbi-agent` bind mount, and an Alpine runtime with bash/curl/git/gh/Node/npm/ripgrep/patch plus build-base/Rust/Cargo/rustfmt and fixed uv 0.11.13 + bun 1.3.13 user installs. Sandbox passes host workspace identity/display envs while tools execute in the container cwd, so sessions/Kanban/runs are shared with non-sandbox. User-local sandbox installs/caches persist per project via writable `/home/pbi` Docker volume; Docker PATH stays generic with `/home/pbi/.local/bin`, while entrypoint/local-source/default-shell/shell-tool bootstrap loads user profiles, discovers `$HOME/**/bin`, and refreshes discovery on missing commands so tools like Bun/Cargo are visible without per-tool ENV hardcoding. Validate with focused sandbox pytest, Ruff, basedpyright, docs build, `uv build`, and Docker build/smoke when available.
- Project install UI: Settings Project Skills/Commands/Agents support official/custom candidate install, force-replace conflict flow, flat ProviderModal-style add dialogs, and API types/static rebuilds after contract changes.
- CLI web startup: server banner is single source of `Serving on...`; Linux browser launch strips only legacy `atk-bridge` from `GTK_MODULES` to suppress Ubuntu noise.
- Shell tool timeout policy: default `timeout_ms` 30000, max 300000; reject non-positive/non-integer/bool before subprocess. ChatGPT-advertised shell schema is compact/command-only so it does not invent default `working_directory`/`timeout_ms`; runtime/base schema still accepts optional overrides.
- Sub-agent web UI: parent timeline keeps child work in stable sub-agent cards; child routes own wait/input/processing/usage state; aggregate child processing/interruptability controls parent Working state without leaking child transcript churn.
- Startup stale cleanup: orphan active turn/sub-agent run records covered by stale web-session intervals become stale; stale projections clear processing/wait/pending state and downgrade running tool/sub-agent snapshots.
- Saved-session fork: `POST /api/sessions/{session_id}/fork` duplicates messages/uploads through target message ids, resets usage/previous_id, stores fork metadata/title `Fork-{original-title}`, and cleans copied uploads on failure.
- Project command `/create-task` creates Kanban cards through `pbi-agent kanban create` without implementation work.

- Web timeline/composer UX: Composer highlights `@file` and `$skill` tokens, `$skill` completions use `/api/skills/search`, user/assistant timelines support copy/fork shortcuts, fenced code uses Shiki `CodeBlock`, and Working summaries categorize read/search/shell/edit/sub-agent/question/other tools.
- Web layout/session chrome: global `AppSessionsContextPanel` provides saved sessions on Sessions/Kanban/Dashboard; sidebar head and session topbar share `--topbar-height: 48px` with collapsed sidebar 48px, muted workspace badge/topbar controls, remembered last saved session nav, and New Session closes the sessions sidebar.
- Frontend shadcn normalization: `FormDialog`/`ConfirmDialog` composites are canonical for migrated modals/confirms; Badge variants include `success`/`warning`/`info`/`running`/`completed`/`failed`, status dots render except `asChild`, `StatusPill` preserves running/completed status groups, and targeted task-form/delete/status/dropdown BEM override CSS was removed.
- CLI startup maintenance checks PyPI once per UTC day and emits a Rich stderr update warning only for newer versions; `MaintenanceResult.update_notice` remains plain string for diagnostics/tests.
- Composer input history recall: SessionPage passes main-session non-empty user messages; Composer ArrowUp/ArrowDown browses history with draft restore and resets browsing when history changes.

- OpenAI/ChatGPT `apply_patch` is Codex-compatible freeform custom tool: exact Lark grammar advertisement, raw V4A `custom_tool_call` input, local Lark validation/move support, Codex-style rejection of empty `Update File`/move-only hunks, and model-facing `custom_tool_call_output.output` is token-efficient plain text (`Success. Updated the following files\nA/M/D path`) rather than `{ok,result}` JSON. Web running events summarize raw V4A headers into path/operation/count metadata while preserving raw arguments.

## Detailed Task Events
## 2026-05-12
- Smoke-tested the new apply_patch tool with one multi-operation call that edited `TODO.md` and created `apply_patch_tool_smoke.txt`; verified both files with `read_file`.
- Fixed `src/pbi_agent/web/display.py` `_EventDisplayBase.user_prompt()` return type to include `QueuedRuntimeChange`, matching `WebDisplay`/`DisplayProtocol`; validation passed with `uv run basedpyright`.

## 2026-05-13
- Smoke-tested a single `apply_patch` call with multiple operations: edited `TODO.md` and created `apply_patch_multi_task_smoke.txt`; verified both files with `read_file`.
- Smoke-tested another single `apply_patch` multi-task call: updated `TODO.md` and created `apply_patch_single_call_smoke_2026-05-13.txt`; verified both files with `read_file`.
- Fixed completed web `apply_patch` multi-operation cards: completed metadata now carries `operation_count`/`affected_paths` from structured results or raw V4A input, and frontend titles show secondary files compactly (for example `TODO.md + apply_patch_tool_smoke.txt`); focused backend/API-codegen/frontend tests, Ruff, basedpyright, Bun lint/typecheck, and `git diff --check` passed.
- Checked saved session `449cd48da1c84e6cb74f310859b2614d` and `pbi-agent.html`: historical run used Codex freeform `apply_patch` successfully with custom tool outputs, but persisted running web events still show `<missing operation_type> <missing path>` because the snapshot predates the running-summary fix; completed cards render as `Edit TODO.md` and hide add/update secondary files.
- Verified Codex rejects empty `Update File` hunks/move-only updates, added matching pbi-agent semantic checks plus focused tests; validation passed with Ruff, basedpyright, and focused apply_patch pytest.
- Ran final full gate for apply_patch freeform changes; fixed stale sandbox Dockerfile package expectation, then Ruff, format check, basedpyright, full pytest, Bun lint/typecheck, and `git diff --check` passed.
- Polished web `apply_patch` running display: raw V4A input now yields compact path/operation/count metadata and frontend renders a running file-edit card; validation passed with focused web-display pytest, API type codegen test, frontend ToolResult test, Bun lint/typecheck, Ruff, basedpyright, and `git diff --check`.
- Updated `.agents/commands/review.md` so `/review` explicitly runs autonomously in the current session and does not call/delegate to `.agents/agents/reviewer.md` or any reviewer sub-agent; validation was a direct content check.
- Fixed review finding for web `apply_patch` running summaries: raw V4A `*** Move to:` now displays move context as `move_file old → new` with diff/count metadata; focused web-display pytest, Ruff, format check, and basedpyright passed.
- Split web `apply_patch` multi-operation display into one card per file operation for running and completed events; raw V4A parsing now carries per-op path/operation/diff with synthetic call-id suffixes. Focused web-display pytest, frontend ToolResult test, Ruff, basedpyright, Bun lint/typecheck, and `git diff --check` passed.
- Fixed review finding for split web apply_patch cards: replacing running cards now reinserts completed cards at the first removed index, preserving mixed tool order; added focused mixed apply_patch+shell test and passed focused pytest, Ruff, format check, basedpyright, and `git diff --check`.
- Fixed review finding for single-file `apply_patch` moves: completion metadata now uses the parsed V4A `Move to` operation/path, so completed cards stay `move_file old → new`; focused web-display pytest, Ruff, format check, basedpyright, and `git diff --check` passed.
