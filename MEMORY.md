# MEMORY.md

## Metadata
- Last compacted: 2026-04-30
- Scope: durable repo memory + append-only active-day task events.
- Format: only `Metadata`, `Long-Term Memory`, `Detailed Task Events`; compact prior-day detail into durable notes before new work.

## Long-Term Memory
- Workflow: no commit/push/merge/PR unless asked. Use session-only `TODO.md`; reset before work; markers `[ ]`, `[>]` in progress, `[X]` done, `[!]` blocked, `[-]` dropped. Shell commands run from workspace root by default; avoid full-path `cd`.
- Validate touched surface: Python = Ruff check/format + focused pytest; frontend = `bun run test:web`, `bun run lint`, `bun run typecheck`, `bun run web:build`; docs = `bun run docs:build`; broad = repo Ruff/Bun/pytest. Note environment hangs/gaps.
- Architecture: CLI entry `src/pbi_agent/__main__.py` -> `cli.py`; system prompt says `pbi-agent`, check `pbi-agent -h`. Commands: `web` default, `run`, `sessions`, `config`, `skills`, `commands`, `agents`. Web FastAPI under `src/pbi_agent/web/`; frontend Vite React TS in `webapp/`; build output `src/pbi_agent/web/static/app`.
- Web API change: keep routes/schemas/session manager + `webapp/src/api.ts` + `types.ts` aligned. Tests use `DisplaySpy`; web tests beside `webapp/src/**/*.test.ts(x)`, behavior > snapshots.
- Provider/runtime: HTTP via `urllib.request`; providers under `src/pbi_agent/providers/`; internal data in `~/.pbi-agent/`; no migrations/backcompat. `chatgpt` + `github_copilot` first-class subscription providers, separate from API-key `openai`.
- Tools: built-in `python_exec`, `list_files`, and `search_files` were removed; use shell for discovery/search (`rg --files`, `find`, `rg -n`) and Python as needed. `shell` truncates stdout/stderr at 12k chars each with middle omission; shared `bound_output` default remains 1k.
- Project imports: `skills`, `commands`, `agents` top-level CLI groups. Materializer discovers public `skills/*/SKILL.md`, `commands/*.md`, `agents/*.md`, ignores source repo `.agents/*` internals unless explicit, installs to `.agents/...`. Official catalogs: `pbi-agent/{skills,commands,agents}`.
- Project sub-agents only `.agents/agents/*.md`; frontmatter only `name`/`description`; child model from active profile/CLI + `sub_agent_model`. Shared frontmatter parser supports YAML block scalars for `description`, rejects block-scalar `name`; install rejects unsafe names.
- Local commands: `plan.md` noninteractive/plain Markdown/no clarifying questions; `review.md` plain Markdown not JSON; `execute.md` compact prose; `fix-review.md` stops on `No findings.`; `ship-task.md` action-oriented, prefixed kebab-case branches, deletes merged branches; `retrospective.md` compressed.
- Branding: public copy = general local coding agent; preserve `pbi-agent`, `pbi_agent`, `PBI_AGENT_*`, `~/.pbi-agent`. Canonical logo `src/pbi_agent/web/static/logo.jpg`; banner dark `BI`, `work smart.`, centered `v{__version__}`.
- Web/session: saved sessions reopen via `resume_session_id`; `_resume_session()` restores provider checkpoints when no replay; `WebSessionManager` uses SQLite lease heartbeat with `BEGIN IMMEDIATE`/`busy_timeout`; managed startup normalizes running tasks.
- Provider ordering: OpenAI Responses use `instructions` + `previous_response_id`; ChatGPT subscription prepends system prompt to replay/current input. `x-codex-turn-state` is sticky routing state, not incremental input.
- Provider parsing/retry: direct OpenAI Responses and Anthropic Messages scan all output/content blocks for text + tool calls; OpenAI-compatible/generic and GitHub Copilot chat-completions must merge text/tool calls/reasoning across all `choices`; OpenAI semantic `response.failed` code `server_is_overloaded` retryable.
- Display/tool runtime: assistant + local tool phases use display protocol hooks; web snapshots `processing_state` and running tool groups; CLI shows local-tool spinner. `execute_tool_calls(..., on_result=...)` streams callbacks serially in completion order but returns provider results in input order; callback lock prevents concurrent display mutation; providers call `tool_execution_stop()` even on exceptions.
- Recent fixes: failed provider turns delete unanswered user messages; sub-agent parent context excludes current user turn; `read_file.start_line` unbounded positive and `max_lines` capped. Kanban first runnable stage sends slash command + task details; later stages command-only. Done sort newest-first; rebuild packaged SPA after frontend changes.
- Settings/model fallback: profile blank `sub_agent_model` resolves to profile main model, while explicit CLI/env sub-agent overrides win; Settings modal should describe blank sub-agent as profile-main fallback.
- Web UI: `!command` composer input calls live-session shell endpoint; persisted shell output to `SessionStore`; shell mode warning UI + refocus after completion. Multiline user timeline uses `pre-wrap`; websocket-only sessions bind `eventLiveSessionId` before `input_state`. Slash/custom commands may include image uploads; shell `!` blocks images. Composer image menu opens native picker via `showPicker()`/`click()` fallback.
- Web UI/design: shadcn/Tailwind v4 uses ThemeProvider/theme switcher + `@/*` alias; text-crop fixes rely on readable min-heights/line-heights, non-clipping badges, `object-fit: contain` attachment previews, centered header nav, and dense chrome buttons per `DESIGN.md`.
- Web overlay/design: `overlays.css` direct `data-slot` selectors enforce dropdown content/item spacing over component overrides. Theme/composer/run-history/profile/session dropdowns should keep shared readable menu spacing. `RunDetailModal`, `TaskModal`, and board editor use large glass surfaces, safe viewport gutters, inner scroll bodies, readable labels/inputs, and ghost footer actions.
- Kanban/dashboard UI: task/card/stage/header actions use ghost header-nav style (`task-card__action-button`/`board-layout__action-button`) with centered compact rows; stage headers cap long badges; dashboard scope/date/filter controls use ghost chips/selects, centered date range, and compact count badge.
- Apply-patch UI: live-session tool events carry V4A diff metadata plus `diff_line_numbers`; `GitDiffResult` renders real gutters, collapsed context, intraline highlights for 1:1/equal-count replacements, conservative unpaired unequal-count blocks, failed labels, and compact delete cards.
- Apply-patch tool: V4A context matcher handles exact/rstrip/trim/Unicode-normalized matches and emits hints for literal diff-prefix chars (`-`, `+`, leading space) in Markdown/list lines. `create_file` replace-existing behavior allowed. `file_edit.unified_diff_to_v4a` used internally for `write_file`/`replace_in_file` display diffs.
- Markdown rendering: assistant/thinking content uses shared `MarkdownContent` with `remark-gfm`; pipe tables render as real horizontally-scrollable tables with no wrapping in cells.
- Docs/artifacts: repo URL `https://github.com/pbi-agent/pbi-agent`; external catalog READMEs point there; `WEBAPP_CURRENT_STATE_SPEC.md` = web UX/UI redesign handoff. Docs IA is flattened into grouped top-level pages (Getting Started, Using pbi-agent, Customizing); `session-commands.md` documents `@file`, `!shell`, slash commands, project commands, and images; provider docs include Azure Responses config and image support matrix.
- Git remote: default `origin` fetch/push URL should be `https://github.com/pbi-agent/pbi-agent.git`; legacy `nasirus/pbi-agent` is not canonical.
- Env repair: if VS Code pytest discovery breaks after partial `.venv`, `uv sync --reinstall` fixed missing `pygments.formatters`/`packaging.version`; verify `.venv/bin/python -m pytest --collect-only -q ...` + `uv pip check`.
- Known validation gaps: repo-wide `uv run ruff format --check .` may report unrelated pre-existing `src/pbi_agent/agents/__init__.py`; some focused provider pytest previously failed only google/xai default reasoning expectation.
- Active follow-up: source-repo internal `.agents/skills/*` bundles never surface as downloadable/installable candidates.
- Active follow-up: web overlay primitives need global readable defaults: collision padding, side offsets, min-height/line-height, p-2-ish panel padding; document in `DESIGN.md`.
- Auto-compaction fix: `_compact_live_session()` must use `_messages_for_compaction()` so unanswered trailing user turns are summarized; `_messages_for_provider_restore()` still strips trailing user turns for resume/restore. `_run_tool_iterations()` accumulates same-turn tool exchanges and clears after successful auto-compaction; compaction transcript uses `<pending_tool_exchange>` for one exchange and `<current_turn_tool_exchanges>` for multiple.

## Detailed Task Events
## 2026-04-30
- Made duplicate/busy web manager startup failures user-facing and fixed review/user repro: `WebManagerStartupError` subclasses are captured from Uvicorn lifespan logging, including traceback text records without `exc_info`; expected traceback/exit logs are suppressed; `PBIWebServer` prints one warning after `server.run()`. Validation: `uv run ruff check src/pbi_agent/web/server_runtime.py tests/test_web_serve.py`; focused `uv run pytest` for warning/filter/extractor/startup/banner tests.
- Evaluated opencode compaction in `~/opencode` vs pbi-agent. Key finding: opencode uses model-limit overflow triggers, non-destructive compaction checkpoints (`compaction` parts + assistant `summary` + `tail_start_id` projection), media stripping/replay, old tool-output pruning, and plugin hooks; pbi-agent uses fixed `compact_threshold`, destructive marker+summary+tail rewrite, chars/4 estimates, and stronger same-turn pending-tool exchange summarization. No code changes/validation.
- Resolved PR #218 merge conflicts with `origin/master`: conflicts were only `MEMORY.md`/`TODO.md`; kept PR compaction notes and current task TODO, formatted merged `tests/test_web_serve.py`, and made `_clear_runtime_env()` clear provider-specific key env vars so tests ignore local secrets. Validation: `uv run ruff check .`, `uv run ruff format --check .`, `uv run pytest tests/test_cli.py tests/test_internal_config.py tests/test_web_serve.py tests/test_session.py -q` (262 passed).
- Replaced ChatGPT Codex HTTP/SSE transport with WebSocket-only live loop internals: stdlib WS handshake/framing in `chatgpt_codex_backend.py`, ChatGPT-only WebSocket routing in `OpenAIProvider`, first turn request full replay/no previous id, same-turn continuations use `previous_response_id` + delta input, server-side `context_management`, wrapped WS errors/retry handling, connection-limit resend, and previous-response loss full-replay fallback. Validation: `uv run ruff check src/pbi_agent/providers tests/test_openai_provider.py`; `uv run ruff format --check src/pbi_agent/providers tests/test_openai_provider.py`; `uv run pytest tests/test_openai_provider.py -q` (51 passed).
- Fixed review findings for ChatGPT Codex WebSocket: stopped advertising `permessage-deflate` without decompression, and made 429 same-turn continuation retries clear/rebuild without `previous_response_id`. Validation: Ruff check/format on providers + `tests/test_openai_provider.py`; focused pytest for ChatGPT WebSocket retry/follow-up tests (4 passed).
- Fixed review findings for ChatGPT Codex WebSocket envelope/incomplete handling: corrected Codex `response.create` frame to top-level `{type, model, input, ...}`, matched headers (`originator=codex_cli_rs`, static Codex `0.124.0` UA), and verified WS URL matches Codex (`wss://chatgpt.com/backend-api/codex/responses`). Validation: Ruff check/format on providers + `tests/test_openai_provider.py`; `uv run pytest tests/test_openai_provider.py -q` (59 passed).
- Fixed review findings for ChatGPT Codex WebSocket handshake/parser: added Codex WS beta header, preserved buffered bytes after upgrade for first WS frame, merged streamed function-call argument delta placeholders with later done events by item id, and made protocol-required WS upgrade headers override caller conflicts while preserving auth. Validation: Ruff check/format on providers + `tests/test_openai_provider.py`; `uv run pytest tests/test_openai_provider.py -q` (62 passed).
- Fixed review findings for ChatGPT Codex WebSocket retry/parser parity: generic WS 429s now fail immediately while connection-limit 429s use normal retry budget, incomplete response events retry like Codex stream errors, and `response.reasoning_text.delta` reconstructs reasoning content. Validation: `uv run ruff check ...`; `uv run ruff format --check ...`; `uv run pytest tests/test_openai_provider.py -q` (64 passed).
- Diagnosed ChatGPT WebSocket reasoning UI for session `51527272515a48aeafe73d15aab1938e` / response `resp_0ec638f43630a39d0169f36f733a508191b5052d6dc94df505`: persisted final response has one `reasoning` output item whose `summary` contains two `summary_text` entries plus two `function_call` items; `_parse_response()` joins all summaries into one `reasoning_summary`, `request_turn()` emits one `render_thinking()`, and web store upserts one `thinking` timeline item. Validation: SQLite inspection only. Next: split per summary/output item if UI should show separate reasoning cards.
- Split OpenAI reasoning summary display: `_parse_response()` now stores `provider_data["reasoning_display_items"]`; `request_turn()` emits one `render_thinking()` per summary when no detailed `reasoning_text` is present, while detailed reasoning stays one block. Validation: `uv run pytest tests/test_openai_provider.py -q`; `uv run ruff check src/pbi_agent/providers/openai_provider.py tests/test_openai_provider.py`; `uv run ruff format --check src/pbi_agent/providers/openai_provider.py tests/test_openai_provider.py`.
- Replaced tool_group header chip in `webapp/src/components/session/TimelineEntry.tsx` with shadcn `Collapsible`/`CollapsibleTrigger`/`CollapsibleContent`: dropped per-group count `Badge` and the `<pre>` fallback (PR #217 `ToolResult`/`GenericToolResult` covers all tools); `defaultOpen` mirrors the prior apply_patch heuristic (`isApplyPatchToolMetadata` requires both `diff` and `path`). CSS `.timeline-entry__count`, `.timeline-entry__tool-item*`, `.timeline-entry__inline-spinner` and the `__running + __count` adjacency rule were removed; chevron rotation now also responds to `[data-slot="collapsible-trigger"][data-state="open"]`. Removed unused `hasCustomToolResult` export from `ToolResult.tsx`. Validation: `bun run test:web` 84/86 (2 pre-existing apply_patch delete + mixed-tool-cards failures, identical to baseline), `bun run lint` 2 pre-existing `ToolResult.tsx` errors, `bun run typecheck` 4 pre-existing `metadata possibly undefined` errors, `bun run web:build` clean.
- Coalesced consecutive non-message timeline items (`thinking` + `tool_group`) into ONE shadcn `Collapsible` at the SessionTimeline level: new `WorkRun` component in `webapp/src/components/session/SessionTimeline.tsx` builds `RenderUnit[]` via `buildRenderUnits()` (flushes work-run buffer on each `message` or `subAgentId` change). Trigger label is generic "Working" + chevron + running spinner; `defaultOpen` if any item running OR any tool entry is `apply_patch`. `TimelineEntry` gained a `bare` prop that strips its own collapsible/header for both `tool_group` (renders only `ToolResult` cards) and `thinking` (renders title + markdown content). Per-group "shell (2 calls)" / "read_file (3 calls)" badge labels are gone. CSS adds `.timeline-entry--work-run`, `.timeline-entry__header--work-run`, `.timeline-entry__work-run-body`, `.timeline-entry--bare`, and a global `[data-slot="collapsible-trigger"][data-state="open"] .timeline-entry__chevron` rotation rule. Existing `SessionTimeline.test.tsx` clicks for shell/read_file/Tool calls now click the single `/Working/` trigger. Autoscroll uses the work-run's last item id as `data-timeline-item-id`. Validation: `bun run test:web` 88/90 (same 2 pre-existing apply_patch delete + mixed-tool-cards failures as baseline), `bun run lint` 2 pre-existing `ToolResult.tsx` errors, `bun run typecheck` 4 pre-existing `metadata possibly undefined` errors, `bun run web:build` clean.
- Changed provider wait copy for tool-result follow-ups from `integrating tool results...` to `waiting for model to process tool results...` in OpenAI, Google, and xAI providers to clarify the app is waiting on model processing. Validation: Ruff check/format on touched providers; `uv run pytest tests/test_openai_provider.py tests/test_google_provider.py tests/test_xai_provider.py -q` (110 passed).
- Removed ShellToolResult fallback Summary block so running shell cards no longer duplicate the command shown in the card title; covered by ToolResult test assertions. Validation: `bun run test:web -- ToolResult` (4 passed).
- Normalized provider model-wait copy helpers across OpenAI/ChatGPT-Codex, Google, xAI, Anthropic, GitHub Copilot chat-completions/responses, and Generic: user turns show `analyzing your request...`; tool-result follow-ups show `waiting for model to process tool results...`; provider-native tool output types remain detected (`function_result`, `function_call_output`, `tool_result`, `role=tool`). Validation: Ruff check/format on touched providers/tests; six provider pytest files (140 passed).
- DRY follow-up: provider wait-message logic lives in `src/pbi_agent/providers/wait_messages.py::waiting_message_for_input`; all providers import it instead of defining local helpers. Validation: Ruff check/format on touched provider files/tests; six provider pytest files (140 passed).
- Added extension-driven syntax highlighting for `read_file` Content/Schema/Preview blocks via new `webapp/src/components/ui/code-block.tsx` (`<CodeBlock>`), backed by lazy-loaded Shiki (`webapp/src/lib/shiki-highlighter.ts` dynamic-imports `shiki` and uses `codeToHtml` with `github-light`/`github-dark` + `defaultColor: false`). Extension-to-language map lives in `webapp/src/lib/code-language.ts::languageForPath` (returns `undefined` -> plain `<pre>` fallback). `ToolResult.tsx` keeps `OutputBlock` for plain sections and uses local `CodeOutputBlock` + `previewLanguage()` heuristic only for `read_file` Content (path-derived), Schema (forced markdown), Preview (markdown if leading `|`/`- `/`# `, else plain). CSS in `webapp/src/styles/session.css` adds `.tool-result__code` constraints and `:root[data-theme="dark"] .tool-result__code .shiki span` overrides for dual-theme switch. Unmatched languages, in-flight async, or highlighter errors all fall back to the existing plain `<pre>` markup; CodeBlock keys highlighted html by `(language, value)` pair instead of synchronously resetting state in `useEffect` (satisfies `react-hooks/set-state-in-effect`). Test mocks use `vi.hoisted` to mock `@/lib/shiki-highlighter`. Added `shiki ^2.5.0` to root `package.json` dependencies (already present transitively via VitePress). Validation: `bun run test:web` 102/102, `bun run lint` clean, `bun run typecheck` clean, `bun run web:build` clean (each Shiki grammar emits its own lazy chunk so initial bundle is unaffected).
- Fixed git-diff-display regression for `replace_in_file` and `write_file` introduced by commit `1b446c4` ("update tool call output"), which narrowed `isApplyPatchToolMetadata` from `metadata?.diff && metadata.path` to `tool_name === "apply_patch" && metadata.path`. Python `agent/tool_display.py::_display_apply_patch_result` routes all of `apply_patch`/`replace_in_file`/`write_file` through `display.patch_result(..., tool_name=call.name)` (set defined in `_PATCH_DISPLAY_TOOL_NAMES`), so their completed metadata still has `path` + `diff` but `tool_name` matches the actual edit tool. `webapp/src/components/session/GitDiffResult.tsx` now exports `FILE_EDIT_TOOL_NAMES = {"apply_patch","replace_in_file","write_file"}` and `isApplyPatchToolMetadata` matches any tool in that set with a `path`; `ToolResult.tsx` imports the shared set instead of redefining its own `FILE_EDIT_TOOLS`. Running file-edit tools still fall through to the generic card (`isApplyPatchToolMetadata && !running` guard unchanged) since metadata may be incomplete mid-flight. Validation: `bun run test:web` 106/106 (added parametrized "renders %s completion with the git-diff layout" + running fallback case in `ToolResult.test.tsx`), `bun run lint` clean, `bun run typecheck` clean, `bun run web:build` clean.
- Redesigned the live-session welcome screen for connected sessions with no events yet. Replaced the generic `EmptyState` (Inbox icon + "Session started. Waiting for updates…") in `webapp/src/components/session/SessionTimeline.tsx` with a new `webapp/src/components/session/SessionWelcome.tsx` built from shadcn `Empty` primitives + lucide `WandSparklesIcon`. The new screen shows a reused `status-pill status-pill--running` "Live session" indicator (uses existing `--color-accent` pulsing dot from `utilities.css`, no raw Tailwind colors), a friendlier title ">_ work smart", a description explaining streaming output, and a 4-tip list (prompt to begin, `/` saved command, `!` shell command, `@` workspace file reference) with semantic Tailwind tokens (`bg-muted/40`, `border-border/60`, `text-muted-foreground`, `bg-background`) and `<kbd>` chips for the trigger characters. Wrapper has `role="status" aria-live="polite"` and the pill has `aria-label="Session is live"`. Updated `SessionTimeline.test.tsx` to assert the new copy; added `SessionWelcome.test.tsx` (live pill class/text, heading + description, 4 tips with kbd `/`/`!`/`@`, polite live region). Validation: `bun run test:web` 109/109, `bun run lint` clean, `bun run typecheck` clean, `bun run web:build` clean.
- Fixed Shiki syntax highlighting for the Prism (default) theme. Shiki was loaded with `defaultColor: false` so per-token `--shiki-light`/`--shiki-dark` CSS variables drive color, but `webapp/src/styles/session.css` only swapped to `--shiki-dark` under `:root[data-theme="dark"]`, leaving Prism (also a dark indigo background, `--background:#0b1326`) on the light tokens and producing near-invisible code. Inverted the logic: base `.tool-result__code .shiki` / `.tool-result__code .shiki span` now use `var(--shiki-dark, …)` and a new `:root[data-theme="light"]` rule opts back to `var(--shiki-light, …)`. ThemeProvider already toggles `.dark` class for both `prism` and `dark`, so this matches the dark-background cohort. Validation: focused `bunx vitest run` for `code-block.test.tsx` + `ToolResult.test.tsx` + `code-language.test.ts` (26/26), `bun run typecheck` clean, `bun run web:build` clean (verified emitted CSS contains only `[data-theme=light] .tool-result__code .shiki{…--shiki-light…}` overrides). `bun run lint`/`bun run test:web` had a pre-existing failure in untracked `SessionWelcome.tsx`/`SessionWelcome.test.tsx` unrelated to this change.
- Removed public `encoding` parameter from `read_file`: tool schema now exposes only `path`/`start_line`/`max_lines`, handler always uses auto encoding detection, and console read-file summaries no longer show encoding. Validation: focused `uv run pytest tests/test_read_file_tool.py tests/test_console_display.py tests/test_tool_registry.py` (51 passed), `uv run ruff check .`, `uv run ruff format --check .`.
