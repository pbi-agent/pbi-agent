# Memory

- 2026-04-12: Added AGENTS.md long-term memory guidance. Memory now belongs in `MEMORY.md` for future session continuity.
- 2026-04-14: Removed default Plan/Review kanban stage seeding from `src/pbi_agent/session_store.py`. Fresh boards now default to only Backlog and Done so command presets are never assumed to exist. Updated session store and web tests to create custom stages explicitly when needed. Validation: `uv run ruff check .`, `uv run ruff format --check .`, `bun run lint`, `bun run typecheck`, `bun run web:build`, `uv run pytest`.
- 2026-04-14: Added concise AGENTS.md instructions for `TODO.md` current-task tracking. `TODO.md` now represents only the active task session, uses checkbox items by default, and should be reset when it contains a fully checked list from the previous task. No tests run because this was a documentation-only change.
- 2026-04-14: Renamed remaining command-related backend/frontend contract wording from `mode` to `command`. This included `CommandConfig` discovery in `src/pbi_agent/config.py`, active command prompt injection in `src/pbi_agent/agent/session.py` and `src/pbi_agent/agent/system_prompt.py`, `/api/config/commands` plus `commands` payloads in the FastAPI web layer, slash-command search kind `command`, and kanban stage `command_id` persistence/API/frontend wiring instead of `mode_id`. Validation: `uv run ruff check .`, `uv run ruff format --check .`, `bun run lint`, `bun run typecheck`, `bun run web:build`, `uv run pytest`.
- 2026-04-14: Renamed the project command directory from `.agents/.commands` to `.agents/commands`. Updated command discovery in `src/pbi_agent/config.py`, aligned tests and settings UI copy, renamed the workspace folder, and rebuilt the web static bundle so generated assets matched the new path. Validation: `uv run ruff check src/pbi_agent/config.py tests/test_web_serve.py tests/test_internal_config.py tests/test_session.py`, `uv run pytest tests/test_internal_config.py tests/test_web_serve.py tests/test_session.py`, `bun run typecheck`, `bun run web:build`.
- 2026-04-14: Fixed board stage reordering in the webapp when persisted stages referenced deleted commands or profiles. Added `webapp/src/components/board/stageConfig.ts` to normalize stale `mode_id` and `profile_id` values against the current config bootstrap, used it in both drag reorder saves and the board stage editor initialization/submit path, and rebuilt the FastAPI-served web bundle. Validation: `bun run typecheck`, `bun run web:build`. `bun run lint` and direct `eslint` invocations hung in this environment with no diagnostics, including when scoped to the touched files.
- 2026-04-14: Fixed the board stage normalization regression so reorder/editor saves preserve existing `profile_id` and `mode_id` values by default, then retry once with a freshly fetched config bootstrap only when the backend rejects an actually unknown profile or command. Updated `BoardPage.tsx`, `BoardStageEditorModal.tsx`, and `stageConfig.ts`, and rebuilt the FastAPI-served web bundle. Validation: `bun run typecheck`, `bun run web:build`. `bun run lint` still hung without diagnostics, and a 20s direct `eslint` run scoped to the touched files timed out the same way.
- 2026-04-14: Reset `TODO.md` for the completed mode-to-command consistency task. The delivered state now uses `command`/`command_id` across command discovery, active prompt injection, FastAPI config payloads, slash-command search results, kanban stage persistence, and the React board/settings client. Validation remained green: `uv run ruff check .`, `uv run ruff format --check .`, `bun run lint`, `bun run typecheck`, `bun run web:build`, `uv run pytest`.
- 2026-04-14: Fixed the empty-board kanban run UX in `webapp/src/components/board/BoardPage.tsx` and `webapp/src/components/board/BoardStageEditorModal.tsx`. Starting a backlog task when the board only has Backlog and Done now opens a modal that offers stage creation instead of surfacing the backend 400, and the board stage editor can open with a blank stage row already inserted before Done for that flow. Validation: `bun run typecheck`, `bun run web:build`. `bun run lint` still hung without diagnostics and was terminated after waiting.
- 2026-04-14: Cleaned the remaining cosmetic command wording after the mode-to-command refactor. Updated example command headings under `.agents/commands/` and renamed residual command-related test names, fixtures, and locals that still said `mode`; left unrelated or historical `mode` usage alone. Validation: `uv run ruff check tests/test_internal_config.py tests/test_session.py tests/test_web_serve.py`, `uv run ruff format --check tests/test_internal_config.py tests/test_session.py tests/test_web_serve.py`, `uv run pytest tests/test_internal_config.py tests/test_session.py tests/test_web_serve.py`.
- 2026-04-14: Refactored `src/pbi_agent/web/serve.py` into a thin facade. Extracted FastAPI schemas, shared dependency/error helpers, and domain route modules under `src/pbi_agent/web/api/`, moved FastAPI assembly into `src/pbi_agent/web/app_factory.py`, and moved Uvicorn/bootstrap code into `src/pbi_agent/web/server_runtime.py` while preserving the `pbi_agent.web.serve` import surface. Also removed one unnecessary type assertion in `webapp/src/components/board/BoardPage.tsx` so `bun run lint` completed cleanly in this environment. Validation: `uv run ruff check .`, `uv run ruff format --check .`, `bun run lint`, `bun run typecheck`, `bun run web:build`, `uv run pytest`.
- 2026-04-14: Removed the temporary `import uvicorn  # noqa: F401` shim from `src/pbi_agent/web/serve.py` after the refactor. Updated `tests/test_web_serve.py` to patch `pbi_agent.web.server_runtime.uvicorn.Server.run`, which is where the server implementation now lives. Validation: `uv run ruff check src/pbi_agent/web/serve.py tests/test_web_serve.py`, `uv run pytest tests/test_web_serve.py`.
- 2026-04-14: Added missing web route coverage in `tests/test_web_serve.py` for config provider/profile list-update-delete endpoints, successful saved-session listing, live-session list/detail, task deletion, `new-chat`, uploaded image fetch, and app-level static/SPA routes. Validation: `uv run ruff check tests/test_web_serve.py`, `uv run pytest tests/test_web_serve.py`.
- 2026-04-14: Resolved the `issue-92-observability` merge conflict by keeping both the observability/dashboard test coverage and the broader web-route coverage in `tests/test_web_serve.py`, preserving both branches' memory history, and resetting `TODO.md` to the merge-resolution task. Validation: `uv run ruff check tests/test_web_serve.py`, `uv run ruff format --check tests/test_web_serve.py`, `uv run pytest tests/test_web_serve.py`.
- 2026-04-16: Renamed the repo-owned interactive `chat` surface to `session` across backend, frontend, web UI, tests, and docs. This included replacing `run_chat_loop`/`NEW_CHAT_SENTINEL`/`chat_reset`/`chat_turn` with `run_session_loop`/`NEW_SESSION_SENTINEL`/`session_reset`/`session_turn`, moving the live interaction API from `/api/chat/*` to `/api/live-sessions/*`, changing live-session payload `kind` to `"session"`, renaming the React session UI route from `/chat/*` to `/sessions/*`, moving the frontend session feature files from `webapp/src/components/chat/` to `webapp/src/components/session/`, renaming the live-session hook and Zustand store vocabulary, and rebuilding the FastAPI-served SPA bundle. Kept third-party `Chat Completions` terminology and external `/chat/completions` URLs unchanged. Validation: `uv run ruff check .`, `uv run ruff format --check .`, `bun run lint`, `bun run typecheck`, `bun run web:build`, `bun run docs:build`, `uv run pytest`.
- 2026-04-16: Implemented the initial frontend testing setup for the webapp. Added Vitest + jsdom + Testing Library wiring (`webapp/vitest.config.ts`, `webapp/src/test/`), added initial tests for `webapp/src/api.ts`, `webapp/src/store.ts`, `webapp/src/hooks/useLiveSessionEvents.ts`, and `webapp/src/components/board/BoardStageEditorModal.tsx`, updated `package.json` scripts for `test:web`, `test:web:watch`, and `test:web:coverage`, and documented the web testing workflow in `AGENTS.md`. Also added `@vitest/coverage-v8` plus a direct `semver` dev dependency so `bun run test:web:coverage` works reliably in this environment. Validation: `bun install`, `bun run test:web:coverage`.
- 2026-04-16: Expanded webapp frontend coverage with new Vitest suites for `webapp/src/hooks/useTaskEvents.ts`, `webapp/src/components/board/BoardPage.tsx`, `webapp/src/components/session/SessionPage.tsx`, and `webapp/src/components/settings/SettingsPage.tsx`. The new tests cover websocket invalidation/reconnect behavior, backlog runnable-stage prompting and stale stage-reference retry, session submit/warning/upload flows plus not-found handling, and settings empty/stale active-profile flows. Also tightened `webapp/src/components/board/BoardStageEditorModal.test.tsx` so TypeScript accepts the queried elements. Validation: `bun run test:web`, `bun run test:web:coverage`, `bun run typecheck`, `bun run web:build`. `bun run lint` still hangs in this environment and timed out after 30s with no diagnostics, consistent with earlier repo memory.
- 2026-04-16: Updated `.github/workflows/tests.yml` so the main GitHub Actions test workflow installs Bun `1.3.11`, runs `bun install --frozen-lockfile`, and executes `bun run test:web` after `uv run pytest`. Validation: `bun run test:web`.
- 2026-04-16: Switched the webapp Vite plugin from `@vitejs/plugin-react-swc` to `@vitejs/plugin-react` in `webapp/vite.config.ts` and `webapp/vitest.config.ts`, then refreshed `package.json` and `bun.lock` through Bun so the warning in web test runs goes away. Validation: `bun run test:web`, `bun run typecheck`, `bun run web:build`. `bun run lint` still hangs in this environment and timed out after 30s with no diagnostics.
- 2026-04-16: Added a modular provider-auth layer for account-backed authentication, with the first backend handling OpenAI ChatGPT subscription sessions. New auth storage/services live under `src/pbi_agent/auth/`, `ProviderConfig` now carries `auth_mode`, OpenAI runtime resolution can load ChatGPT account sessions from the auth store, and `OpenAIProvider` now builds request auth dynamically and refreshes stored OAuth sessions after a 401. Exposed provider auth status/import/refresh/logout through the web API (`/api/provider-auth/*`) and the CLI (`pbi-agent config providers auth-*`), and added targeted coverage for config runtime resolution, OpenAI header routing, and provider-auth web endpoints. Validation: `uv run ruff check src/pbi_agent/auth src/pbi_agent/config.py src/pbi_agent/providers/openai_provider.py src/pbi_agent/web/session_manager.py src/pbi_agent/web/api src/pbi_agent/web/app_factory.py src/pbi_agent/cli.py tests/test_internal_config.py tests/test_openai_provider.py tests/test_web_serve.py`, `uv run ruff format --check src/pbi_agent/auth src/pbi_agent/config.py src/pbi_agent/providers/openai_provider.py src/pbi_agent/web/session_manager.py src/pbi_agent/web/api src/pbi_agent/web/app_factory.py src/pbi_agent/cli.py tests/test_internal_config.py tests/test_openai_provider.py tests/test_web_serve.py`, `uv run pytest tests/test_internal_config.py tests/test_openai_provider.py tests/test_web_serve.py`.
- 2026-04-16: Fixed two `resolve_runtime()` regressions in the new auth-mode path. Saved ChatGPT-account profiles now keep their stored auth sessions in normal CLI resolution because `_resolved_provider_id()` compares against the auth-mode-specific default responses URL, and provider/API-key CLI overrides over a ChatGPT-backed profile now work because `resolve_runtime()` computes the effective provider before deciding auth mode and secret resolution. Added regression coverage in `tests/test_internal_config.py` for both the saved ChatGPT profile path and `--provider ... --api-key ...` overrides. Validation: `uv run ruff check .`, `uv run ruff format --check .`, `uv run pytest`.
- 2026-04-16: Aligned the ChatGPT subscription auth backend more closely with `~/opencode` by adding organization-ID fallback for account extraction and by honoring `expires_in` during refresh when the token payload does not expose a usable `exp` claim. Updated `src/pbi_agent/auth/providers/openai_chatgpt.py` and added direct regression coverage in `tests/test_openai_chatgpt_auth_provider.py` for both organization-claim import and refresh-expiry handling. Validation: `uv run ruff check .`, `uv run ruff format --check .`, `uv run pytest`.
- 2026-04-16: Added built-in ChatGPT browser and device auth flows on top of the modular provider-auth layer. `src/pbi_agent/auth/providers/openai_chatgpt.py` now owns PKCE authorize URL generation, OAuth code exchange, and device-code polling; `src/pbi_agent/auth/service.py` exposes generic browser/device flow helpers; `src/pbi_agent/auth/cli_flow.py` runs the localhost browser callback server for CLI login. Added `pbi-agent config providers auth-login --method browser|device`, and extended the FastAPI provider-auth API with flow start/get/poll plus a browser callback route, with pending flow state kept in-memory in `src/pbi_agent/web/session_manager.py`. Added regression coverage for backend flow primitives, CLI auth-login dispatch, and browser/device web flow round-trips. Validation: `uv run ruff check .`, `uv run ruff format --check .`, `uv run pytest`.
- 2026-04-16: Hardened the WSL browser opener used by CLI auth login so OAuth URLs with query parameters are launched through PowerShell first, with a quoted `cmd.exe` fallback, instead of relying on unquoted `cmd.exe start` arguments. Updated `src/pbi_agent/cli.py` and tightened `tests/test_cli.py` to exercise a real OAuth-style authorize URL rather than a bare localhost URL. Validation: `uv run ruff check src/pbi_agent/cli.py tests/test_cli.py`, `uv run ruff format --check src/pbi_agent/cli.py tests/test_cli.py`, `uv run pytest tests/test_cli.py`.
- 2026-04-16: Split OpenAI Responses request shaping into backend-specific options so ChatGPT-account runs can diverge cleanly from the default API contract. `src/pbi_agent/providers/openai_provider.py` now applies a ChatGPT backend contract when the responses URL is `https://chatgpt.com/backend-api/codex/responses`, omitting `max_output_tokens`, `prompt_cache_retention`, and `context_management`, and forcing `store=False` to match the `~/opencode` codex path more closely. Added regression coverage in `tests/test_openai_provider.py` for the ChatGPT-backed request body while preserving the default OpenAI request-shape assertions. Validation: `uv run ruff check src/pbi_agent/providers/openai_provider.py tests/test_openai_provider.py`, `uv run ruff format --check src/pbi_agent/providers/openai_provider.py tests/test_openai_provider.py`, `uv run pytest tests/test_openai_provider.py`.
- 2026-04-16: Matched the ChatGPT-backed model-call headers more closely to `~/opencode` by adding `originator: opencode`, an `opencode/... (platform release; arch)` user agent, and a `session_id` header on normal agent turns. Threaded `session_id` explicitly through `provider.request_turn(...)` from `src/pbi_agent/agent/session.py` instead of overloading `UserTurnInput`, keeping the header logic modular inside `src/pbi_agent/providers/openai_provider.py`. Extended `tests/test_openai_provider.py` to assert the ChatGPT auth path now sends the full Codex-style header set. Validation: `uv run ruff check src/pbi_agent/providers/openai_provider.py src/pbi_agent/agent/session.py tests/test_openai_provider.py`, `uv run ruff format --check src/pbi_agent/providers/openai_provider.py src/pbi_agent/agent/session.py tests/test_openai_provider.py`, `uv run pytest tests/test_openai_provider.py`.
- 2026-04-16: Added a ChatGPT-specific OpenAI Responses tool serializer so the ChatGPT backend no longer reuses the generic OpenAI tool shape verbatim. `src/pbi_agent/providers/openai_provider.py` now adds `strict: true` to function tools only when targeting `https://chatgpt.com/backend-api/codex/responses`, which matches the `~/opencode` Responses tool adapter more closely while leaving the default OpenAI path untouched. Extended `tests/test_openai_provider.py` to assert strict function tools are present in the ChatGPT-backed request body. Validation: `uv run ruff check src/pbi_agent/providers/openai_provider.py tests/test_openai_provider.py`, `uv run ruff format --check src/pbi_agent/providers/openai_provider.py tests/test_openai_provider.py`, `uv run pytest tests/test_openai_provider.py`.

- 2026-04-16: Aligned the ChatGPT-backed OpenAI Responses request envelope more closely with local Codex/opencode format without changing transport semantics. `src/pbi_agent/providers/openai_provider.py` now applies ChatGPT-specific `tool_choice="auto"`, `include=[]`, and a stable `prompt_cache_key` derived from the session id, while keeping those fields out of the default OpenAI path. Extended `tests/test_openai_provider.py` to cover both the session-backed prompt cache key and the no-session fallback. Validation: `uv run ruff check src/pbi_agent/providers/openai_provider.py tests/test_openai_provider.py`, `uv run ruff format --check src/pbi_agent/providers/openai_provider.py tests/test_openai_provider.py`, `uv run pytest tests/test_openai_provider.py`.

- 2026-04-16: Switched the ChatGPT-backed OpenAI Responses path to the streaming transport contract used by local Codex/opencode. `src/pbi_agent/providers/openai_provider.py` now sets `stream=True` and `Accept: text/event-stream` for the ChatGPT backend, then decodes SSE output by extracting the final `response.completed` payload before reusing the existing response parser. Updated `tests/test_openai_provider.py` so the ChatGPT header/request test now exercises an SSE response and asserts the streaming request shape. Validation: `uv run ruff check src/pbi_agent/providers/openai_provider.py tests/test_openai_provider.py`, `uv run ruff format --check src/pbi_agent/providers/openai_provider.py tests/test_openai_provider.py`, `uv run pytest tests/test_openai_provider.py`.

- 2026-04-16: Fixed ChatGPT strict function-schema shaping after backend validation started returning concrete schema errors. `src/pbi_agent/providers/openai_provider.py` now rewrites optional properties in ChatGPT function tool schemas into strict-compatible nullable fields and sets `required` to every property key, which matches the strict API expectations surfaced by session `0a7a73e119ac4d57beb7c5c2d7b6d228` (`shell.working_directory`). Extended `tests/test_openai_provider.py` to assert the ChatGPT `shell` schema now requires all properties and represents optional fields as `anyOf[..., {type: null}]`. Validation: `uv run ruff check src/pbi_agent/providers/openai_provider.py tests/test_openai_provider.py`, `uv run ruff format --check src/pbi_agent/providers/openai_provider.py tests/test_openai_provider.py`, `uv run pytest tests/test_openai_provider.py`.

- 2026-04-16: Fixed the ChatGPT streaming parser for successful 200 responses whose final `response.completed` payload omits `output`. `src/pbi_agent/providers/openai_provider.py` now reconstructs streamed message/function/reasoning output from SSE events like `response.output_item.added`, `response.output_item.done`, `response.output_text.delta`, `response.function_call_arguments.delta`, and `response.reasoning_summary_text.delta`, then merges those items into the final response metadata. Added `tests/test_openai_provider.py::test_parse_sse_response_reconstructs_output_from_stream_events` to cover the exact empty-output pattern seen in session `9b5ab8bf1a8d4fb3a5bd02c4957ca732`. Validation: `uv run ruff check src/pbi_agent/providers/openai_provider.py tests/test_openai_provider.py`, `uv run ruff format --check src/pbi_agent/providers/openai_provider.py tests/test_openai_provider.py`, `uv run pytest tests/test_openai_provider.py`.

- 2026-04-16: Fixed the ChatGPT tool-follow-up regression where post-tool requests dropped `session_id` and therefore omitted `prompt_cache_key` on the second Responses call. `src/pbi_agent/agent/session.py` now threads `session_id` through `_run_tool_iterations(...)` follow-up calls and the resumed `run_single_turn(...)` path, and the shared provider interface in `src/pbi_agent/providers/base.py` plus non-OpenAI providers now accepts an optional `session_id` kwarg for orchestration consistency. Updated `tests/test_session.py` so single-turn command/tool flows assert the same session id is preserved across tool iterations. Validation: `uv run ruff check src/pbi_agent/agent/session.py src/pbi_agent/providers/base.py src/pbi_agent/providers/google_provider.py src/pbi_agent/providers/anthropic_provider.py src/pbi_agent/providers/xai_provider.py src/pbi_agent/providers/generic_provider.py tests/test_session.py tests/test_openai_provider.py`, `uv run ruff format --check ...`, `uv run pytest tests/test_session.py tests/test_openai_provider.py`.
- 2026-04-16: Fixed the remaining ChatGPT tool-follow-up mismatch surfaced by session `4efa7b4b395b4a9e90eb3b25d0f6bc06`. `src/pbi_agent/providers/openai_provider.py` now builds backend-specific tool follow-up items: the default OpenAI path still sends only `function_call_output`, while the ChatGPT Codex backend replays each prior `function_call` immediately before its matching `function_call_output`, which matches the stricter Codex request invariant observed in local `~/codex`. Added `tests/test_openai_provider.py::test_openai_execute_tool_calls_replays_function_calls_for_chatgpt_backend` to lock the shape down. Validation: `uv run ruff check src/pbi_agent/providers/openai_provider.py tests/test_openai_provider.py tests/test_session.py`, `uv run ruff format --check src/pbi_agent/providers/openai_provider.py tests/test_openai_provider.py tests/test_session.py`, `uv run pytest tests/test_openai_provider.py tests/test_session.py`.
- 2026-04-16: Cross-checked the remaining ChatGPT follow-up failure against `~/codex`, `~/opencode`, and `~/hermes-agent`. Codex is the only local reference with explicit `x-codex-turn-state` handling and dedicated turn-state tests; opencode and hermes-agent expose `originator`/`User-Agent`/`session_id` behavior for ChatGPT but no explicit turn-state replay in repo search. Updated `src/pbi_agent/providers/openai_provider.py` to capture `x-codex-turn-state` from ChatGPT backend responses, replay it on later requests in the same turn, and clear it when the turn completes or the conversation resets. Added `tests/test_openai_provider.py::test_openai_request_turn_replays_chatgpt_turn_state_within_turn`. Validation: `uv run ruff check src/pbi_agent/providers/openai_provider.py tests/test_openai_provider.py tests/test_session.py`, `uv run ruff format --check src/pbi_agent/providers/openai_provider.py tests/test_openai_provider.py tests/test_session.py`, `uv run pytest tests/test_openai_provider.py tests/test_session.py`.
- 2026-04-16: Session `0db83833ce9f48e5a71ee9e74a2a40fd` showed the ChatGPT backend returning multiple `function_call` items with backend-assigned `id` fields (`fc_...`) and then rejecting our simplified replay on the tool-follow-up request. Updated `src/pbi_agent/providers/openai_provider.py` so parsed responses retain raw `function_call` items in `provider_data`, and the ChatGPT follow-up path now reuses those raw items verbatim before each matching `function_call_output` instead of reconstructing minimal call objects. Extended `tests/test_openai_provider.py::test_openai_execute_tool_calls_replays_function_calls_for_chatgpt_backend` to assert the raw `id`/`status` preserving replay. Validation: `uv run ruff check src/pbi_agent/providers/openai_provider.py tests/test_openai_provider.py tests/test_session.py`, `uv run ruff format --check src/pbi_agent/providers/openai_provider.py tests/test_openai_provider.py tests/test_session.py`, `uv run pytest tests/test_openai_provider.py tests/test_session.py`.
- 2026-04-16: Session `670b35a537444db5873ea250924388ef` showed that even a single-tool ChatGPT follow-up still failed after replaying the raw backend `function_call` item, which narrowed the remaining suspect to `previous_response_id`. Updated `src/pbi_agent/providers/openai_provider.py` to retry ChatGPT tool follow-ups once, with the same `input` items but without `previous_response_id`, when the backend returns `400 invalid_request_error`. Added `tests/test_openai_provider.py::test_openai_chatgpt_tool_follow_up_retries_without_previous_response_id` to lock down the fallback. Validation: `uv run ruff check src/pbi_agent/providers/openai_provider.py tests/test_openai_provider.py tests/test_session.py`, `uv run ruff format --check src/pbi_agent/providers/openai_provider.py tests/test_openai_provider.py tests/test_session.py`, `uv run pytest tests/test_openai_provider.py tests/test_session.py`.
- 2026-04-16: Refactored the ChatGPT subscription transport rules out of `src/pbi_agent/providers/openai_provider.py` into a dedicated `src/pbi_agent/providers/chatgpt_codex_backend.py` helper. The new backend module now owns ChatGPT/Codex-specific tool schema shaping, request options, SSE header shaping, turn-state capture/replay, raw function-call replay, and the one-shot fallback that drops `previous_response_id` for invalid tool follow-ups, while `OpenAIProvider` stays focused on generic Responses orchestration and parsing. Updated `tests/test_openai_provider.py` to import the ChatGPT user-agent helper from the new module and revalidated the focused provider/session suite. Validation: `uv run ruff check src/pbi_agent/providers/openai_provider.py src/pbi_agent/providers/chatgpt_codex_backend.py tests/test_openai_provider.py tests/test_session.py`, `uv run ruff format --check src/pbi_agent/providers/openai_provider.py src/pbi_agent/providers/chatgpt_codex_backend.py tests/test_openai_provider.py tests/test_session.py`, `uv run pytest tests/test_openai_provider.py tests/test_session.py`.
