# MEMORY.md

## Metadata
- Last compacted: 2026-04-27
- Scope: durable repo memory + append-only active-day task events.
- Format: only `Metadata`, `Long-Term Memory`, `Detailed Task Events`; compact prior-day detail into durable notes before new work.

## Long-Term Memory
- Workflow: no commit/push/merge/PR unless asked. Use session-only `TODO.md`; reset before work; markers `[ ]`, `[>]`, `[X]`, `[!]`, `[-]`. Shell commands run from workspace root by default; use relative paths and avoid full-path `cd ... &&` prefixes unless required.
- Validate touched surface: Python = Ruff check/format + focused pytest; frontend = `bun run test:web`, `bun run lint`, `bun run typecheck`, `bun run web:build`; docs = `bun run docs:build`; broad = repo Ruff/Bun/pytest. Env quirk: Bun/Node/npm/lint/test may hang; note gaps.
- Architecture: CLI entry `src/pbi_agent/__main__.py` -> `cli.py`; system prompt says `pbi-agent`, check `pbi-agent -h`. Commands: `web` default, `run`, `sessions`, `config`, `skills`, `commands`, `agents`. Web FastAPI under `src/pbi_agent/web/`; frontend Vite React TS in `webapp/`; build output `src/pbi_agent/web/static/app`.
- Web API change: keep routes/schemas/session manager + `webapp/src/api.ts` + `types.ts` aligned. Tests use `DisplaySpy`; web tests beside `webapp/src/**/*.test.ts(x)`, behavior > snapshots.
- Provider/runtime: HTTP via `urllib.request`; providers under `src/pbi_agent/providers/`; internal data in `~/.pbi-agent/`; no migrations/backcompat. `chatgpt` + `github_copilot` first-class subscription providers, separate from API-key `openai`.
- Model discovery live at runtime: ChatGPT Codex, Gemini, OpenAI-compatible `/models`, GitHub Copilot `GET /models`; Copilot live default `gpt-5.4`.
- Tools: built-in `python_exec`, `list_files`, and `search_files` were removed; use shell for discovery/search (`rg --files`, `find`, `rg -n`) and Python as needed. `shell` truncates stdout/stderr at 12k chars each with middle omission; shared `bound_output` default remains 1k.
- Project imports: `skills`, `commands`, `agents` top-level CLI groups. Shared GitHub/local materializer discovers public `skills/*/SKILL.md`, `commands/*.md`, `agents/*.md`; ignores source repo `.agents/*` internals unless explicit; installs to local `.agents/...`. Official catalogs: `pbi-agent/{skills,commands,agents}`.
- Project sub-agents only `.agents/agents/*.md`; frontmatter only `name`/`description`; child model from active profile/CLI + `sub_agent_model`. Shared frontmatter parser supports YAML block scalars for `description`, rejects block-scalar `name`; install rejects unsafe names.
- Local commands: `plan.md` noninteractive, no clarifying questions or `request_user_input`, plain Markdown; `review.md` plain Markdown not JSON; `execute.md` compact prose; `fix-review.md` fixes prior findings, stops on `No findings.`; `ship-task.md` action-oriented, prefixed kebab-case branches, deletes merged local/remote branches; `retrospective.md` compressed. Local `compress` skill prompt-only/no network.
- Branding: public copy = general local coding agent; preserve `pbi-agent`, `pbi_agent`, `PBI_AGENT_*`, `~/.pbi-agent`. Canonical logo `src/pbi_agent/web/static/logo.jpg` + favicon/logo fallbacks; banner dark `BI`, `work smart.`, centered `v{__version__}`.
- Web/session: saved sessions reopen via `resume_session_id`; `_resume_session()` restores provider checkpoints when no replay; `WebSessionManager` uses SQLite lease heartbeat with `BEGIN IMMEDIATE`/`busy_timeout`; managed startup normalizes running tasks.
- Provider ordering: standard OpenAI Responses use `instructions` + `previous_response_id`; ChatGPT subscription prepends system prompt to replay/current input. `x-codex-turn-state` = sticky routing state, not incremental input; no tool-result-only deltas from that header alone.
- Provider parsing/retry: direct OpenAI Responses and Anthropic Messages scan all output/content blocks for text + tool calls; OpenAI-compatible/generic and GitHub Copilot chat-completions must merge text/tool calls/reasoning across all `choices`; OpenAI semantic `response.failed` code `server_is_overloaded` retryable.
- Display/tool runtime: assistant + local tool phases use display protocol hooks; web snapshots `processing_state` and running tool groups; CLI shows local-tool spinner. `execute_tool_calls(..., on_result=...)` streams callbacks serially in completion order but returns provider results in input order; callback lock prevents concurrent display mutation; providers call `tool_execution_stop()` even on exceptions.
- Recent fixes: failed provider turns delete unanswered user messages; sub-agent parent context excludes current user turn; project sub-agent discovery temp-workspace covered. `read_file.start_line` unbounded positive; `max_lines` capped. Kanban first runnable stage sends slash command + task details; later stages command-only; slash detection splits whitespace. Done sort newest-first; packaged SPA may need rebuild after frontend changes.
- Settings/model fallback: profile blank `sub_agent_model` resolves to profile main model, while explicit CLI/env sub-agent overrides win; Settings modal should describe blank sub-agent as profile-main fallback.
- Web UI: `!command` composer input calls live-session shell endpoint; persisted shell output to `SessionStore`; shell mode warning UI + refocus after completion. Multiline user timeline uses `pre-wrap`; websocket-only sessions bind `eventLiveSessionId` before `input_state`. Slash/custom commands may include image uploads; shell `!` blocks images.
- Web UI/design durability: shadcn/Tailwind v4 refactor uses ThemeProvider/theme switcher + `@/*` alias; global text-crop fixes rely on readable min-heights/line-heights, non-clipping badges, `object-fit: contain` attachment previews, centered header nav, and dense chrome buttons using compact `--text-xs`/normal leading per `DESIGN.md`.
- Web overlay/design: `overlays.css` direct `data-slot` selectors enforce dropdown content/item spacing over component overrides. Theme/composer/run-history/profile/session dropdowns should keep shared readable menu spacing. `RunDetailModal`, `TaskModal`, and board editor use large glass surfaces, safe viewport gutters, inner scroll bodies, readable labels/inputs, and ghost footer actions.
- Kanban/dashboard UI: task/card/stage/header actions use ghost header-nav style (`task-card__action-button`/`board-layout__action-button`) with centered compact rows; stage headers cap long badges; dashboard scope/date/filter controls use ghost chips/selects, centered date range, and compact count badge.
- Apply-patch UI: live-session tool events carry V4A diff metadata plus `diff_line_numbers`; `GitDiffResult` renders apply_patch groups with real gutters, collapsed context, intraline highlights for 1:1/equal-count multi-row replacements, conservative unpaired unequal-count blocks, failed operation labels, and compact centered delete cards.
- Apply-patch tool: V4A context matcher handles exact/rstrip/trim/Unicode-normalized matches and emits hints for literal diff-prefix chars (`-`, `+`, leading space) in Markdown/list lines. `create_file` replace-existing behavior allowed. `file_edit.unified_diff_to_v4a` used internally for `write_file`/`replace_in_file` display diffs.
- Markdown rendering: assistant/thinking content uses shared `MarkdownContent` with `remark-gfm`; pipe tables render as real horizontally-scrollable tables with no wrapping in cells.
- Docs/artifacts: repo URL `https://github.com/pbi-agent/pbi-agent`; external catalog READMEs point there; `WEBAPP_CURRENT_STATE_SPEC.md` = web UX/UI redesign handoff.
- Git remote: default `origin` fetch/push URL should be `https://github.com/pbi-agent/pbi-agent.git`; legacy `nasirus/pbi-agent` is not canonical.
- Env repair: if VS Code pytest discovery breaks after partial `.venv`, `uv sync --reinstall` fixed missing `pygments.formatters`/`packaging.version`; verify `.venv/bin/python -m pytest --collect-only -q ...` + `uv pip check`.
- Known validation gaps: repo-wide `uv run ruff format --check .` may still report unrelated pre-existing `src/pbi_agent/agents/__init__.py`; some focused provider pytest previously failed only google/xai default reasoning expectation (`medium` vs `high`).
- Active follow-up: source-repo internal `.agents/skills/*` bundles never surface as downloadable/installable candidates.
- Active follow-up: web overlay primitives (`TooltipContent`, `DropdownMenuContent`/items, `SelectContent`/items, submenus) need global readable defaults: collision padding, side offsets, min-height/line-height, p-2-ish panel padding; document in `DESIGN.md` so new shadcn components inherit safe spacing.

## Detailed Task Events
## 2026-04-27
- Auto-compaction placement: inspected saved session `dcf6de3ac81d43ee8ab8cde2fe891b4d`; root cause was auto compact running after completed assistant turn in `run_session_loop`, so moved trigger into `_run_tool_iterations` after tool execution and before follow-up model request, guarded by non-empty tool results. Added tests for between-tool compaction and no final-response compaction. Validation: full `tests/test_session.py`, Ruff check/format touched files.
- Azure settings UI: provider form now defaults `azure_openai` env var to `AZURE_OPENAI_API_KEY`, requires Azure Responses URL with resource URL placeholder/help, and profile modal explains model = Azure deployment name while still fetching models through `/api/config/providers/{id}/models`. Added `SettingsPage` coverage for Azure provider save payload + Azure model discovery options. Validation: focused `SettingsPage.test.tsx`; `bun run typecheck`; `bun run lint`; `bun run web:build`; `git diff --check`.
- Fixed `tests/test_session.py` local `_DisplaySpy` after diagnostic `uv run pytest tests/test_session.py -x -vv` showed missing `assistant_start`/`assistant_stop`; added counters/no-op-compatible methods to match `DisplayProtocol`. Validation: `uv run pytest tests/test_session.py -x -vv`; `uv run ruff check tests/test_session.py`; `uv run ruff format --check tests/test_session.py`.
- Refactored `apply_patch` create/update into single `AppliedPatchResult` pipeline with normalized V4A display diff, line-number metadata, path/operation metadata, and observable fuzzy-match warnings; `apply_diff` now tries exact/rstrip/trim/Unicode-normalized context matching. Validation: `uv run pytest tests/test_apply_patch_tool.py tests/test_apply_diff.py`; requested Ruff check/format on touched apply files/tests; extra runtime metadata regression.
- Added provider-agnostic file edit backend tools inspired by Hermes: `write_file` for full-file writes and `replace_in_file` for old/new block replacements with conservative fuzzy warnings; shared file-edit helpers now own safe paths, normalized V4A display metadata, unified-diff conversion, and diff line-number metadata; `apply_patch` description is V4A-dedicated and registry/display route new edit tools through patch metadata. Validation: focused file-edit/apply/tool-registry/runtime pytest plus Ruff check/format on touched Python files.
- Shortened file-edit tool descriptions to match concise built-in style while keeping them self-contained: `apply_patch` = V4A/one-file unified diff only, `write_file` = complete text write, `replace_in_file` = unique old_string targeted edit. Validation: focused Ruff check/format and pytest for registry/file-edit/apply-patch.
- Made `apply_patch` V4A-only: removed unified-diff conversion fallback from the tool path, updated description/parameter text, and changed tests to reject unified diff syntax; `file_edit.unified_diff_to_v4a` remains for internal before/after display diff generation in `write_file`/`replace_in_file`. Validation: focused Ruff check/format, apply/file-edit/registry pytest, `git diff --check`.
- Simplified V4A diff API by merging `apply_diff_result()` into `apply_diff()`, which now returns `ApplyDiffResult`; updated apply-patch call site and tests to use `.content`/`.warnings`. Validation: focused Ruff check/format, apply-diff/apply-patch pytest, `git diff --check`.
- Reworked `apply_patch` to accept a single full V4A `patch` envelope (`*** Begin Patch`/file sections/`*** End Patch`) with grammar-style schema text, multi-file add/update/delete support, no move support yet, no `operation_type`/`path`/`diff` public fields, and no “Codex” wording. Existing UI metadata still uses first changed file. Validation: focused Ruff check/format, apply-patch/tool-registry/runtime pytest, `git diff --check`.
- Located Hermes edit-file tool description: not in `tools/file_operations.py`; public schema lives in `tools/file_tools.py` as `PATCH_SCHEMA` around lines 1052-1067, registered as `patch` around line 1120. `file_operations.py` only implements `patch_replace`/`patch_v4a` methods and docstrings. Validation: inspected upstream clone `/tmp/hermes-agent` only.
- TODO test update: used edit-file tool (`replace_in_file`) to replace fictitious TODO item. Validation: file edit only.
- Azure endpoint routing: `azure_openai` routes by `responses_url`: `/openai/v1/responses` -> `OpenAIProvider`; `/openai/v1` (or chat-completions base) -> `GenericProvider` with `/chat/completions`; `/anthropic/v1/messages` -> `AnthropicProvider`. Azure discovery follows endpoint shape and uses `api-key` for OpenAI-compatible paths.
- Azure OpenAI provider: added `azure_openai` kind reusing `OpenAIProvider`; API-key auth sends `api-key`, env fallback `AZURE_OPENAI_API_KEY`, requires explicit Azure `/openai/v1/responses` URL, model is deployment name, service tier rejected. Validation: focused provider/config/CLI pytest passed; `uv run ruff check .` passed; `uv run ruff format --check .` still reports pre-existing `src/pbi_agent/agents/__init__.py` formatting.
- Settings add provider/profile modal alignment: `ProviderModal` and `ModelProfileModal` now use task modal shell/body/footer (`task-form-dialog`, internal scroll body, ghost action buttons); settings form rows are minmax responsive and collapse on mobile. Added `SettingsPage` tests for standard modal shell/classes; rebuilt packaged SPA. Validation: focused `SettingsPage.test.tsx`; `bun run typecheck`; `bun run lint`; `bun run web:build`; `git diff --check`.
- Command tuning: `review.md` now scopes mixed diffs to active task before findings; `execute.md` now requires start/end dirty-file + touched-file + validation handoff. Validation: command doc diff inspected.
- Compressed `.agents/commands/execute.md` prose with compress skill; preserved headings, examples, inline code, and command/path refs. Validation: reread target file.
- Compressed `.agents/commands/review.md` prose with compress skill; preserved headings, bullets, output schema, inline code, and command/path refs. Validation: reread target file + inspected diff.
- Tooling audit: pbi-agent exposes 10 built-ins (~6.4k serialized chars): shell/apply_patch/replace_in_file/write_file/list_files/search_files/read_file/read_image/read_web_url/sub_agent. Compared local Codex, opencode, Hermes, and repo `deepagents`; recommendation is not delete all file discovery, but make `shell` first-class for discovery and remove/disable `list_files` first, optionally `search_files`, while keeping `read_file`/image/read_web_url and edit tools. No code changed. Validation: inspected registries/schemas with shell.
- Settings provider/profile actions now reuse kanban task action button design: ghost variant + `task-card__action-button` for Add Provider/Add Profile, Connect/Reconnect, Refresh, Disconnect, Edit, and Delete; settings action container spacing matches task card gap. Rebuilt packaged SPA. Validation: `bun run typecheck`; `bun run lint`; `bun run web:build`; `git diff --check`.
- Tool removal: deleted `list_files`/`search_files` tools and tests, removed registry/display/docs/system-prompt refs, and updated shell guidance for bounded `rg`/`find` discovery. Validation: focused registry/provider tests passed; `uv run pytest` 701 passed with 2 unrelated defaults failures (`google`/`xai` reasoning expected high vs medium); `uv run ruff check .` passed; `uv run ruff format --check .` only pre-existing `src/pbi_agent/agents/__init__.py`; `bun run docs:build` passed; grep excluding memory/TODO/deepagents clean.
- Daily memory routine: compacted 2026-04-26 entries into `Long-Term Memory`, removed prior-day dated detail, compressed prose per compress skill, and kept 2026-04-27 active entries. Validation: inspected `MEMORY.md` structure only.
- Settings design pass: refreshed shared `AlertDialog` sizing/grid/footer/buttons to match app action-button styling, fixed cramped delete-provider/profile confirmation layout, used header margin for dialog border inset and red destructive trash icon shell, kept provider action buttons in a 3-column desktop grid, and rebuilt packaged SPA. Validation: `bun run typecheck`; `bun run lint`; `bun run web:build`; `git diff --check`.
- Azure endpoint routing update: added endpoint classifier and routed Azure OpenAI settings to OpenAI Responses, OpenAI-compatible chat completions, or Anthropic Messages providers by URL; model discovery follows URL shape and settings copy now names all supported Azure endpoint forms. Validation: focused provider/generic/anthropic/web pytest; Ruff check/format touched Python; focused `SettingsPage.test.tsx`; `bun run typecheck`; `bun run lint`; `bun run web:build`.
- AlertDialog shadcn reset: replaced custom `webapp/src/components/ui/alert-dialog.tsx` styling with upstream radix-nova pattern from `shadcn view alert-dialog`, removing app-specific `task-card__action-button` and custom cropped widths/overflow. Rebuilt packaged SPA. Validation: `bun run typecheck`; `bun run lint`; `bun run web:build`; `git diff --check` on touched frontend/build files.

- Shadcn/Vite config audit: confirmed `webapp` shadcn info detects Vite, Tailwind v4, `src/styles/index.css`, `@` alias, radix-nova/lucide aliases/components; added missing `baseUrl: "."` plus TS 6 `ignoreDeprecations` to `webapp/tsconfig.json`; rebuilt packaged SPA. Validation: `bunx --bun shadcn@latest info --json` in `webapp`; `bun run typecheck`; `bun run lint`; `bun run web:build`; `bun run test:web`.

- Shadcn CLI prompt fix: `webapp` had `components.json` but no `webapp/package.json`, so `shadcn add -c webapp` treated it as not-a-project and prompted to scaffold. Added minimal `webapp/package.json` (`private`, `type: module`, Bun packageManager). Use `bunx --bun shadcn@latest add <component> -c webapp` from repo root, or run inside `webapp` once Bun cache is healthy. Validation: root `bunx --bun shadcn@latest add alert-dialog -c webapp --dry-run --yes` reaches overwrite preview; `bun run typecheck` passes.

- AlertDialog destructive usage: delete dialogs now use `AlertDialogContent size="sm"`, destructive media icon classes (`bg-destructive/10 text-destructive dark:bg-destructive/20 dark:text-destructive`), and existing `AlertDialogAction variant="destructive"`. Added `alert-dialog.test.tsx` to assert destructive action/media variants. Validation: focused alert-dialog test, `bun run typecheck`, `bun run lint`, `bun run web:build`.
- Input mention warnings: `@` paths that resolve outside workspace are ignored without warnings, so pasted aliases like `@/components/ui/button` do not produce noisy "path outside workspace" alerts; missing in-workspace mentions still warn. Validation: focused input-mentions pytest + Ruff check/format.

- Shadcn import alignment: destructive delete dialogs now import UI primitives via shadcn alias paths (`@/components/ui/alert-dialog`, `@/components/ui/alert`) matching examples; `alert-dialog.tsx` already imports `Button` from `@/components/ui/button`. Validation: focused alert-dialog test, `bun run typecheck`, `bun run lint`.

- AlertDialog Button usage check: no source change needed; `webapp/src/components/ui/alert-dialog.tsx` already imports shadcn `Button` from `@/components/ui/button` and wraps `AlertDialogAction`/`AlertDialogCancel` with `<Button ... asChild>`, so destructive actions use the shadcn Button variant.

- AlertDialog hover polish: `AlertDialogAction` and `AlertDialogCancel` keep using shadcn `Button` and now add app-style enabled hover/focus lift + shadow classes (`not-disabled:hover:-translate-y-0.5`, `not-disabled:hover:shadow-sm`, focus-visible equivalents). Test asserts destructive/cancel buttons include hover classes. Validation: focused alert-dialog test, `bun run typecheck`, `bun run lint`, `bun run web:build`.

- AlertDialog hover background fix: analyzed app action hover CSS (`task-card__action-button`/dashboard use `color: var(--foreground); background: var(--muted)`, task cards use `!important`). Moved AlertDialog action/cancel hover from Tailwind classes to `overlays.css` slot selectors with `background: var(--muted) !important`, foreground text, shadow, and lift so it beats shadcn Button variant hover utilities. Added CSS test. Validation: focused alert-dialog+overlays tests, `bun run typecheck`, `bun run lint`, `bun run web:build`.

- AlertDialog outside-click close: Radix AlertDialog prevents pointer-down outside by design, so added app-level overlay click handling in `alert-dialog.tsx`; root now supports controlled/uncontrolled open state via context and overlay calls `setOpen(false)` when clicking backdrop. Added test for overlay click close. Validation: focused alert-dialog test, `bun run typecheck`, `bun run lint`, `bun run web:build`.

- AlertDialog outside-click revert: removed app-level overlay click close/context state and restored Radix AlertDialog default behavior so users must click Cancel/action. Removed overlay-click test. Validation: focused alert-dialog+overlays tests, `bun run typecheck`, `bun run lint`, `bun run web:build`.
- Azure rename: provider kind now `azure` (label Azure, env `AZURE_API_KEY`) because one Azure endpoint can route OpenAI Responses, OpenAI-compatible chat completions, or Anthropic Messages; model discovery remains manual/deployment-name only. Validation: focused Azure provider/config/CLI/web pytest; focused `SettingsPage.test.tsx`; Ruff check touched scope; `bun run typecheck`; `bun run lint`; `bun run web:build`; format check still hits pre-existing `src/pbi_agent/agents/__init__.py`.
- Local config Azure rename: updated `~/.pbi-agent/config.json` so the former `openai-azure` provider/profile reference is `azure`, and Azure provider kinds use `azure` instead of `azure_openai`. Validation: JSON parse; `uv run python` `load_internal_config()` shows providers include `azure` and no stale `openai-azure`/`azure_openai` references remain.
- Review fix: Azure model discovery now returns manual-entry-only before auth checks, so Settings does not require `AZURE_API_KEY` to show custom deployment entry. Validation: focused `test_azure_provider_model_discovery_returns_manual_entry_required`; Ruff check touched files.
- Live session interrupt: added cooperative active-turn interrupt endpoint/UI, `WebDisplay` interrupt flag, `SessionTurnInterrupted`, `message_removed` optimistic user cleanup, and loop guardrail deleting persisted unanswered user turns without updating checkpoints. Validation: focused session/web pytest; focused web tests; Ruff check/format touched Python; `bun run typecheck`; `bun run lint`; `bun run web:build`.
- Live session interrupt UI restyle: removed topbar interrupt button; `Composer` now swaps the send button for a destructive filled-square stop button (`SquareIcon`, `.composer__stop` with `--color-error` halo + soft pulse, reduced-motion aware) whenever `processing.active` and `canInterrupt` are true; new optional Composer props `isProcessing`/`canInterrupt`/`isInterrupting`/`onInterrupt` keep tests/composer harnesses backwards compatible; mock Composer in `SessionPage.test.tsx` now renders the same `aria-label="Interrupt assistant turn"` button when those props are set. Validation: `bun run test:web -- SessionPage.test.tsx Composer.test.tsx`; `bun run typecheck`; `bun run lint`; `bun run web:build`.
- AGENTS workflow shell-path instruction: added guidance that shell commands run from workspace root by default, so agents should use relative paths and avoid full-path `cd ... &&` prefixes unless changing directories is explicitly required. Validation: docs-only edit inspected.

- Interrupt fixes from session `f24f104f7a224744899955e05944acc9`: OpenAI Responses now clears `previous_response_id` at each new user turn and uses it only for tool-result follow-ups inside the same turn; completed turns append local replay transcript so later turns replay stable history instead of branching from stale provider state. Interrupt events now carry `restore_input`, remove the matching optimistic queued user item, restore the text into Composer, and keep interrupted tool-call responses from poisoning the next turn. Validation: focused interrupt/OpenAI pytest, focused web tests, Ruff check/format touched Python, `bun run typecheck`, `bun run lint`, `bun run test:web -- Composer.test.tsx store.test.ts SessionPage.test.tsx`, `bun run web:build`.
- Interrupt logging polish: `_run_tool_iterations` now lets `SessionTurnInterrupted` propagate without `_log.exception(...)`, so expected cooperative interrupts no longer print ERROR tracebacks for tool execution/follow-up phases. Validation: focused interrupt pytest + Ruff check/format on `src/pbi_agent/agent/session.py`.
- Live-session compaction: added `/compact` local command and auto compact in `run_session_loop` when active context reaches `compact_threshold`; compaction appends marker `[pbi-agent compacted context]` + summary, resets/restores provider context from latest summary onward, clears previous checkpoint, and active-context restore/sub-agent snapshots ignore pre-marker transcript. Old messages remain persisted/displayed; no end-session compaction. Validation: full `tests/test_session.py`, focused slash-command web pytest, Ruff check/format touched files.
- Compaction tool exposure fix: compaction provider now uses an explicitly empty `ToolCatalog()` plus `web_search=False`, not a hard-coded disabled-tool set; `ToolCatalog.__bool__` is always true so empty catalogs do not fall back to built-ins in providers. Validation: full `tests/test_session.py`, `tests/test_tool_registry.py`, Ruff check/format touched files.
- Compaction prompt organization: moved long `COMPACTION_PROMPT` text out of `agent/session.py` into `src/pbi_agent/agent/compaction_prompt.py`; session imports the constant only. Validation: focused compaction session pytest + Ruff check/format touched files.
- Compaction UI wait-state fix: `_compact_live_session()` now calls `display.wait_stop()` after summary request, and `WebDisplay.wait_stop()` clears `processing_state` when ending `model_wait`, so manual/auto compaction no longer leaves web timeline stuck on "analyzing your request..." after input re-enables. Validation: full `tests/test_session.py`, focused web display pytest, Ruff check/format touched files.
- Validated manual compaction on saved session `85960970a5f643878d9bc4a5389ce206`: persisted one marker + summary, later turn request replayed only summary + latest user (no old `/plan`/`/execute`/marker), no trailing user/unended/error runs. Note compaction itself currently has no observability run/model_call record. Validation: SQLite inspection via `~/.pbi-agent/sessions.db` and `_messages_for_provider_restore()`.
- Compaction observability: live manual/auto compaction now creates a `run_sessions` row with `agent_type="compaction"`, logs `run_start`/`agent_step_start`/provider `model_call`/`agent_step_end`/`run_end`, uses compaction-only `TokenUsage`, and records metadata (`reason`, `input_message_count`, `summary_chars`, `estimated_context_tokens`). Validation: full `tests/test_session.py`, focused web display pytest, Ruff check/format touched files.
- Compact threshold default: global/default runtime compaction threshold is `200000` tokens. Keep Settings, runtime resolution fallbacks, CLI/help, web server args, docs, and provider tests aligned. Validation: focused CLI/provider/internal-config pytest + Ruff check/format touched files.
- Compaction prompt audit: pbi-agent prompt is `src/pbi_agent/agent/compaction_prompt.py`; opencode source prompt is `~/opencode/packages/opencode/src/session/compaction.ts` inline `defaultPrompt`, while hidden agent prompt is `~/opencode/packages/opencode/src/agent/prompt/compaction.txt` registered in `agent/agent.ts`; opencode plugin hook `experimental.session.compacting` lives in `~/opencode/packages/plugin/src/index.ts`. Hermes prompt is `~/hermes-agent/agent/context_compressor.py::_generate_summary` (structured handoff with Active Task, constraints, actions, state, decisions, pending asks, remaining work, secret redaction, same-language, focus topic, iterative updates).
- Auto-compaction tool-followup fix: when auto compact triggers after tool execution, compaction transcript now includes pending `ToolCall`s and provider-native tool result items, resets/restores summarized context, then continues with `COMPACTION_CONTINUATION_PROMPT` as a fresh user-style request instead of sending stale `tool_result_items`. Validation: full `tests/test_session.py`, Ruff check/format touched files.
- Compaction system prompt fix: session `ebc6bf91b27a4b7a894c86910f98ac38` showed compaction model calls still using the normal pbi-agent instructions while putting `COMPACTION_PROMPT` in user input; `_open_compaction_provider()` now passes `system_prompt=COMPACTION_PROMPT`, keeps empty `ToolCatalog()` and `web_search=False`, and compaction user input is only `<session_transcript>...`. Validation: full `tests/test_session.py`, Ruff check/format touched files.
- Review/ship fix: `compact_threshold=0` is now valid and preserved for saved profiles/runtime resolution so users can disable auto-compaction; profile/default fallbacks still use `200000`. Validation: touched-file Ruff check/format and focused pytest suite including `tests/test_internal_config.py`.
- Post-merge compaction hotfix: updated `tests/test_command_registry.py` to include `/compact` in web slash-command registry expectations; pushed commit `4e8f379` to `master`. Validation: focused command-registry + slash-command web pytest, Ruff check/format on touched test.
- Reload command: added `/reload` local session command to refresh provider initialization (`get_system_prompt()` + existing-catalog `refresh_tools()`), invoke optional reload callback for web `@file` cache, and avoid model/persistence. It intentionally excludes MCP reload; restart the session after `.agents/mcp.json` or MCP server tool changes. Web manager passes `on_reload=self.refresh_file_mentions_cache` for immediate manual `/reload` cache refresh and also refreshes when a live session ends; web `run_session_loop` test fakes must accept `on_reload`. `/reload` is a reserved project command alias and appears in web slash autocomplete. Follow-up removed redundant `/agents reload`; `/agents reload` now behaves like a normal user message, `/agents` only lists sub-agents. Validation: focused reload/session/web pytest, full `tests/test_web_serve.py`, focused removal/review pytest, touched-file Ruff check/format, `bun run docs:build`.
- Kanban live-session duplicate-response fix: `pbi-agent.html` showed UI-only duplicate final assistant response (`message-93` live + `history-923` replay). Root cause was Kanban task worker resuming saved session with UI history replay between stage runs; `run_single_turn(..., replay_history=False)` now suppresses display replay while still restoring provider context, and `WebSessionManager._run_task_worker()` passes it for all task runs. Validation: full `tests/test_session.py` + `tests/test_web_serve.py`, Ruff check/format touched files.
- Ship-task command update: `.agents/commands/ship-task.md` now requires focused validation plus full Python (`uv run pytest`) and full web (`bun run test:web`) test-suite verification before PR creation/merge; scoped tests alone are insufficient. Validation: diff inspection + Python text assertions.
- Git commit: committed only `.agents/commands/ship-task.md` as `82cde51` (`Update ship task validation requirements`); left pre-existing workspace changes uncommitted. Validation: checked cached diff and post-commit status.

- Kanban duplicate display implementation: added `replay_history` parameter through `run_single_turn()` and `run_single_turn_in_directory()`, disabled it in task worker, and added tests for default replay, no-replay provider restore, and task-run calls passing `False` while current-stage prompts remain visible. Validation: focused 4-test regression, full `tests/test_session.py tests/test_web_serve.py`, Ruff check/format touched files.
- IMPORTANT: Kanban task-card overflow fix: in `webapp/src/styles/board.css`, `.task-card` uses explicit `flex-grow: 0; flex-shrink: 0; flex-basis: auto;` (Vite/lightningcss optimizer silently drops the `flex: 0 0 auto` shorthand on this rule, so the longhand form is required for the no-shrink behavior to ship in the production CSS bundle as `flex:none`); plus `overflow: hidden`, `min-width: 0`, and `.task-card__title` uses `min-width: 0; max-width: 100%` with the existing ellipsis. This stops long titles from overflowing horizontally and prevents vertical cropping in crowded stages like DONE. Always rebuild via `bun run web:build` and grep the produced `index-*.css` to confirm the rule survived minification. Validation: `bun run lint`, `bun run typecheck`, `bun run web:build`.
