# Memory

- 2026-04-14: Established repo workflow notes in `AGENTS.md` for long-term memory and current-task tracking. `MEMORY.md` is the durable session log; `TODO.md` is the active-task checklist and should be reset when a prior task is fully checked.
- 2026-04-14: Simplified board stage defaults and command vocabulary across the app. Fresh boards now seed only `Backlog` and `Done`, the older `mode`/`mode_id` naming was renamed to `command`/`command_id` across backend, frontend, tests, and workspace command discovery, and repo-owned command examples now live under `.agents/commands/`. Validation during that work included Ruff, Bun typecheck/build, and pytest.
- 2026-04-14: Hardened the board stage editor and run flow in the web app. The frontend now normalizes stale command/profile references, retries with refreshed config only on genuine backend rejections, and prompts users to create a runnable stage when starting work from an otherwise empty board instead of surfacing a raw 400. Validation covered TypeScript and web builds; repo memory notes that `bun run lint` intermittently hung in this environment at that time.
- 2026-04-14: Refactored the FastAPI web server into smaller modules without changing the public `pbi_agent.web.serve` surface. Shared API schemas/helpers moved under `src/pbi_agent/web/api/`, app assembly moved to `app_factory.py`, runtime/bootstrap moved to `server_runtime.py`, and `tests/test_web_serve.py` gained broader route coverage, including config, session/task, static asset, and SPA routes. Validation included Ruff and pytest, with a later full repo pass also succeeding.
- 2026-04-16: Renamed the repo-owned interactive `chat` surface to `session` across backend, frontend, routes, hooks, Zustand state, tests, and docs, while intentionally leaving third-party `Chat Completions` terminology untouched. The live API moved from `/api/chat/*` to `/api/live-sessions/*`, the React route moved from `/chat/*` to `/sessions/*`, and the rebuilt FastAPI-served SPA bundle plus docs passed Ruff, Bun lint/typecheck/build, docs build, and pytest.
- 2026-04-16: Added the initial frontend test harness for the web app with Vitest, jsdom, and Testing Library, then expanded coverage for API helpers, Zustand state, websocket hooks, board flows, session flows, and settings flows. Supporting scripts live in `package.json`, shared test setup lives under `webapp/src/test/`, CI now installs Bun 1.3.11 and runs `bun run test:web`, and the Vite config uses `@vitejs/plugin-react` instead of the SWC plugin. Validation covered web tests, coverage runs, typecheck, and web builds; lint hangs were still observed intermittently in this environment during some of that work.
- 2026-04-16: Added modular provider-auth support, starting with OpenAI ChatGPT subscription sessions. Auth storage/services live under `src/pbi_agent/auth/`; provider runtime resolution can load account-backed sessions; the CLI gained `pbi-agent config providers auth-*` commands; the FastAPI web API exposes `/api/provider-auth/*`; and browser/device login flows plus the localhost callback server were implemented. Follow-up fixes preserved stored auth sessions during normal resolution, kept provider/API-key CLI overrides working over ChatGPT-backed profiles, added organization-id fallback during import, honored `expires_in` when refresh responses lack `exp`, and hardened WSL browser launching for OAuth URLs. Validation covered Ruff and targeted/full pytest runs.
- 2026-04-16: Brought the ChatGPT-backed OpenAI Responses transport much closer to local Codex/opencode behavior. The provider now uses backend-specific request shaping, strict ChatGPT tool schemas, Codex-style headers (`originator`, `User-Agent`, `session_id`), session-based prompt cache keys, SSE streaming decode, reconstruction of streamed output when the final payload omits `output`, and session-id preservation across tool iterations. Validation repeatedly covered `tests/test_openai_provider.py` and `tests/test_session.py` plus focused Ruff checks.
- 2026-04-16: Stabilized ChatGPT tool follow-up handling after several backend contract mismatches. Fixes included replaying prior `function_call` items before each `function_call_output`, capturing and replaying `x-codex-turn-state` within a turn, preserving raw backend `function_call` items in provider data, and finally replacing `previous_response_id` chaining with an explicit local transcript that replays prior request/response items, including reasoning and tool outputs. ChatGPT/Codex-specific transport logic now lives in `src/pbi_agent/providers/chatgpt_codex_backend.py`, keeping `OpenAIProvider` focused on generic Responses orchestration.
- 2026-04-16: Improved CLI web browser auto-open diagnostics and startup tolerance in `src/pbi_agent/cli.py`. The readiness wait now records resolved host, elapsed time, attempts, and the last socket error, waits longer before the first browser open, and performs one automatic retry window before warning. Validation covered focused Ruff and `tests/test_cli.py`.
- 2026-04-16: Compacted `MEMORY.md` by removing bookkeeping-only entries and consolidating stepwise implementation notes into durable summaries that preserve architecture, behavior changes, known environment quirks, and validation history for future sessions.
- 2026-04-16: Added webapp provider-auth management for ChatGPT-backed OpenAI providers. Frontend config/provider types now include `auth_mode`, `auth_status`, provider auth sessions/flows, and provider metadata auth modes; settings cards now show auth badges, account details, and Connect/Refresh/Disconnect actions; `ProviderModal` supports switching between API-key and ChatGPT-account auth; and a new `ProviderAuthFlowModal` runs browser/device auth flows with manual status checks plus automatic polling. Validation passed for `uv run ruff check .`, `uv run ruff format --check .`, `bun run lint`, `bun run typecheck`, `bun run test:web`, `bun run web:build`, and `uv run pytest tests/test_web_serve.py -q`. A full `uv run pytest` still has unrelated pre-existing failures in `tests/test_internal_config.py` and `tests/test_xai_provider.py` around runtime/default resolution.
- 2026-04-16: Fixed a provider-auth webapp regression where the manual `Check status` action updated only modal-local flow state and left the surrounding settings page stale after a completed browser/device login. `ProviderAuthFlowModal` now funnels both polling and manual checks through a shared completion handler that invokes the parent `onCompleted` invalidation path, and `SettingsPage.test.tsx` now verifies the provider card refreshes from `not connected` to `connected` after a manual status check completes the flow. Validation covered `bun run test:web -- webapp/src/components/settings/SettingsPage.test.tsx`, `bun run typecheck`, and `bun run lint`.
- 2026-04-18: Fixed the webapp ChatGPT browser sign-in redirect mismatch for provider auth. The CLI-compatible OAuth redirect is now the fixed root path `/auth/callback` instead of the previous dynamic `/api/provider-auth/{provider}/flows/{flow}/callback`, and the web session manager now resolves pending browser auth flows by OAuth `state` when the callback arrives. This avoids OpenAI rejecting the authorize step before our backend receives the callback. Validation covered `uv run ruff check` on the touched backend files plus `uv run pytest tests/test_openai_chatgpt_auth_provider.py tests/test_web_serve.py -q`.
- 2026-04-18: Followed up on the same browser-auth issue by normalizing loopback redirect hosts from `127.0.0.1` / `0.0.0.0` / `::1` to `localhost` in the web provider-auth route, since the CLI always authorizes against `localhost` and the web server commonly runs on `127.0.0.1`. Added a regression test that starts the web auth flow under `http://127.0.0.1:8000` and asserts the generated OAuth redirect is `http://localhost:8000/auth/callback`. Validation covered `uv run ruff check src/pbi_agent/web/api/routes/provider_auth.py tests/test_web_serve.py` and `uv run pytest tests/test_web_serve.py -q`.
- 2026-04-18: Replaced the webapp browser-auth callback path with the same dedicated localhost listener pattern used by the CLI. A new shared helper in `src/pbi_agent/auth/browser_callback.py` now owns the temporary callback server, `src/pbi_agent/auth/cli_flow.py` was refactored to use it, and the web session manager now starts a localhost callback listener per pending browser flow and completes the flow asynchronously when that listener receives the OAuth callback. The old FastAPI `/auth/callback` browser-auth path was removed from the web provider-auth route because OpenAI was still rejecting the web-generated `redirect_uri=http://localhost:8000/auth/callback` authorize URL even after path and host cleanup. Validation covered `uv run ruff check` on the touched auth/web files plus `uv run pytest tests/test_web_serve.py tests/test_openai_chatgpt_auth_provider.py tests/test_cli.py -q`.
- 2026-04-18: Addressed follow-up review issues in the web browser-auth listener flow. `WebSessionManager.start_provider_auth_flow()` now registers browser auth flows before starting the callback listener so eager callbacks cannot race with flow lookup, and pending browser flows now own a timeout timer that marks abandoned flows as failed and shuts down their dedicated listener instead of leaking threads/sockets until server shutdown. Added web regressions for eager callback completion during listener startup and for timed-out flow cleanup. Validation covered `uv run ruff check src/pbi_agent/web/session_manager.py tests/test_web_serve.py` and `uv run pytest tests/test_web_serve.py -q`.
- 2026-04-18: Fixed a webapp auth-modal UX bug where the completed ChatGPT account flow showed the correct connected state but kept the `Done` button disabled while the parent settings queries were still refetching. `ProviderAuthFlowModal` now allows dismissal after completion even if the background `onCompleted()` refresh promise is still pending, and `SettingsPage.test.tsx` now covers that case by holding the refresh promise open and asserting `Done` remains enabled. Validation passed for `bun run test:web`, `bun run typecheck`, and `bun run web:build`; `bun run lint` still hung in this environment, consistent with earlier repo notes.
- 2026-04-18: Tightened the same auth-modal fix after user verification. The modal no longer awaits the parent settings refresh at all before becoming dismissible; instead it updates local completed state immediately and fires `onCompleted()` in the background, while the regression test now clicks `Done` and verifies the modal actually closes even while the refresh promise is still unresolved. Validation again passed for `bun run test:web`, `bun run typecheck`, and `bun run web:build`; lint was not re-run because the repo's Bun/ESLint command has continued to hang intermittently in this environment.
